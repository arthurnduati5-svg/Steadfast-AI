datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

generator client {
  provider = "prisma-client-js"
}

model ChatMessage {
  id            String      @id @default(uuid())
  sessionId     String
  chatSession   ChatSession @relation(fields: [sessionId], references: [id], onDelete: Cascade)
  role          String
  content       String      @db.Text
  timestamp     DateTime    @default(now())
  messageNumber Int
  metadata      Json?
  revisionItems RevisionItem[] @relation("RevisionItemSourceMessage")

  @@index([sessionId])
  @@index([timestamp])
}

model ChatSession {
  id        String         @id @default(uuid())
  studentId String
  student   StudentProfile @relation(fields: [studentId], references: [userId], onDelete: Cascade)
  startTime DateTime       @default(now())
  endTime   DateTime?
  topic     String?
  isActive  Boolean        @default(true)
  messages  ChatMessage[]
  metadata  Json?
  createdAt DateTime       @default(now())
  updatedAt DateTime       @updatedAt
  voiceSessionUsages VoiceSessionUsage[]
  revisionItems RevisionItem[]

  @@index([studentId, updatedAt])
}

model Progress {
  id        String         @id @default(uuid())
  studentId String
  student   StudentProfile @relation(fields: [studentId], references: [userId], onDelete: Cascade)
  subject   String
  topic     String
  mastery   Int
  updatedAt DateTime       @updatedAt

  @@index([studentId])
}

model Mistake {
  id        String         @id @default(uuid())
  studentId String
  student   StudentProfile @relation(fields: [studentId], references: [userId], onDelete: Cascade)
  topic     String
  error     String
  attempts  Int            @default(1)
  lastSeen  DateTime       @default(now())

  @@index([studentId, lastSeen])
}

model GlobalMemory {
  id         String   @id @default(uuid())
  topic      String
  question   String
  correction String
  frequency  Int      @default(1)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}

model StudentProfile {
  userId               String                @id
  name                 String?
  email                String?
  gradeLevel           String?
  preferredLanguage    String                @default("English")
  topInterests         String[]              @default([])
  preferences          Json?
  favoriteShows        Json?
  profileCompleted     Boolean               @default(false)
  createdAt            DateTime              @default(now())
  updatedAt            DateTime              @updatedAt
  chatSessions         ChatSession[]
  progress             Progress[]
  mistakes             Mistake[]
  copilotPreferences   CopilotPreferences?
  voiceUsages          VoiceUsage[]
  voicePackageGrants   VoicePackageGrant[]
  voiceSessionUsages   VoiceSessionUsage[]
  voiceLedgerEntries   VoiceLedgerEntry[]
  latencyMetrics       TurnLatencyMetric[]
  revisionCollections  RevisionCollection[]
  revisionItems        RevisionItem[]
  mediaAssets          MediaAsset[]
}

model RevisionCollection {
  id          String          @id @default(uuid())
  userId      String
  student     StudentProfile  @relation(fields: [userId], references: [userId], onDelete: Cascade)
  title       String
  subject     String?
  topic       String?
  description String?
  kind        String?
  bundleSummary String?
  featuredItemIds Json?
  coverRef    Json?
  examFocus   Boolean         @default(false)
  pinned      Boolean         @default(false)
  sourceSessionId String?
  metadata    Json?
  items       RevisionItem[]
  createdAt   DateTime        @default(now())
  updatedAt   DateTime        @updatedAt

  @@index([userId, updatedAt])
  @@index([userId, title])
  @@index([userId, topic])
}

model RevisionItem {
  id              String             @id @default(uuid())
  userId          String
  student         StudentProfile     @relation(fields: [userId], references: [userId], onDelete: Cascade)
  sessionId       String?
  chatSession     ChatSession?       @relation(fields: [sessionId], references: [id], onDelete: SetNull)
  sourceMessageId String?
  sourceMessage   ChatMessage?       @relation("RevisionItemSourceMessage", fields: [sourceMessageId], references: [id], onDelete: SetNull)
  collectionId    String?
  collection      RevisionCollection? @relation(fields: [collectionId], references: [id], onDelete: SetNull)
  title           String
  summary         String             @db.Text
  content         String             @db.Text
  contentType     String
  subject         String?
  saveType        String?
  mediaType       String?
  topic           String?
  subtopic        String?
  tags            String[]           @default([])
  artifactLabels  String[]           @default([])
  selectedText    String?            @db.Text
  studentNote     String?            @db.Text
  isPinned        Boolean            @default(false)
  mastery         String?
  needsPractice   Boolean            @default(false)
  isMistakeBased  Boolean            @default(false)
  saveMode        String?
  lastPracticedAt DateTime?
  practiceCount   Int                @default(0)
  reviewStatus    String?
  lastReviewedAt  DateTime?
  nextReviewAt    DateTime?
  reviewCount     Int                @default(0)
  successCount    Int                @default(0)
  struggleCount   Int                @default(0)
  recentOutcome   String?
  confidenceTrend String?
  examPriority    Boolean            @default(false)
  sourceType      String?
  sourceUrl       String?
  imageUrl        String?
  audioUrl        String?
  videoId         String?
  videoTitle      String?
  transcriptSnippet String?          @db.Text
  audioRecapRef   Json?
  featuredRank    Int?
  bundleRole      String?
  sourceRefs      Json?
  mediaRefs       Json?
  metadata        Json?
  createdAt       DateTime           @default(now())
  updatedAt       DateTime           @updatedAt

  @@index([userId, updatedAt])
  @@index([userId, collectionId, updatedAt])
  @@index([userId, subject, updatedAt])
  @@index([userId, saveType, updatedAt])
  @@index([sessionId])
  @@index([sourceMessageId])
  @@index([contentType, updatedAt])
  @@index([userId, isPinned, updatedAt])
  @@index([userId, reviewStatus, nextReviewAt])
}

model MediaAsset {
  id           String         @id @default(uuid())
  userId       String
  student      StudentProfile @relation(fields: [userId], references: [userId], onDelete: Cascade)
  assetKind    String
  title        String
  summary      String?        @db.Text
  subject      String?
  topic        String?
  tags         String[]       @default([])
  language     String?
  sessionId    String?
  revisionItemId String?
  sourceUrl    String?
  videoId      String?
  dataUrl      String?        @db.Text
  assetUrl     String?
  thumbnailUrl String?
  durationSec  Int?
  recapText    String?        @db.Text
  keyPoints    Json?
  quickChecks  Json?
  metadata     Json?
  safetyStatus String?
  sourceTrust  String?
  dedupeKey    String?
  createdAt    DateTime       @default(now())
  updatedAt    DateTime       @updatedAt

  @@index([userId, updatedAt])
  @@index([userId, assetKind, updatedAt])
  @@index([revisionItemId])
  @@index([userId, topic])
  @@unique([userId, dedupeKey])
}

model RevisionReviewEvent {
  id             String   @id @default(uuid())
  userId         String
  revisionItemId String
  sessionId      String?
  eventType      String
  outcome        String?
  metadata       Json?
  createdAt      DateTime @default(now())

  @@index([userId, createdAt])
  @@index([revisionItemId, createdAt])
}

model StudyPlan {
  id                   String   @id @default(uuid())
  userId               String
  title                String
  scope                String
  subject              String?
  topic                String?
  subjects             Json?
  dateRangeStart       DateTime?
  dateRangeEnd         DateTime?
  summary              String?
  focusAreas           Json?
  recommendedBlocks    Json?
  suggestedCollectionIds Json?
  suggestedItemIds     Json?
  metadata             Json?
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt

  @@index([userId, updatedAt])
}

model StudyGoal {
  id          String   @id @default(uuid())
  userId      String
  studyPlanId String?
  title       String
  description String?
  goalType    String
  targetCount Int?
  currentCount Int     @default(0)
  status      String   @default("not_started")
  subject     String?
  topic       String?
  dueAt       DateTime?
  metadata    Json?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([userId, updatedAt])
  @@index([studyPlanId])
}

model InterventionEffectEvent {
  id                    String   @id @default(uuid())
  userId                String
  sessionId             String?
  subject               String?
  topic                 String?
  interventionType      String
  relatedRevisionItemId String?
  outcome               String?
  metadata              Json?
  createdAt             DateTime @default(now())

  @@index([userId, createdAt])
  @@index([userId, subject, topic])
}

model MetacognitiveEvent {
  id                 String   @id @default(uuid())
  userId             String
  sessionId          String?
  revisionItemId     String?
  sourceMessageId    String?
  eventType          String
  confidence         String?
  problemFraming     String?
  errorType          String?
  strategyPreference String?
  transferReadiness  String?
  note               String?  @db.Text
  metadata           Json?
  createdAt          DateTime @default(now())

  @@index([userId, createdAt])
  @@index([sessionId, createdAt])
  @@index([revisionItemId, createdAt])
  @@index([sourceMessageId, createdAt])
}

model LearningEffectEvent {
  id             String   @id @default(uuid())
  userId         String
  sessionId      String?
  subject        String?
  topic          String?
  revisionItemId String?
  messageId      String?
  eventType      String
  outcome        String?
  metadata       Json?
  createdAt      DateTime @default(now())

  @@index([userId, createdAt])
  @@index([userId, eventType])
  @@index([sessionId, createdAt])
  @@index([revisionItemId, createdAt])
}

model GrowthWeakTopicState {
  id                    String   @id
  userId                String
  subject               String
  topic                 String
  subtopic              String?
  weaknessScore         Float
  microMasteryLabel     String
  status                String
  weaknessReasonSummary String
  triggers              Json?
  lastStruggledAt       DateTime?
  lastReviewedAt        DateTime?
  nextReviewAt          DateTime?
  linkedRevisionIds     Json?
  linkedMediaIds        Json?
  linkedMistakePatternIds Json?
  recommendedAction     String
  snapshotDate          DateTime @db.Date
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  @@unique([userId, subject, topic, snapshotDate])
  @@index([userId, snapshotDate])
  @@index([userId, status, snapshotDate])
}

model GrowthMistakePatternState {
  id               String   @id
  userId           String
  subject          String
  patternKey       String
  title            String
  description      String
  examples         Json?
  recurrenceScore  Float
  status           String
  commonContext    String
  fixReminder      String
  linkedTopics     Json?
  linkedRevisionIds Json?
  lastSeenAt       DateTime?
  lastImprovedAt   DateTime?
  snapshotDate     DateTime @db.Date
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@unique([userId, patternKey, snapshotDate])
  @@index([userId, snapshotDate])
  @@index([userId, status, snapshotDate])
}

model GrowthMasteryTrendState {
  id               String   @id
  userId           String
  subject          String
  topic            String
  subtopic         String?
  trendStatus      String
  microMasteryLabel String
  confidenceScore  Float
  evidenceScore    Float
  trendSummary     String
  delta            Float?
  lastSeenAt       DateTime?
  snapshotDate     DateTime @db.Date
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@unique([userId, subject, topic, snapshotDate])
  @@index([userId, snapshotDate])
  @@index([userId, trendStatus, snapshotDate])
}

model GrowthRecommendationState {
  id               String   @id
  userId           String
  recommendationType String
  priorityScore    Float
  sourceType       String
  title            String
  reason           String
  primaryAction    Json
  secondaryAction  Json?
  linkedTopic      String?
  linkedRevisionId String?
  linkedMediaId    String?
  expiresAt        DateTime?
  snapshotDate     DateTime @db.Date
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([userId, snapshotDate])
  @@index([userId, recommendationType, snapshotDate])
}

model CopilotPreferences {
  id                String         @id @default(uuid())
  userId            String         @unique
  student           StudentProfile @relation(fields: [userId], references: [userId], onDelete: Cascade)
  preferredLanguage String
  interests         Json
  lastUpdatedAt     DateTime       @updatedAt
  createdAt         DateTime       @default(now())
}

model VoiceUsage {
  id          String         @id @default(uuid())
  studentId   String
  student     StudentProfile @relation(fields: [studentId], references: [userId], onDelete: Cascade)
  sessionId   String?
  durationSec Int
  startedAt   DateTime       @default(now())
  createdAt   DateTime       @default(now())
  source      String?

  @@index([studentId, startedAt])
  @@index([sessionId])
}

model VoicePackageGrant {
  id               String           @id @default(uuid())
  studentId        String
  student          StudentProfile   @relation(fields: [studentId], references: [userId], onDelete: Cascade)
  secondsGranted   Int
  secondsRemaining Int
  grantedAt        DateTime         @default(now())
  expiresAt        DateTime?
  source           String
  metadata         Json?
  ledgerEntries    VoiceLedgerEntry[]

  @@index([studentId, grantedAt])
  @@index([studentId, secondsRemaining, grantedAt])
  @@index([studentId, expiresAt])
}

model VoiceSessionUsage {
  id            String           @id @default(uuid())
  studentId     String
  student       StudentProfile   @relation(fields: [studentId], references: [userId], onDelete: Cascade)
  chatSessionId String?
  chatSession   ChatSession?     @relation(fields: [chatSessionId], references: [id], onDelete: SetNull)
  startedAt     DateTime         @default(now())
  endedAt       DateTime?
  billedSeconds Int              @default(0)
  mode          VoiceSessionMode
  stopReason    String?
  metadata      Json?
  ledgerEntries VoiceLedgerEntry[]

  @@index([studentId, startedAt])
  @@index([chatSessionId])
  @@index([studentId, endedAt])
}

model VoiceLedgerEntry {
  id                  String            @id @default(uuid())
  studentId           String
  student             StudentProfile    @relation(fields: [studentId], references: [userId], onDelete: Cascade)
  createdAt           DateTime          @default(now())
  type                String
  secondsDelta        Int
  relatedSessionId    String?
  relatedSession      VoiceSessionUsage? @relation(fields: [relatedSessionId], references: [id], onDelete: SetNull)
  relatedGrantId      String?
  relatedGrant        VoicePackageGrant? @relation(fields: [relatedGrantId], references: [id], onDelete: SetNull)
  balanceAfterSeconds Int
  metadata            Json?

  @@index([studentId, createdAt])
  @@index([relatedSessionId])
  @@index([relatedGrantId])
  @@index([studentId, type, createdAt])
}

model TutorState {
  id                String    @id @default(cuid())
  studentId         String
  schoolId          String    @default("")
  sessionId         String?
  activeSubject     String?
  activeTopic       String?
  activeSkillIds    String[]  @default([])
  activeArtifactIds String[]  @default([])
  activeVideoId     String?
  learningMode      String    @default("learn")
  primaryLanguage   String    @default("en")
  supportLanguage   String?
  stateQuality      String    @default("no_data_yet")
  evidence          Json      @default("[]")
  stateVersion      Int       @default(1)
  lastResolvedAt    DateTime?
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  @@index([schoolId, studentId])
  @@index([sessionId])
}

model SafetyAlert {
  id                  String            @id @default(uuid())
  studentId           String
  sessionId           String?
  messageId           String?
  category            String
  severity            SafetySeverity
  confidence          Float
  riskScore           Int?
  excerptRedacted     String            @db.Text
  status              SafetyAlertStatus @default(open)
  counselorNotified   Boolean           @default(false)
  counselorNotifiedAt DateTime?
  metadata            Json?
  createdAt           DateTime          @default(now())
  updatedAt           DateTime          @updatedAt

  @@index([studentId, createdAt])
  @@index([severity, createdAt])
  @@index([status, createdAt])
  @@index([sessionId])
  @@index([messageId])
}

model SafetyEventAudit {
  id         String   @id @default(uuid())
  actorId    String
  actorRole  String
  action     String
  targetType String
  targetId   String?
  metadata   Json?
  createdAt  DateTime @default(now())

  @@index([actorId, createdAt])
  @@index([targetType, createdAt])
  @@index([action, createdAt])
}

model TurnLatencyMetric {
  id               String                 @id @default(uuid())
  studentId        String
  student          StudentProfile         @relation(fields: [studentId], references: [userId], onDelete: Cascade)
  sessionId        String?
  turnId           String
  responseMode     String                 @default("default")
  route            String                 @default("copilot_chat")
  forceWebSearch   Boolean                @default(false)
  languageMode     String?
  source           String?
  sttMs            Int?
  firstTokenMs     Int?
  doneMs           Int?
  totalMs          Int?
  ttsStartMs       Int?
  aiMs             Int?
  inputChars       Int?
  outputChars      Int?
  thresholdBreached Boolean               @default(false)
  thresholdLevel   LatencyThresholdLevel?
  metadata         Json?
  createdAt        DateTime               @default(now())
  updatedAt        DateTime               @updatedAt
  alerts           LatencyThresholdAlert[]

  @@unique([studentId, turnId])
  @@index([studentId, createdAt])
  @@index([sessionId, createdAt])
  @@index([responseMode, createdAt])
  @@index([route, createdAt])
  @@index([thresholdBreached, createdAt])
}

model LatencyThresholdAlert {
  id          String                @id @default(uuid())
  metricId    String
  metric      TurnLatencyMetric     @relation(fields: [metricId], references: [id], onDelete: Cascade)
  studentId   String
  severity    LatencyThresholdLevel
  thresholdType String
  thresholdMs Int
  observedMs  Int
  acknowledged Boolean              @default(false)
  metadata    Json?
  createdAt   DateTime              @default(now())
  updatedAt   DateTime              @updatedAt

  @@unique([metricId, thresholdType])
  @@index([studentId, createdAt])
  @@index([severity, createdAt])
  @@index([acknowledged, createdAt])
  @@index([metricId])
}

enum VoiceSessionMode {
  listening_only
  listening_plus_tts
}

enum SafetySeverity {
  low
  medium
  high
  critical
}

enum SafetyAlertStatus {
  open
  reviewing
  resolved
  dismissed
}

enum LatencyThresholdLevel {
  warn
  critical
}
// ── Universal Islamic Tutor: School Auth Bridge, Tutor Learner Identity Map v1 ──

model TutorLearnerIdentityMap {
  id                String   @id @default(uuid())
  tutorLearnerId    String   @unique
  externalUserId    String?
  externalStudentId String
  externalTeacherId String?
  schoolId          String
  classId           String?
  grade             String?
  role              String   @default("student")
  status            String   @default("active")
  reasonCodes       Json     @default("[]")
  firstSeenAt       DateTime @default(now())
  lastSeenAt        DateTime @updatedAt

  @@unique([externalStudentId, schoolId])
  @@index([schoolId])
  @@index([externalUserId])
  @@index([classId])
}

model TutorSession {
  id                  String   @id @default(uuid())
  tutorLearnerId      String
  schoolId            String
  externalStudentId   String
  status              String   @default("active")
  sessionPurpose      String   @default("copilot_default")
  displayMode         String?
  lastActiveSchoolPage String?
  lastSeenAt          DateTime @default(now())
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  @@index([tutorLearnerId, status])
  @@index([tutorLearnerId, schoolId, sessionPurpose, status])
  @@index([schoolId])
}

// ── Durable Audit Persistence, Operational Event Storage, Privacy-Safe Review Trail v1 ──

model DurableAuditEvent {
  id                String   @id @default(cuid())
  category          String
  eventType         String
  severity          String
  visibility        String
  actorType         String
  actorIdHash       String?
  studentIdHash     String?
  schoolIdHash      String?
  classIdHash       String?
  requestId         String?
  traceId           String?
  route             String?
  method            String?
  serviceName       String?
  operation         String?
  safeSummary       String
  safeMetadataJson  Json?
  redactionJson     Json
  occurredAt        DateTime
  persistedAt       DateTime @default(now())

  @@index([category, occurredAt])
  @@index([visibility, occurredAt])
  @@index([requestId])
  @@index([traceId])
  @@index([schoolIdHash, occurredAt])
  @@index([studentIdHash, occurredAt])
}

// ── Teacher Intervention Backend Hardening v1 ──

model TeacherInterventionAssignment {
  id                    String   @id @default(cuid())
  sourceRecommendationId String?
  teacherId             String
  studentId             String
  schoolId              String
  classId               String?
  subject               String?
  topic                 String?
  skillId               String?
  skillLabel            String?
  artifactId            String?
  questionId            String?
  videoId               String?
  watchSessionId        String?
  analyticsEvidenceRefs Json     @default("[]")
  actionType            String
  priority              String   @default("medium")
  status                String   @default("draft")
  outcomeStatus         String   @default("not_started")
  teacherReason         String
  learnerFacingInstruction String?
  teacherPrivateNote    String?
  dueAt                 DateTime?
  followUpRequired      Boolean  @default(false)
  followUpStatus        String   @default("not_required")
  evidenceSummary       String   @default("")
  warnings              Json     @default("[]")
  metadata              Json     @default("{}")

  startedAt             DateTime?
  completedAt           DateTime?
  cancelledAt           DateTime?
  closedAt              DateTime?

  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  @@index([schoolId, studentId])
  @@index([schoolId, teacherId])
  @@index([schoolId, studentId, status])
  @@index([schoolId, teacherId, priority, status])
  @@index([schoolId, classId])
  @@index([dueAt])
}

model TeacherInterventionAuditEvent {
  id                  String   @id @default(cuid())
  interventionId      String
  actorId             String
  actorRole           String   @default("teacher")
  schoolId            String
  classId             String?
  studentId           String
  eventType           String
  eventTime           DateTime @default(now())
  safeSummary         String
  redactedPayloadJson String   @default("{}")
  metadata            Json     @default("{}")
  requestId           String?
  createdAt           DateTime @default(now())

  @@index([schoolId, studentId])
  @@index([schoolId, interventionId])
  @@index([schoolId, actorId])
  @@index([eventType])
  @@index([eventTime])
}

// ── Structured Artifact Pipeline v1 ──

model LearningArtifact {
  id                String   @id @default(cuid())
  schoolId          String
  ownerStudentId    String?
  ownerTeacherId    String?
  classId           String?
  kind              String
  accessScope       String
  title             String
  description       String?
  originalFileName  String?
  mimeType          String?
  sizeBytes         Int?
  source            String
  parseStatus       String   @default("not_parsed")
  structureQuality  String   @default("unavailable")
  blockCount        Int      @default(0)
  questionCount     Int      @default(0)
  diagramCount      Int      @default(0)
  answerKeyCount    Int      @default(0)
  contentFingerprint String
  warnings          Json     @default("[]")
  parsedAt          DateTime?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  blocks            LearningArtifactBlock[]

  @@index([schoolId])
  @@index([schoolId, ownerStudentId])
  @@index([schoolId, classId])
  @@index([contentFingerprint])
}

model LearningArtifactBlock {
  id                 String   @id @default(cuid())
  artifactId          String
  schoolId            String
  kind                String
  order               Int
  text                String?  @db.Text
  normalizedText      String?  @db.Text
  summary             String?
  pageNumber          Int?
  sectionTitle        String?
  headingPath         Json     @default("[]")
  confidence          Float    @default(0)
  provenance          Json
  educationalTags     Json     @default("{}")
  metadata            Json     @default("{}")
  contentFingerprint  String
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  artifact            LearningArtifact @relation(fields: [artifactId], references: [id], onDelete: Cascade)

  @@index([schoolId, artifactId])
  @@index([kind])
  @@index([contentFingerprint])
}

// ── Durable Learner Memory v1 ──

model LearnerMemoryItem {
  id               String   @id @default(cuid())
  schoolId         String
  studentId        String

  kind             String
  status           String   @default("active")
  visibility       String   @default("system_only")

  subject          String?
  topic            String?
  skillIds         Json     @default("[]")

  label            String
  summary          String
  tutorUse         String

  evidence         Json     @default("[]")
  confidence       String   @default("low")
  confidenceScore  Float    @default(0.3)

  firstObservedAt  DateTime @default(now())
  lastObservedAt   DateTime @default(now())
  observationCount Int      @default(1)

  source           String
  sourceEventIds   Json     @default("[]")

  artifactIds      Json     @default("[]")
  artifactBlockIds Json     @default("[]")

  expiresAt        DateTime?
  softDeletedAt    DateTime?
  deletedReason    String?

  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([schoolId, studentId])
  @@index([schoolId, studentId, kind])
  @@index([schoolId, studentId, subject, topic])
  @@index([status])
  @@unique([schoolId, studentId, kind, subject, topic])
}

model LearningEvent {
  id              String   @id @default(cuid())
  schoolId        String
  studentId       String
  sessionId       String?

  kind            String
  subject         String?
  topic           String?
  skillIds        Json     @default("[]")

  artifactId      String?
  artifactBlockId String?

  promptSummary   String?
  responseSummary String?
  outcomeSummary  String?

  signals         Json     @default("[]")
  source          String
  privacyLevel    String   @default("medium")

  createdAt       DateTime @default(now())

  @@index([schoolId, studentId])
  @@index([schoolId, studentId, sessionId])
  @@index([schoolId, studentId, kind])
  @@index([artifactId])
}

// ── Practice and Mastery Scaffolding v1 ──

model PracticeAttempt {
  id                   String   @id @default(cuid())
  schoolId             String
  studentId            String
  sessionId            String?

  kind                 String
  status               String   @default("submitted")
  outcome              String   @default("not_evaluated")

  subject              String?
  topic                String?
  skillIds             Json     @default("[]")

  promptSummary        String
  learnerAnswerSummary String?
  expectedAnswerSummary String?
  feedbackSummary      String?

  artifactId           String?
  artifactBlockId      String?
  sourceQuestionId     String?

  hintsRequested       Int      @default(0)
  attemptNumber        Int      @default(1)
  timeSpentSeconds     Int?

  confidence           Float    @default(0.3)

  misconceptionSignals Json     @default("[]")
  evidence             Json     @default("[]")

  createdAt            DateTime @default(now())
  evaluatedAt          DateTime?

  @@index([schoolId, studentId])
  @@index([schoolId, studentId, subject, topic])
  @@index([artifactId])
}

model SkillMasterySnapshot {
  id                  String   @id @default(cuid())
  schoolId            String
  studentId           String

  subject             String
  topic               String
  skillId             String
  skillLabel          String

  level               String   @default("not_started")
  status              String   @default("active")

  confidenceScore     Float    @default(0)
  evidenceCount       Int      @default(0)
  attemptCount        Int      @default(0)
  correctCount        Int      @default(0)
  partialCount        Int      @default(0)
  incorrectCount      Int      @default(0)
  misconceptionCount  Int      @default(0)

  lastAttemptAt       DateTime?
  lastCorrectAt       DateTime?
  lastIncorrectAt     DateTime?

  nextReviewAt        DateTime?
  reviewIntervalDays  Int?

  evidence            Json     @default("[]")

  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  @@unique([schoolId, studentId, subject, topic, skillId])
  @@index([schoolId, studentId])
  @@index([schoolId, studentId, subject, topic])
  @@index([status])
}

model PracticeMisconceptionSignal {
  id                 String   @id @default(cuid())
  schoolId           String
  studentId          String

  subject            String?
  topic              String?
  skillIds           Json     @default("[]")

  label              String
  summary            String

  evidenceAttemptIds Json     @default("[]")
  evidenceMemoryIds  Json     @default("[]")

  confidenceScore    Float    @default(0.3)
  observationCount   Int      @default(1)

  status             String   @default("active")

  firstObservedAt    DateTime @default(now())
  lastObservedAt     DateTime @default(now())
  updatedAt          DateTime @updatedAt

  @@index([schoolId, studentId])
  @@index([schoolId, studentId, subject, topic])
  @@index([status])
}

model SpacedReviewItem {
  id             String   @id @default(cuid())
  schoolId       String
  studentId      String

  subject        String
  topic          String
  skillId        String
  skillLabel     String

  masteryId      String?

  dueAt          DateTime
  intervalDays   Int
  status         String   @default("scheduled")
  reason         String

  createdAt      DateTime @default(now())
  completedAt    DateTime?

  @@index([schoolId, studentId])
  @@index([schoolId, studentId, dueAt])
  @@index([status])
}

// ── PostgreSQL Tutor Data Schema, Long-Retention Storage Foundation v1 (Task 003) ──
// ── Extended for Conversation Archive Runtime, Safe Memory, Retention Policy v1 (Task 005) ──

model ConversationArchiveRecord {
  id              String   @id @default(cuid())
  schoolId        String
  tutorLearnerId  String
  externalStudentId String?
  sessionId       String?
  archiveDate     DateTime @default(now())
  retentionUntil  DateTime
  visibilityTier  String   @default("system_only")
  redactionStatus String   @default("not_redacted")
  summary         String?  @db.Text
  messageCount    Int      @default(0)
  subjectTags     String[] @default([])
  firstMessageAt  DateTime
  lastMessageAt   DateTime
  tokenCount      Int?
  metadata        Json?

  // ── Task 005 extension fields ──
  rawContent      String?  @db.Text
  redactedContent String?  @db.Text
  redactionApplied Boolean  @default(false)
  storagePolicy   String   @default("retained_raw")
  sender          String   @default("learner")
  sourceSurface   String   @default("unknown")
  safetyTags      String[] @default([])
  topicId         String?
  subjectId       String?
  curriculumTrack String   @default("unknown")

  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([schoolId, tutorLearnerId])
  @@index([retentionUntil])
  @@index([visibilityTier])
  @@index([schoolId, archiveDate])
  @@index([tutorLearnerId, archiveDate])
  @@index([storagePolicy])
  @@index([sender])
}

model SafeMemorySummary {
  id                String   @id @default(cuid())
  schoolId          String
  tutorLearnerId    String
  externalStudentId String?
  kind              String
  summary           String   @db.Text
  confidenceScore   Float    @default(0.3)
  observationCount  Int      @default(1)
  sourceArchiveIds  String[] @default([])
  firstObservedAt   DateTime @default(now())
  lastObservedAt    DateTime @default(now())
  expiresAt         DateTime?
  metadata          Json?

  // ── Task 005 extension fields ──
  subjectId         String?
  topicId           String?
  curriculumTrack   String   @default("unknown")
  strengths         String[] @default([])
  weakAreas         String[] @default([])
  commonMistakes    String[] @default([])

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([schoolId, tutorLearnerId])
  @@index([expiresAt])
  @@index([kind])
  @@index([schoolId, kind])
  @@index([subjectId])
  @@index([topicId])
}

model LearnerPreferenceFeedbackRecord {
  id                  String   @id @default(uuid())
  schoolId            String
  tutorLearnerId      String
  studentId           String?
  sessionId           String?
  recommendationId    String
  recommendationType  String?
  feedbackType        String?
  subject             String?
  topic               String?
  skillTag            String?
  safeReasonCodes     Json?
  safetyFlags         Json?
  privacyMetadata     Json?
  supportLevelAdjustment String?
  difficultyAdjustment   String?
  stepSizeAdjustment     String?
  recommendationBiases   Json?
  profileSnapshot        Json?
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  @@index([schoolId, tutorLearnerId])
  @@index([tutorLearnerId])
  @@index([schoolId])
}

model RecommendationInteractionRecord {
  id                  String   @id @default(uuid())
  schoolId            String
  tutorLearnerId      String
  studentId           String?
  sessionId           String?
  recommendationId    String?
  recommendationType  String?
  interactionType     String
  subject             String?
  topic               String?
  skillTag            String?
  safeReasonCodes     Json?
  safetyFlags         Json?
  privacyMetadata     Json?
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  @@index([schoolId, tutorLearnerId])
  @@index([tutorLearnerId])
  @@index([interactionType])
}

model AdaptiveRecommendationProfileRecord {
  id                      String   @id @default(uuid())
  schoolId                String
  tutorLearnerId          String   @unique
  preferredSupportLevel   String   @default("guided_support")
  preferredStepSize       String   @default("standard")
  practiceModePreference  String   @default("balanced")
  challengeReadinessSignal String  @default("medium")
  foundationReviewPreference String @default("medium")
  hintFrequencySignal      String  @default("medium")
  difficultyCalibration    Float   @default(0.5)
  recentTooHardCount       Int     @default(0)
  recentTooEasyCount       Int     @default(0)
  recentConfusionCount     Int     @default(0)
  recentChallengeRequestCount Int @default(0)
  recentTeacherHelpRequestCount Int @default(0)
  profileSnapshot          Json?
  privacyMetadata          Json?
  createdAt               DateTime @default(now())
  updatedAt               DateTime @updatedAt

  @@index([schoolId])
  @@index([schoolId, tutorLearnerId])
}

model PersonalizationAuditRecord {
  id                        String   @id @default(uuid())
  schoolId                  String
  tutorLearnerId            String
  studentId                 String?
  sessionId                 String?
  recommendationId          String
  feedbackType              String?
  interactionType           String?
  tuningReasonCodes         Json?
  safetyFlags               Json?
  privacyDecision           String   @default("teacher_safe_learner_preference")
  deenSensitivityHandled    Boolean  @default(false)
  safeguardingBoundaryApplied Boolean @default(false)
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt

  @@index([schoolId, tutorLearnerId])
  @@index([tutorLearnerId])
  @@index([createdAt])
}

// ── Adaptive Challenge, Remediation, Difficulty Calibration v1 (Task 015) ──

model AdaptiveChallengeRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  tutorLearnerId        String
  studentId             String?
  sessionId             String?

  subject               String
  topic                 String?
  skillTag              String?

  challengeType         String
  difficultyLevel       String
  status                String   @default("active")

  learnerPrompt         String
  socraticOpeningQuestion String?
  allowedHints          Json     @default("[]")
  safeEvidenceRefs      Json     @default("[]")
  reasonCodes           Json     @default("[]")
  privacyMetadata       Json     @default("{}")

  attemptSubmitted      Boolean  @default(false)
  attemptOutcome        String?
  hintLevelUsed         Int      @default(0)

  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  @@index([schoolId, tutorLearnerId])
  @@index([schoolId, tutorLearnerId, status])
  @@index([tutorLearnerId])
}

model RemediationPathRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  tutorLearnerId        String
  studentId             String?
  sessionId             String?

  subject               String
  topic                 String?
  skillTag              String?
  blockingSkillTag      String?
  prerequisiteSkillTags Json     @default("[]")

  steps                 Json     @default("[]")
  currentStepIndex      Int      @default(0)
  status                String   @default("active")
  reasonCodes           Json     @default("[]")
  privacyMetadata       Json     @default("{}")

  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  @@index([schoolId, tutorLearnerId])
  @@index([schoolId, tutorLearnerId, status])
  @@index([tutorLearnerId])
}

model DifficultyCalibrationRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  tutorLearnerId        String
  subject               String
  topic                 String?
  skillTag              String?

  currentDifficultyLevel String  @default("standard")
  supportLevel          String   @default("moderate")

  calibrationSignals    Json     @default("[]")
  recentSuccessCount    Int      @default(0)
  recentStruggleCount   Int      @default(0)
  recentHintDependency  Int      @default(0)
  recentTooHardCount    Int      @default(0)
  recentTooEasyCount    Int      @default(0)

  lastCalibratedAt      DateTime @default(now())
  privacyMetadata       Json     @default("{}")

  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  @@unique([schoolId, tutorLearnerId, subject, skillTag])
  @@index([schoolId, tutorLearnerId])
  @@index([tutorLearnerId])
}

// ── Student Learning Session State & Event Records v1 (Task 016) ──

model StudentLearningSessionState {
  id                      String   @id @default(cuid())
  schoolId                String
  tutorLearnerId          String
  studentId               String?
  externalStudentId       String?

  status                  String   @default("active")
  currentMode             String   @default("session_start")
  previousMode            String?
  subject                 String?
  topic                   String?
  skillTag                String?

  activeChallengeId       String?
  activeRemediationPathId String?
  activeRevisionItemId    String?

  supportLevel            String?
  difficultyLevel         String?
  safeProgressSummary     String?
  safeEvidenceRefs        Json     @default("[]")
  reasonCodes             Json     @default("[]")
  privacyMetadata         Json     @default("{}")

  lastTransitionAt        DateTime @default(now())
  createdAt               DateTime @default(now())
  updatedAt               DateTime @updatedAt

  @@index([schoolId, tutorLearnerId])
  @@index([schoolId, tutorLearnerId, status])
  @@index([tutorLearnerId, status])
}

model StudentLearningSessionEvent {
  id                  String   @id @default(cuid())
  schoolId            String
  tutorLearnerId      String
  sessionId           String

  eventType           String
  previousMode        String?
  nextMode            String?
  subject             String?
  topic               String?
  skillTag            String?

  safeEventSummary    String?
  safeEvidenceRefs    Json     @default("[]")
  reasonCodes         Json     @default("[]")
  privacyMetadata     Json     @default("{}")

  createdAt           DateTime @default(now())

  @@index([schoolId, tutorLearnerId])
  @@index([schoolId, tutorLearnerId, sessionId])
  @@index([sessionId, createdAt])
}

// ── Task 021: School Integration Durable Persistence v1 ──

model SchoolRosterSyncJobRecord {
  id                 String   @id @default(cuid())
  schoolId           String
  syncBatchId        String
  idempotencyKey     String?
  status             String
  startedAt          DateTime @default(now())
  completedAt        DateTime?
  createdMappings    Int      @default(0)
  updatedMappings    Int      @default(0)
  inactivatedMappings Int     @default(0)
  reactivatedMappings Int    @default(0)
  conflictCount      Int      @default(0)
  quarantinedCount   Int      @default(0)
  reasonCodes        Json     @default("[]")
  privacyMetadata    Json     @default("{}")
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  @@unique([schoolId, syncBatchId])
  @@index([schoolId])
  @@index([status])
  @@index([createdAt])
}

model SchoolRosterSyncConflictRecord {
  id                 String   @id @default(cuid())
  schoolId           String
  syncBatchId        String
  conflictType       String
  externalUserId     String?
  externalStudentId  String?
  externalTeacherId  String?
  tutorLearnerId     String?
  status             String   @default("quarantined")
  safeSummary        String
  reasonCodes        Json     @default("[]")
  privacyMetadata    Json     @default("{}")
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  @@index([schoolId])
  @@index([syncBatchId])
  @@index([status])
}

model SchoolIntegrationIdempotencyRecord {
  id                 String   @id @default(cuid())
  schoolId           String
  idempotencyKey     String
  operation          String
  requestHash        String
  status             String   @default("in_progress")
  safeResultSummary  String?
  reasonCodes        Json     @default("[]")
  expiresAt          DateTime?
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  @@unique([schoolId, operation, idempotencyKey])
  @@index([expiresAt])
  @@index([status])
}

model SchoolIntegrationAuditRecord {
  id                 String   @id @default(cuid())
  schoolId           String?
  actorId            String?
  actorRole          String
  externalUserId     String?
  tutorLearnerId     String?
  eventType          String
  operation          String?
  decision           String
  reasonCodes        Json     @default("[]")
  privacyMetadata    Json     @default("{}")
  requestId          String?
  correlationId      String?
  createdAt          DateTime @default(now())

  @@index([schoolId])
  @@index([eventType])
  @@index([createdAt])
}

// ── Task 022: Curriculum & Content Governance Durable Persistence v1 ──

model CurriculumVersionRecord {
  id               String   @id @default(uuid())
  schoolId         String?
  curriculumFamily String
  versionCode      String
  title            String
  status           String   @default("draft")
  effectiveFrom    DateTime?
  effectiveTo      DateTime?
  metadata         Json?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  topics           CurriculumTopicRecord[]

  @@index([schoolId])
  @@index([curriculumFamily])
  @@index([status])
  @@unique([schoolId, curriculumFamily, versionCode])
}

model CurriculumTopicRecord {
  id                   String   @id @default(uuid())
  curriculumVersionId  String
  subject              String
  stage                String?
  topicCode            String?
  title                String
  descriptionSafe      String?
  status               String   @default("active")
  metadata             Json?
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt

  curriculumVersion    CurriculumVersionRecord @relation(fields: [curriculumVersionId], references: [id], onDelete: Cascade)
  skills               CurriculumSkillRecord[]

  @@index([curriculumVersionId])
  @@index([subject])
  @@index([status])
}

model CurriculumSkillRecord {
  id                      String   @id @default(uuid())
  curriculumTopicId       String
  skillCode               String?
  title                   String
  studentSafeDescription  String?
  teacherSafeDescription  String?
  difficultyBand          String?
  status                  String   @default("active")
  metadata                Json?
  createdAt               DateTime @default(now())
  updatedAt               DateTime @updatedAt

  curriculumTopic         CurriculumTopicRecord @relation(fields: [curriculumTopicId], references: [id], onDelete: Cascade)
  learningObjectives      LearningObjectiveRecord[]
  prerequisiteLinksFrom   PrerequisiteLinkRecord[] @relation("PrerequisiteLinkFrom")
  prerequisiteLinksTo     PrerequisiteLinkRecord[] @relation("PrerequisiteLinkTo")

  @@index([curriculumTopicId])
  @@index([status])
}

model LearningObjectiveRecord {
  id                        String   @id @default(uuid())
  curriculumSkillId         String
  objectiveCode             String?
  title                     String
  studentSafeDescription    String?
  teacherSafeDescription    String?
  status                    String   @default("active")
  sourceRequirement         Json?
  metadata                  Json?
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt

  curriculumSkill           CurriculumSkillRecord @relation(fields: [curriculumSkillId], references: [id], onDelete: Cascade)

  @@index([curriculumSkillId])
  @@index([status])
}

model PrerequisiteLinkRecord {
  id               String   @id @default(uuid())
  fromSkillId      String
  toSkillId        String
  relationshipType String
  reasonCodes      Json?
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  fromSkill        CurriculumSkillRecord @relation("PrerequisiteLinkFrom", fields: [fromSkillId], references: [id], onDelete: Cascade)
  toSkill          CurriculumSkillRecord @relation("PrerequisiteLinkTo", fields: [toSkillId], references: [id], onDelete: Cascade)

  @@index([fromSkillId])
  @@index([toSkillId])
}

model ApprovedSourceRecord {
  id                String   @id @default(uuid())
  schoolId          String?
  sourceKey         String
  title             String
  sourceType        String
  curriculumFamily  String
  subject           String?
  stage             String?
  topic             String?
  deenCategory      String?
  trustLevel        String
  approvalStatus    String   @default("pending_review")
  approvedByActorId String?
  approvedByRole    String?
  approvedAt        DateTime?
  reviewRequired    Boolean  @default(true)
  restrictedUse     Boolean  @default(false)
  citationMetadata  Json?
  metadata          Json?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([schoolId])
  @@index([curriculumFamily])
  @@index([approvalStatus])
  @@index([trustLevel])
}

model ContentItemRecord {
  id                    String   @id @default(uuid())
  schoolId              String?
  curriculumVersionId   String?
  topicId               String?
  skillId               String?
  learningObjectiveId   String?
  sourceId              String?
  contentType           String
  status                String   @default("draft")
  sensitivity           String   @default("basic")
  studentSafeContent    String?  @db.Text
  teacherSafeContent    String?  @db.Text
  answerKeyProtected    Boolean  @default(false)
  teacherOnly           Boolean  @default(false)
  reviewState           String   @default("not_reviewed")
  reasonCodes           Json?
  metadata              Json?
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  @@index([schoolId])
  @@index([curriculumVersionId])
  @@index([topicId])
  @@index([skillId])
  @@index([learningObjectiveId])
  @@index([status])
  @@index([reviewState])
}

model ContentGapRecord {
  id               String   @id @default(uuid())
  schoolId         String?
  curriculumFamily String
  subject          String?
  topic            String?
  skill            String?
  gapType          String
  status           String   @default("open")
  safeSummary      String
  reasonCodes      Json     @default("[]")
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  @@index([schoolId])
  @@index([curriculumFamily])
  @@index([gapType])
  @@index([status])
}

model ContentGovernanceAuditRecord {
  id                  String   @id @default(uuid())
  schoolId            String?
  actorId             String?
  actorRole           String
  eventType           String
  curriculumFamily    String?
  curriculumVersionId String?
  sourceId            String?
  contentItemId       String?
  decision            String
  reasonCodes         Json     @default("[]")
  privacyMetadata     Json?
  requestId           String?
  correlationId       String?
  createdAt           DateTime @default(now())

  @@index([schoolId])
  @@index([eventType])
  @@index([curriculumFamily])
  @@index([createdAt])
}

model ContentReviewRecord {
  id                String   @id @default(uuid())
  schoolId          String?
  targetType        String
  targetId          String
  reviewState       String   @default("not_reviewed")
  reviewedByActorId String?
  reviewedByRole    String?
  reviewedAt        DateTime?
  decision          String
  reasonCodes       Json?
  safeNotes         String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([schoolId])
  @@index([targetType, targetId])
  @@index([reviewState])
}

model OpsIncident {
  id                         String   @id @default(uuid())
  schoolId                   String?
  category                   String
  severity                   String
  status                     String   @default("open")
  source                     String?
  safeTitle                  String
  safeSummary                String
  redactionStatus            String   @default("redacted")
  reasonCodes                Json     @default("[]")
  affectedComponents         Json     @default("[]")
  recommendedOwnerRole       String   @default("admin")
  studentSafetyRelevant      Boolean  @default(false)
  privacyRelevant            Boolean  @default(false)
  deenGovernanceRelevant     Boolean  @default(false)
  metadataSafeJson           Json?
  detectedAt                 DateTime @default(now())
  classifiedAt               DateTime?
  acknowledgedAt             DateTime?
  investigatingAt            DateTime?
  mitigatedAt                DateTime?
  resolvedAt                 DateTime?
  falsePositiveAt            DateTime?
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt

  auditRecords               OpsIncidentAudit[]

  @@index([schoolId])
  @@index([category])
  @@index([severity])
  @@index([status])
  @@index([detectedAt])
}

model OpsIncidentAudit {
  id              String   @id @default(uuid())
  incidentId      String
  schoolId        String?
  actorRole       String   @default("unknown")
  actorIdSafe     String?
  action          String
  previousStatus  String?
  newStatus       String   @default("open")
  safeNote        String?
  metadataSafeJson Json?
  redactionStatus String   @default("redacted")
  createdAt       DateTime @default(now())
  requestId       String?
  correlationId   String?

  incident        OpsIncident @relation(fields: [incidentId], references: [id], onDelete: Cascade)

  @@index([incidentId])
  @@index([schoolId])
  @@index([action])
  @@index([createdAt])
}

model OpsMetricSnapshot {
  id                     String   @id @default(uuid())
  schoolId               String?
  requestCount           Int      @default(0)
  errorCount             Int      @default(0)
  rateLimitCount         Int      @default(0)
  p50LatencyMs           Int?
  p95LatencyMs           Int?
  p99LatencyMs           Int?
  statusCodeFamilyCounts Json     @default("{}")
  routeFamilyCounts      Json     @default("{}")
  incidentCount          Int      @default(0)
  openIncidentCount      Int      @default(0)
  databaseStatus         String   @default("unknown")
  redisStatus            String?
  aiProviderFailureCount Int      @default(0)
  backupStatus           String   @default("unknown")
  lastBackupAt           DateTime?
  restoreDrillStatus     String   @default("unknown")
  lastRestoreDrillAt     DateTime?
  metadataSafeJson       Json?
  createdAt              DateTime @default(now())

  @@index([schoolId])
  @@index([createdAt])
}

model OpsBackupCheck {
  id                            String   @id @default(uuid())
  backupConfigured              Boolean  @default(false)
  backupProvider                String   @default("unknown")
  backupMode                    String   @default("local_drill")
  lastBackupAt                  DateTime?
  lastBackupStatus              String   @default("unknown")
  backupAgeHours                Int?
  expectedBackupFrequencyHours  Int?
  safeSummary                   String
  blockingIssues                Json     @default("[]")
  metadataSafeJson              Json?
  createdAt                     DateTime @default(now())

  @@index([createdAt])
}

model OpsRestoreDrill {
  id                           String   @id @default(uuid())
  drillId                      String
  status                       String   @default("pending")
  startedAt                    DateTime @default(now())
  completedAt                  DateTime?
  backupArtifactRefSafe        String
  checksum                     String?
  restoreTargetSafe            String
  integrityChecks              Json     @default("[]")
  blockingIssues               Json     @default("[]")
  safeSummary                  String
  destructiveCommandExecuted   Boolean  @default(false)
  realProductionDataOverwritten Boolean @default(false)
  metadataSafeJson             Json?
  createdAt                    DateTime @default(now())

  @@index([drillId])
  @@index([status])
  @@index([createdAt])
}

model OpsReport {
  id                   String   @id @default(uuid())
  taskId               String
  taskName             String
  reportKind           String   @default("task-024")
  status               String   @default("draft")
  safeSummary          String
  safeToStartNextTask  Boolean  @default(false)
  blockingIssues       Json     @default("[]")
  knownLimitations     Json     @default("[]")
  verificationSummary  Json     @default("{}")
  artifactPaths        Json     @default("[]")
  metadataSafeJson     Json?
  generatedAt          DateTime @default(now())
  createdAt            DateTime @default(now())

  @@index([taskId])
  @@index([status])
  @@index([generatedAt])
}

// ── Task 025: Controlled School Pilot Readiness v1 ──

model PilotProgram {
  id                String   @id @default(uuid())
  schoolId          String
  name              String
  status            String   @default("draft")
  pilotMode         String   @default("controlled")
  scopeSummarySafe  String
  allowedSubjects   Json     @default("[]")
  allowedYearGroups Json     @default("[]")
  allowedCurriculumTracks Json @default("[]")
  allowedRoles      Json     @default("[]")
  maxStudents       Int      @default(50)
  maxTeachers       Int      @default(10)
  startAt           DateTime?
  endAt             DateTime?
  createdByRole     String   @default("admin")
  createdByActorIdHash String?
  approvalStatus    String   @default("pending")
  approvedByRole    String?
  approvedAt        DateTime?
  rollbackEnabled   Boolean  @default(true)
  killSwitchEnabled Boolean  @default(true)
  metadataSafeJson  Json     @default("{}")
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  cohorts           PilotCohort[]
  participants      PilotParticipant[]
  readinessChecks   PilotReadinessCheck[]
  dryRuns           PilotDryRun[]
  auditRecords      PilotAuditRecord[]
  pilotExecutionRuns PilotExecutionRun[]
  expansionProposals PilotExpansionProposal[]

  @@index([schoolId])
  @@index([schoolId, status])
  @@index([status])
}

model PilotCohort {
  id                    String   @id @default(uuid())
  pilotProgramId        String
  schoolId              String
  name                  String
  status                String   @default("active")
  studentCount          Int      @default(0)
  teacherCount          Int      @default(0)
  allowedClassIds       Json     @default("[]")
  allowedSubjectIds     Json     @default("[]")
  allowedCurriculumScopes Json   @default("[]")
  metadataSafeJson      Json     @default("{}")
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  pilotProgram          PilotProgram @relation(fields: [pilotProgramId], references: [id], onDelete: Cascade)

  @@index([pilotProgramId])
  @@index([schoolId])
  @@index([status])
}

model PilotParticipant {
  id                String   @id @default(uuid())
  pilotProgramId    String
  cohortId          String?
  schoolId          String
  actorIdHash       String
  role              String   @default("student")
  eligibilityStatus String   @default("pending_review")
  reasonCodes       Json     @default("[]")
  joinedAt          DateTime?
  removedAt         DateTime?
  metadataSafeJson  Json     @default("{}")
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  pilotProgram      PilotProgram @relation(fields: [pilotProgramId], references: [id], onDelete: Cascade)

  @@unique([pilotProgramId, actorIdHash])
  @@index([pilotProgramId])
  @@index([cohortId])
  @@index([schoolId])
  @@index([eligibilityStatus])
}

model PilotReadinessCheck {
  id              String   @id @default(uuid())
  pilotProgramId  String
  schoolId        String
  checkType       String
  status          String   @default("not_run")
  safeSummary     String
  blockingIssues  Json     @default("[]")
  warnings        Json     @default("[]")
  evidenceRefs    Json     @default("[]")
  createdAt       DateTime @default(now())

  pilotProgram    PilotProgram @relation(fields: [pilotProgramId], references: [id], onDelete: Cascade)

  @@index([pilotProgramId])
  @@index([schoolId])
  @@index([checkType])
  @@index([status])
}

model PilotDryRun {
  id              String   @id @default(uuid())
  pilotProgramId  String
  schoolId        String
  status          String   @default("pending")
  scenarioName    String
  startedAt       DateTime @default(now())
  completedAt     DateTime?
  checksPassed    Json     @default("[]")
  checksFailed    Json     @default("[]")
  safeSummary     String
  metadataSafeJson Json    @default("{}")
  createdAt       DateTime @default(now())

  pilotProgram    PilotProgram @relation(fields: [pilotProgramId], references: [id], onDelete: Cascade)

  @@index([pilotProgramId])
  @@index([schoolId])
  @@index([status])
}

model PilotAuditRecord {
  id              String   @id @default(uuid())
  pilotProgramId  String?
  schoolId        String?
  actorRole       String   @default("unknown")
  actorIdHash     String?
  action          String
  safeSummary     String
  metadataSafeJson Json    @default("{}")
  requestId       String?
  correlationId   String?
  createdAt       DateTime @default(now())

  pilotProgram    PilotProgram? @relation(fields: [pilotProgramId], references: [id], onDelete: SetNull)

  @@index([pilotProgramId])
  @@index([schoolId])
  @@index([action])
  @@index([createdAt])
}

// ── Task 027: Controlled Pilot Expansion Governance v1 ──

model PilotExpansionProposal {
  id                       String   @id @default(uuid())
  pilotProgramId           String
  executionRunId           String?
  postPilotReviewId        String?
  schoolId                 String
  status                   String   @default("draft")
  proposalName             String
  safeSummary              String
  requestedStudentIncrease Int      @default(0)
  requestedTeacherIncrease Int      @default(0)
  requestedClassIds        Json     @default("[]")
  requestedSubjectIds      Json     @default("[]")
  requestedCurriculumScopes Json    @default("[]")
  requestedYearGroups      Json     @default("[]")
  requestedStartAt         DateTime?
  requestedEndAt           DateTime?
  createdByRole            String
  createdByActorIdHash     String?
  blockingIssues           Json     @default("[]")
  warnings                 Json     @default("[]")
  metadataSafeJson         Json     @default("{}")
  createdAt                DateTime @default(now())
  updatedAt                DateTime @updatedAt

  pilotProgram    PilotProgram @relation(fields: [pilotProgramId], references: [id], onDelete: Cascade)

  reviews         PilotExpansionReview[]
  evidencePacks   PilotExpansionEvidencePack[]
  riskAssessments PilotExpansionRiskAssessment[]
  approvals       PilotExpansionApproval[]
  cohortChanges   PilotExpansionCohortChange[]
  auditRecords    PilotExpansionAuditRecord[]

  @@index([schoolId])
  @@index([pilotProgramId])
  @@index([status])
}

model PilotExpansionReview {
  id                   String   @id @default(uuid())
  expansionProposalId  String
  schoolId             String
  reviewType           String
  reviewStatus         String   @default("draft")
  reviewerRole         String
  reviewerActorIdHash  String?
  safeSummary          String
  blockingIssues       Json     @default("[]")
  warnings             Json     @default("[]")
  evidenceRefs         Json     @default("[]")
  metadataSafeJson     Json     @default("{}")
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt

  expansionProposal    PilotExpansionProposal @relation(fields: [expansionProposalId], references: [id], onDelete: Cascade)

  @@index([expansionProposalId])
  @@index([reviewType])
}

model PilotExpansionEvidencePack {
  id                   String   @id @default(uuid())
  expansionProposalId  String
  schoolId             String
  status               String   @default("draft")
  safeSummary          String
  learningQualityEvidence Json  @default("{}")
  socraticEvidence     Json     @default("{}")
  deenEvidence         Json     @default("{}")
  privacyEvidence      Json     @default("{}")
  curriculumEvidence   Json     @default("{}")
  operationsEvidence   Json     @default("{}")
  feedbackEvidence     Json     @default("{}")
  incidentEvidence     Json     @default("{}")
  rollbackEvidence     Json     @default("{}")
  artifactPaths        Json     @default("[]")
  blockingIssues       Json     @default("[]")
  warnings             Json     @default("[]")
  metadataSafeJson     Json     @default("{}")
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt

  expansionProposal    PilotExpansionProposal @relation(fields: [expansionProposalId], references: [id], onDelete: Cascade)

  @@index([expansionProposalId])
}

model PilotExpansionRiskAssessment {
  id                    String   @id @default(uuid())
  expansionProposalId   String
  schoolId              String
  overallRiskLevel      String   @default("medium")
  learningRiskLevel     String   @default("medium")
  privacyRiskLevel      String   @default("medium")
  deenRiskLevel         String   @default("medium")
  socraticRiskLevel     String   @default("medium")
  curriculumRiskLevel   String   @default("medium")
  operationsRiskLevel   String   @default("medium")
  safeguardingRiskLevel String   @default("medium")
  riskReasons           Json     @default("[]")
  mitigations           Json     @default("[]")
  blockingIssues        Json     @default("[]")
  safeSummary           String
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  expansionProposal     PilotExpansionProposal @relation(fields: [expansionProposalId], references: [id], onDelete: Cascade)

  @@index([expansionProposalId])
}

model PilotExpansionApproval {
  id                    String   @id @default(uuid())
  expansionProposalId   String
  schoolId              String
  decision              String
  approvalStatus        String   @default("pending")
  approvedByRole        String?
  approvedByActorIdHash String?
  approvedAt            DateTime?
  rejectedAt            DateTime?
  safeDecisionSummary   String
  conditions            Json     @default("[]")
  blockingIssues        Json     @default("[]")
  metadataSafeJson      Json     @default("{}")
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  expansionProposal     PilotExpansionProposal @relation(fields: [expansionProposalId], references: [id], onDelete: Cascade)

  @@index([expansionProposalId])
}

model PilotExpansionCohortChange {
  id                     String   @id @default(uuid())
  expansionProposalId    String
  pilotProgramId         String
  schoolId               String
  changeStatus           String   @default("pending")
  previousCohortSnapshot Json     @default("{}")
  newCohortSnapshot      Json     @default("{}")
  addedStudentCount      Int      @default(0)
  addedTeacherCount      Int      @default(0)
  addedClassIds          Json     @default("[]")
  addedSubjectIds        Json     @default("[]")
  safeSummary            String
  rollbackPlan           Json     @default("{}")
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt

  expansionProposal      PilotExpansionProposal @relation(fields: [expansionProposalId], references: [id], onDelete: Cascade)

  @@index([expansionProposalId])
  @@index([pilotProgramId])
}

model PilotExpansionReport {
  id                String   @id @default(uuid())
  expansionProposalId String?
  schoolId          String?
  taskId            String
  taskName          String
  status            String   @default("draft")
  safeToStartNextTask Boolean @default(false)
  safeSummary       String
  decisionSummary   Json     @default("{}")
  evidenceSummary   Json     @default("{}")
  riskSummary       Json     @default("{}")
  reviewSummary     Json     @default("{}")
  blockingIssues    Json     @default("[]")
  knownLimitations  Json     @default("[]")
  verificationSummary Json  @default("{}")
  artifactPaths     Json     @default("[]")
  generatedAt       DateTime @default(now())
  createdAt         DateTime @default(now())

  @@index([taskId])
}

model PilotExpansionAuditRecord {
  id                  String   @id @default(uuid())
  expansionProposalId String?
  pilotProgramId      String?
  schoolId            String?
  actorRole           String   @default("unknown")
  actorIdHash         String?
  action              String
  safeSummary         String
  metadataSafeJson    Json     @default("{}")
  requestId           String?
  correlationId       String?
  createdAt           DateTime @default(now())

  expansionProposal   PilotExpansionProposal? @relation(fields: [expansionProposalId], references: [id], onDelete: SetNull)

  @@index([expansionProposalId])
  @@index([pilotProgramId])
  @@index([action])
  @@index([createdAt])
}

// ── Task 026: Controlled Pilot Execution Runtime v1 ──

model PilotExecutionRun {
  id                String   @id @default(uuid())
  pilotProgramId    String
  schoolId          String
  status            String   @default("not_started")
  startedAt         DateTime?
  pausedAt          DateTime?
  resumedAt         DateTime?
  rolledBackAt      DateTime?
  completedAt       DateTime?
  startedByRole     String?
  startedByActorIdHash String?
  safeSummary       String
  activeStudentCount Int     @default(0)
  activeTeacherCount Int     @default(0)
  allowedCohortIds  Json     @default("[]")
  executionGateSnapshot Json @default("{}")
  blockingIssues    Json     @default("[]")
  warnings          Json     @default("[]")
  metadataSafeJson  Json     @default("{}")
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  pilotProgram      PilotProgram @relation(fields: [pilotProgramId], references: [id], onDelete: Cascade)

  events            PilotExecutionEvent[]
  metricSnapshots   PilotRuntimeMetricSnapshot[]
  feedbackRecords   PilotFeedbackRecord[]
  safetySignals     PilotSafetySignal[]
  postPilotReviews  PilotPostPilotReview[]
  auditRecords      PilotExecutionAuditRecord[]

  @@index([pilotProgramId])
  @@index([schoolId])
  @@index([status])
}

model PilotExecutionEvent {
  id              String   @id @default(uuid())
  executionRunId  String
  pilotProgramId  String
  schoolId        String
  actorRole       String
  actorIdHash     String?
  eventType       String
  eventStatus     String
  safeSummary     String
  reasonCodes     Json     @default("[]")
  metadataSafeJson Json    @default("{}")
  requestId       String?
  correlationId   String?
  createdAt       DateTime @default(now())

  executionRun    PilotExecutionRun @relation(fields: [executionRunId], references: [id], onDelete: Cascade)

  @@index([executionRunId])
  @@index([pilotProgramId])
  @@index([eventType])
}

model PilotRuntimeMetricSnapshot {
  id                      String   @id @default(uuid())
  executionRunId          String
  pilotProgramId          String
  schoolId                String
  activeSessions          Int       @default(0)
  allowedSessionStarts    Int       @default(0)
  blockedSessionStarts    Int       @default(0)
  pilotAccessDeniedCount  Int       @default(0)
  curriculumGateBlockCount Int      @default(0)
  schoolAuthGateBlockCount Int      @default(0)
  socraticGateBlockCount  Int       @default(0)
  deenGateBlockCount      Int       @default(0)
  privacyGateBlockCount   Int       @default(0)
  aiCallBlockedCount      Int       @default(0)
  aiCallAllowedCount      Int       @default(0)
  feedbackCount           Int       @default(0)
  safetySignalCount       Int       @default(0)
  incidentBridgeCount     Int       @default(0)
  p95LatencyMs            Int?
  errorCount              Int       @default(0)
  metadataSafeJson        Json      @default("{}")
  createdAt               DateTime  @default(now())

  executionRun            PilotExecutionRun @relation(fields: [executionRunId], references: [id], onDelete: Cascade)

  @@index([executionRunId])
  @@index([pilotProgramId])
  @@index([createdAt])
}

model PilotFeedbackRecord {
  id                    String   @id @default(uuid())
  executionRunId        String
  pilotProgramId        String
  schoolId              String
  actorRole             String
  actorIdHash           String?
  feedbackType          String
  sentiment             String?
  safeSummary           String
  redactionStatus       String   @default("not_redacted")
  riskFlags             Json     @default("[]")
  teacherActionRequested Boolean @default(false)
  safeguardingRelevant  Boolean  @default(false)
  deenRelevant          Boolean  @default(false)
  privacyRelevant       Boolean  @default(false)
  metadataSafeJson      Json     @default("{}")
  createdAt             DateTime @default(now())

  executionRun          PilotExecutionRun @relation(fields: [executionRunId], references: [id], onDelete: Cascade)

  @@index([executionRunId])
  @@index([pilotProgramId])
  @@index([feedbackType])
}

model PilotSafetySignal {
  id                    String   @id @default(uuid())
  executionRunId        String
  pilotProgramId        String
  schoolId              String
  signalType            String
  severity              String
  source                String
  status                String   @default("open")
  safeSummary           String
  reasonCodes           Json     @default("[]")
  linkedIncidentId      String?
  requiresPause         Boolean  @default(false)
  requiresRollback      Boolean  @default(false)
  requiresTeacherReview Boolean  @default(false)
  requiresSafeguardingReview Boolean @default(false)
  requiresDeenReview    Boolean  @default(false)
  metadataSafeJson      Json     @default("{}")
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  executionRun          PilotExecutionRun @relation(fields: [executionRunId], references: [id], onDelete: Cascade)

  @@index([executionRunId])
  @@index([pilotProgramId])
  @@index([severity])
  @@index([status])
}

model PilotPostPilotReview {
  id                    String   @id @default(uuid())
  executionRunId        String
  pilotProgramId        String
  schoolId              String
  status                String   @default("draft")
  safeSummary           String
  learningQualitySummary Json    @default("{}")
  safetySummary         Json     @default("{}")
  privacySummary        Json     @default("{}")
  deenSummary           Json     @default("{}")
  operationsSummary     Json     @default("{}")
  feedbackSummary       Json     @default("{}")
  recommendedDecision   String   @default("pause_and_fix")
  safeToStartNextTask   Boolean  @default(false)
  blockingIssues        Json     @default("[]")
  knownLimitations      Json     @default("[]")
  artifactPaths         Json     @default("[]")
  metadataSafeJson      Json     @default("{}")
  generatedAt           DateTime @default(now())
  createdAt             DateTime @default(now())

  executionRun          PilotExecutionRun @relation(fields: [executionRunId], references: [id], onDelete: Cascade)

  @@index([executionRunId])
  @@index([pilotProgramId])
}

model PilotExecutionAuditRecord {
  id              String   @id @default(uuid())
  executionRunId  String?
  pilotProgramId  String?
  schoolId        String?
  actorRole       String
  actorIdHash     String?
  action          String
  safeSummary     String
  metadataSafeJson Json    @default("{}")
  requestId       String?
  correlationId   String?
  createdAt       DateTime @default(now())

  executionRun    PilotExecutionRun? @relation(fields: [executionRunId], references: [id], onDelete: SetNull)

  @@index([executionRunId])
  @@index([pilotProgramId])
  @@index([action])
  @@index([createdAt])
}

// ── Task 028: Controlled Expansion Execution v1 ──

model ExpansionExecutionRun {
  id                String   @id @default(uuid())
  expansionProposalId String
  pilotProgramId    String
  schoolId          String
  status            String   @default("not_started")
  approvedDecisionRef String?
  task027ReportRef  String?
  safeSummary       String
  startedAt         DateTime?
  pausedAt          DateTime?
  rolledBackAt      DateTime?
  completedAt       DateTime?
  startedByRole     String?
  startedByActorIdHash String?
  currentStage      Int      @default(0)
  stagePlan         Json     @default("{}")
  approvedScopeSnapshot Json @default("{}")
  blockingIssues    Json     @default("[]")
  warnings          Json     @default("[]")
  metadataSafeJson  Json     @default("{}")
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  stages            ExpansionExecutionStage[]
  participants      ExpandedPilotParticipant[]
  runtimeEvents     ExpansionRuntimeEvent[]
  healthSnapshots   ExpansionHealthSnapshot[]
  oversightItems    ExpansionOversightItem[]
  interventions     ExpansionInterventionRecord[]
  rollbackRecords   ExpansionRollbackRecord[]

  @@index([expansionProposalId])
  @@index([pilotProgramId])
  @@index([schoolId])
  @@index([status])
}

model ExpansionExecutionStage {
  id                      String   @id @default(uuid())
  executionRunId          String
  expansionProposalId     String
  schoolId                String
  stageNumber             Int
  status                  String   @default("pending")
  plannedStudentCount     Int      @default(0)
  plannedTeacherCount     Int      @default(0)
  activatedStudentCount   Int      @default(0)
  activatedTeacherCount   Int      @default(0)
  allowedClassIds         Json     @default("[]")
  allowedSubjectIds       Json     @default("[]")
  allowedCurriculumScopes Json     @default("[]")
  startedAt               DateTime?
  completedAt             DateTime?
  pausedAt                DateTime?
  safeSummary             String
  blockingIssues          Json     @default("[]")
  warnings                Json     @default("[]")
  metadataSafeJson        Json     @default("{}")
  createdAt               DateTime @default(now())
  updatedAt               DateTime @updatedAt

  executionRun            ExpansionExecutionRun @relation(fields: [executionRunId], references: [id], onDelete: Cascade)

  @@index([executionRunId])
  @@index([status])
}

model ExpandedPilotParticipant {
  id                  String   @id @default(uuid())
  executionRunId      String
  stageId             String?
  pilotProgramId      String
  schoolId            String
  actorIdHash         String
  role                String   @default("student")
  classId             String?
  subjectIds          Json     @default("[]")
  curriculumScopes    Json     @default("[]")
  activationStatus    String   @default("pending")
  activationReasonCodes Json   @default("[]")
  joinedAt            DateTime?
  removedAt           DateTime?
  metadataSafeJson    Json     @default("{}")
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  executionRun        ExpansionExecutionRun @relation(fields: [executionRunId], references: [id], onDelete: Cascade)

  @@index([executionRunId])
  @@index([actorIdHash])
  @@index([activationStatus])
}

model ExpansionRuntimeEvent {
  id              String   @id @default(uuid())
  executionRunId  String
  stageId         String?
  pilotProgramId  String
  schoolId        String
  actorRole       String
  actorIdHash     String?
  eventType       String
  eventStatus     String
  safeSummary     String
  reasonCodes     Json     @default("[]")
  metadataSafeJson Json    @default("{}")
  requestId       String?
  correlationId   String?
  createdAt       DateTime @default(now())

  executionRun    ExpansionExecutionRun @relation(fields: [executionRunId], references: [id], onDelete: Cascade)

  @@index([executionRunId])
  @@index([eventType])
}

model ExpansionHealthSnapshot {
  id                       String   @id @default(uuid())
  executionRunId           String
  stageId                  String?
  pilotProgramId           String
  schoolId                 String
  activeExpandedSessions   Int      @default(0)
  allowedExpandedSessionStarts Int  @default(0)
  blockedExpandedSessionStarts Int  @default(0)
  schoolAuthBlocks         Int      @default(0)
  cohortScopeBlocks        Int      @default(0)
  curriculumGateBlocks     Int      @default(0)
  socraticGateBlocks       Int      @default(0)
  deenGateBlocks           Int      @default(0)
  privacyGateBlocks        Int      @default(0)
  aiCallBlocks             Int      @default(0)
  memoryAccessBlocks       Int      @default(0)
  evidenceWriteBlocks      Int      @default(0)
  feedbackCount            Int      @default(0)
  oversightItemCount       Int      @default(0)
  interventionCount        Int      @default(0)
  incidentBridgeCount      Int      @default(0)
  errorCount               Int      @default(0)
  p95LatencyMs             Int?
  safeSummary              String
  metadataSafeJson         Json     @default("{}")
  createdAt                DateTime @default(now())

  executionRun             ExpansionExecutionRun @relation(fields: [executionRunId], references: [id], onDelete: Cascade)

  @@index([executionRunId])
}

model ExpansionOversightItem {
  id                    String   @id @default(uuid())
  executionRunId        String
  stageId               String?
  pilotProgramId        String
  schoolId              String
  itemType              String
  severity              String
  status                String   @default("open")
  source                String
  safeSummary           String
  reasonCodes           Json     @default("[]")
  assignedRole          String?
  requiresTeacherReview Boolean  @default(false)
  requiresAdminReview   Boolean  @default(false)
  requiresPrivacyReview Boolean  @default(false)
  requiresDeenReview    Boolean  @default(false)
  requiresSocraticReview Boolean @default(false)
  requiresCurriculumReview Boolean @default(false)
  requiresPause         Boolean  @default(false)
  requiresRollback      Boolean  @default(false)
  metadataSafeJson      Json     @default("{}")
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  executionRun          ExpansionExecutionRun @relation(fields: [executionRunId], references: [id], onDelete: Cascade)

  @@index([executionRunId])
  @@index([severity])
  @@index([status])
}

model ExpansionInterventionRecord {
  id              String   @id @default(uuid())
  executionRunId  String
  stageId         String?
  pilotProgramId  String
  schoolId        String
  interventionType String
  status          String   @default("requested")
  actorRole       String
  actorIdHash     String?
  safeSummary     String
  reasonCodes     Json     @default("[]")
  beforeSnapshot  Json     @default("{}")
  afterSnapshot   Json     @default("{}")
  metadataSafeJson Json    @default("{}")
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  executionRun    ExpansionExecutionRun @relation(fields: [executionRunId], references: [id], onDelete: Cascade)

  @@index([executionRunId])
}

model ExpansionRollbackRecord {
  id                    String   @id @default(uuid())
  executionRunId        String
  stageId               String?
  pilotProgramId        String
  schoolId              String
  rollbackStatus        String   @default("pending")
  rollbackReason        String
  safeSummary           String
  previousScopeSnapshot Json     @default("{}")
  restoredScopeSnapshot Json     @default("{}")
  affectedParticipantCount Int   @default(0)
  dataDeleted           Boolean  @default(false)
  auditPreserved        Boolean  @default(true)
  metadataSafeJson      Json     @default("{}")
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  executionRun          ExpansionExecutionRun @relation(fields: [executionRunId], references: [id], onDelete: Cascade)

  @@index([executionRunId])
}

model ExpansionCompletionReview {
  id                    String   @id @default(uuid())
  executionRunId        String
  pilotProgramId        String
  schoolId              String
  status                String   @default("draft")
  safeSummary           String
  learningQualitySummary Json    @default("{}")
  safetySummary         Json     @default("{}")
  privacySummary        Json     @default("{}")
  deenSummary           Json     @default("{}")
  socraticSummary       Json     @default("{}")
  curriculumSummary     Json     @default("{}")
  operationsSummary     Json     @default("{}")
  teacherAdminSummary   Json     @default("{}")
  rollbackSummary       Json     @default("{}")
  recommendedDecision   String   @default("continue_controlled_expansion")
  safeToStartNextTask   Boolean  @default(false)
  blockingIssues        Json     @default("[]")
  knownLimitations      Json     @default("[]")
  artifactPaths         Json     @default("[]")
  generatedAt           DateTime @default(now())
  createdAt             DateTime @default(now())

  @@index([executionRunId])
}

model ExpansionExecutionReport {
  id                   String   @id @default(uuid())
  executionRunId       String?
  schoolId             String?
  taskId               String
  taskName             String
  status               String   @default("draft")
  safeToStartNextTask  Boolean  @default(false)
  safeSummary          String
  executionSummary     Json     @default("{}")
  stageSummary         Json     @default("{}")
  runtimeGateSummary   Json     @default("{}")
  monitoringSummary    Json     @default("{}")
  oversightSummary     Json     @default("{}")
  rollbackSummary      Json     @default("{}")
  completionReviewSummary Json @default("{}")
  blockingIssues       Json     @default("[]")
  knownLimitations     Json     @default("[]")
  verificationSummary  Json     @default("{}")
  artifactPaths        Json     @default("[]")
  generatedAt          DateTime @default(now())
  createdAt            DateTime @default(now())

  @@index([taskId])
}

model ExpansionExecutionAuditRecord {
  id              String   @id @default(uuid())
  executionRunId  String?
  stageId         String?
  pilotProgramId  String?
  schoolId        String?
  actorRole       String   @default("unknown")
  actorIdHash     String?
  action          String
  safeSummary     String
  metadataSafeJson Json    @default("{}")
  requestId       String?
  correlationId   String?
  createdAt       DateTime @default(now())

  @@index([executionRunId])
  @@index([createdAt])
}

// ── Phase 2: Learning Mode Runtime & Safe Signal Backbone ──

model LearningModeSession {
  id              String   @id @default(cuid())
  schoolId        String
  studentId       String
  conversationId  String?

  mode            String
  status          String   @default("requested")
  stage           String   @default("entry")

  subjectId       String?
  topicId         String?
  skillId         String?

  requestedAt     DateTime @default(now())
  startedAt       DateTime?
  endedAt         DateTime?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  signals         LearningModeSignal[]
  attempts        LearningModeAttempt[]
  hintEvents      LearningModeHintEvent[]
  exitSummary     LearningModeExitSummary?

  @@index([schoolId, studentId])
  @@index([schoolId, mode])
  @@index([schoolId, studentId, status])
  @@index([createdAt])
}

model LearningModeSignal {
  id              String   @id @default(cuid())
  modeSessionId   String
  modeSession     LearningModeSession @relation(fields: [modeSessionId], references: [id], onDelete: Cascade)
  schoolId        String
  studentId       String

  signalType      String
  stage           String?

  topicId         String?
  subjectId       String?
  skillId         String?

  attemptNumber   Int?
  hintLevel       String?
  answerQuality   String?
  mistakeCategory String?
  supportActionType String?
  confidenceSignal String?
  timeSpentBucket String?
  difficultyBucket String?
  sourceType      String?

  createdAt       DateTime @default(now())

  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([signalType])
  @@index([createdAt])
}

model LearningModeAttempt {
  id              String   @id @default(cuid())
  modeSessionId   String
  modeSession     LearningModeSession @relation(fields: [modeSessionId], references: [id], onDelete: Cascade)
  schoolId        String
  studentId       String

  stage           String
  attemptNumber   Int
  answerQuality   String?
  isCorrect       Boolean?
  mistakeCategory String?
  usedHint        Boolean  @default(false)
  hintLevel       String?
  timeSpentBucket String?
  topicId         String?
  skillId         String?

  createdAt       DateTime @default(now())

  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([schoolId, studentId, createdAt])
}

model LearningModeHintEvent {
  id                    String   @id @default(cuid())
  modeSessionId         String
  modeSession           LearningModeSession @relation(fields: [modeSessionId], references: [id], onDelete: Cascade)
  schoolId              String
  studentId             String

  hintLevel             String
  stage                 String?
  attemptNumber         Int?
  wasRequestedByStudent Boolean  @default(false)
  wasSuggestedBySystem  Boolean  @default(false)
  effectivenessUnknown  Boolean  @default(true)

  createdAt             DateTime @default(now())

  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([hintLevel])
}

model LearningModeExitSummary {
  id                    String   @id @default(cuid())
  modeSessionId         String   @unique
  modeSession           LearningModeSession @relation(fields: [modeSessionId], references: [id], onDelete: Cascade)
  schoolId              String
  studentId             String

  mode                  String
  topicId               String?
  subjectId             String?
  skillsTouched         Json     @default("[]")
  strengthSignals       Json     @default("[]")
  weaknessSignals       Json     @default("[]")
  hintsUsedCount        Int      @default(0)
  attemptsCount         Int      @default(0)
  stuckCount            Int      @default(0)
  recoveryCount         Int      @default(0)
  finalUnderstandingLevel String?
  recommendedNextAction String?

  createdAt             DateTime @default(now())

  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([createdAt])
}

// ── Phase 2 Task 011: Safe Learning Evidence Ledger ──

model SafeLearningEvidenceRecord {
  id                         String   @id @default(cuid())
  idempotencyKey             String   @unique
  schoolId                   String
  studentId                  String
  tutorLearnerId             String?
  conversationId             String?
  tutorSessionId             String?
  turnId                     String?
  modeSessionId              String?
  sourceTask                 String
  sourceMode                 String
  evidenceType               String
  evidenceStrength           String
  sourceTruthStatus          String
  dataQualityStatus          String
  approvedContentRef         String?
  contentFingerprint         String?
  subjectId                  String?
  topicId                    String?
  skillId                    String?
  objectiveId                String?
  targetType                 String?
  targetRef                  String?
  attemptNumber              Int?
  timeSpentBucket            String?
  difficultyBucket           String?
  supportNeed                String?
  hintLevel                  String?
  hintDependencyBucket       String?
  mistakeCategory            String?
  misconceptionCategory      String?
  recallQuality              String?
  explanationQualityBucket   String?
  reflectionQualityBucket    String?
  readinessBucket            String?
  masterySignal              String?
  weakTopicSignal            String?
  revisionSignal             String?
  growthProofSignal          String?
  policyDecision             String
  safeReasonCodesJson        Json     @default("[]")
  safeEvidenceRefsJson       Json     @default("[]")
  safeMetadataJson           Json     @default("{}")
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt

  @@index([idempotencyKey])
  @@index([schoolId, studentId])
  @@index([schoolId, tutorLearnerId])
  @@index([schoolId, sourceTask])
  @@index([schoolId, sourceMode])
  @@index([schoolId, evidenceType])
  @@index([schoolId, subjectId, topicId, skillId])
  @@index([schoolId, studentId, createdAt])
  @@index([createdAt])
}

model SafeLearningEvidenceAggregateRecord {
  id                   String   @id @default(cuid())
  schoolId             String
  studentId            String
  tutorLearnerId       String?
  aggregateWindow      String
  subjectId            String?
  topicId              String?
  skillId              String?
  evidenceCount        Int      @default(0)
  realEvidenceCount    Int      @default(0)
  weakSignalCount      Int      @default(0)
  masterySignalCount   Int      @default(0)
  revisionSignalCount  Int      @default(0)
  mistakeSignalCount   Int      @default(0)
  hintDependencyCount  Int      @default(0)
  reflectionCount      Int      @default(0)
  teachBackCount       Int      @default(0)
  quizRecallCount      Int      @default(0)
  lastEvidenceAt       DateTime?
  confidenceBucket     String   @default("none")
  safeSummaryJson      Json     @default("{}")
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt

  @@unique([schoolId, studentId, aggregateWindow])
  @@index([schoolId, studentId])
  @@index([schoolId, studentId, aggregateWindow])
  @@index([createdAt])
}

model GrowthProofRecord {
  id                   String   @id @default(cuid())
  schoolId             String
  studentId            String
  tutorLearnerId       String?
  subjectId            String?
  topicId              String?
  skillId              String?
  proofStatus          String
  proofStrength        String
  sourceEvidenceIdsJson Json   @default("[]")
  safeEvidenceRefsJson  Json   @default("[]")
  safeReasonCodesJson   Json   @default("[]")
  sourceTruthStatus    String
  confidenceBucket     String   @default("none")
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt

  @@index([schoolId, studentId])
  @@index([schoolId, studentId, subjectId, skillId])
  @@index([createdAt])
}

model SafeLearningEvidenceAuditRecord {
  id                 String   @id @default(cuid())
  schoolId           String
  studentId          String?
  eventType          String
  policyDecision     String
  safeReasonCodesJson Json   @default("[]")
  safeMetadataJson   Json     @default("{}")
  createdAt          DateTime @default(now())

  @@index([schoolId])
  @@index([schoolId, studentId])
  @@index([eventType])
  @@index([createdAt])
}

// ── Phase 2 Task 002: Student Learning Profile + Mastery Foundation ──

model StudentLearningProfileSnapshot {
  id                       String   @id @default(cuid())
  schoolId                 String
  studentId                String
  tutorLearnerId           String?

  profileVersion           Int      @default(1)
  status                   String   @default("active")
  profileJson              Json     @default("{}")
  subjectSummariesJson     Json     @default("[]")
  topicSummariesJson       Json     @default("[]")
  supportPatternsJson      Json     @default("[]")
  recommendedActionsJson   Json     @default("[]")
  safeEvidenceRefsJson     Json     @default("[]")
  privacyLevel             String   @default("student_only")
  confidenceScore          Float    @default(0)
  generatedFromEventCount  Int      @default(0)
  lastEvidenceAt           DateTime?
  createdAt                DateTime @default(now())
  updatedAt                DateTime @updatedAt

  @@unique([schoolId, studentId])
  @@index([schoolId, studentId])
  @@index([schoolId, tutorLearnerId])
  @@index([status])
  @@index([updatedAt])
}

model StudentSupportPatternSnapshot {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String

  supportType           String
  effectivenessSignal   String   @default("unknown")
  timesUsed             Int      @default(0)
  recoveryAfterUseCount Int      @default(0)
  hintLevelMostHelpful  String?
  confidenceScore       Float    @default(0)
  safeEvidenceRefsJson  Json     @default("[]")
  lastEffectiveAt       DateTime?
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  @@unique([schoolId, studentId, supportType])
  @@index([schoolId, studentId])
  @@index([schoolId, studentId, supportType])
}

model StudentMasteryAggregationRun {
  id                 String   @id @default(cuid())
  schoolId           String
  studentId          String

  status             String   @default("pending")
  sourceEventCount   Int      @default(0)
  updatedSkillCount  Int      @default(0)
  updatedTopicCount  Int      @default(0)
  weakTopicCount     Int      @default(0)
  mistakePatternCount Int     @default(0)
  warningsJson       Json     @default("[]")
  startedAt          DateTime @default(now())
  completedAt        DateTime?
  createdAt          DateTime @default(now())

  @@index([schoolId, studentId])
  @@index([schoolId, studentId, status])
  @@index([createdAt])
}

// ── Phase 2 Task 003: Tutor Action Ranking + Hint Ladder Engine ──

model TutorActionDecisionRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String
  tutorLearnerId        String?

  modeSessionId         String?
  conversationId        String?
  mode                  String?
  stage                 String?
  subjectId             String?
  topicId               String?
  skillId               String?

  selectedAction        String
  hintLevel             String?
  supportLevel          String
  learnerNeedCategory   String

  rankedActionsJson     Json     @default("[]")
  answerPolicyJson      Json     @default("{}")
  contentPolicyJson     Json     @default("{}")
  decisionReasonCodesJson Json   @default("[]")
  safeEvidenceRefsJson  Json     @default("[]")
  confidenceScore       Float    @default(0)

  createdAt             DateTime @default(now())

  @@index([schoolId, studentId])
  @@index([schoolId, tutorLearnerId])
  @@index([modeSessionId])
  @@index([selectedAction])
  @@index([createdAt])
}

model TutorHintLadderStateRecord {
  id                String   @id @default(cuid())
  schoolId          String
  studentId         String
  tutorLearnerId    String?

  modeSessionId     String?
  topicId           String?
  skillId           String?

  currentHintLevel  String   @default("attention_hint")
  hintCount         Int      @default(0)
  lastHintAt        DateTime?
  stuckCount        Int      @default(0)
  recoveryCount     Int      @default(0)
  status            String   @default("active")

  safeEvidenceRefsJson Json  @default("[]")
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([schoolId, studentId])
  @@index([schoolId, studentId, topicId, skillId])
  @@index([modeSessionId])
  @@index([status])
}

model TutorActionEffectivenessRecord {
  id                  String   @id @default(cuid())
  schoolId            String
  studentId           String
  tutorLearnerId      String?

  decisionId          String
  selectedAction      String
  hintLevel           String?

  effectivenessSignal String
  recoveryDetected    Boolean  @default(false)
  studentProgressSignal String?

  safeEvidenceRefsJson Json    @default("[]")
  createdAt           DateTime @default(now())

  @@index([schoolId, studentId])
  @@index([decisionId])
  @@index([selectedAction])
  @@index([createdAt])
}

// ── Phase 2 Task 004: Focus Mode Runtime + One-Problem Learning Chamber ──

model FocusModeSessionRecord {
  id                String   @id @default(cuid())
  schoolId          String
  studentId         String
  tutorLearnerId    String?
  modeSessionId     String
  conversationId    String?
  subjectId         String?
  topicId           String?
  skillId           String?
  approvedContentRef String?
  problemRef        String?
  problemFingerprint String?
  targetType        String
  focusGoalCategory String
  status            String   @default("active")
  currentStage      String   @default("awaiting_problem")
  currentStepKey    String?
  attemptCount      Int      @default(0)
  hintCount         Int      @default(0)
  stuckCount        Int      @default(0)
  recoveryCount     Int      @default(0)
  safeEvidenceRefsJson Json   @default("[]")
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  startedAt         DateTime @default(now())
  endedAt           DateTime?

  steps             FocusModeStepRecord[]
  attempts          FocusModeAttemptRecord[]
  summaries         FocusModeSummaryRecord[]

  @@index([schoolId, studentId])
  @@index([schoolId, tutorLearnerId])
  @@index([modeSessionId])
  @@index([conversationId])
  @@index([status])
  @@index([createdAt])
}

model FocusModeStepRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String
  focusSessionId        String
  modeSessionId         String
  stepKey               String
  stepType              String
  stage                 String
  status                String   @default("active")
  selectedTutorAction   String?
  hintLevel             String?
  supportLevel          String?
  learnerNeedCategory   String?
  attemptNumber         Int?
  safeReasonCodesJson   Json     @default("[]")
  safeEvidenceRefsJson  Json     @default("[]")
  createdAt             DateTime @default(now())
  completedAt           DateTime?

  focusSession          FocusModeSessionRecord @relation(fields: [focusSessionId], references: [id], onDelete: Cascade)

  @@index([focusSessionId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([status])
  @@index([createdAt])
}

model FocusModeAttemptRecord {
  id                String   @id @default(cuid())
  schoolId          String
  studentId         String
  focusSessionId    String
  modeSessionId     String
  stepKey           String?
  stage             String
  attemptNumber     Int
  answerQuality     String?
  isCorrect         Boolean?
  mistakeCategory   String?
  usedHint          Boolean  @default(false)
  hintLevel         String?
  timeSpentBucket   String?
  safeEvidenceRefsJson Json  @default("[]")
  createdAt         DateTime @default(now())

  focusSession      FocusModeSessionRecord @relation(fields: [focusSessionId], references: [id], onDelete: Cascade)

  @@index([focusSessionId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([attemptNumber])
  @@index([createdAt])
}

model FocusModeSummaryRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String
  focusSessionId        String
  modeSessionId         String
  finalStage            String
  exitReason            String
  attemptCount          Int      @default(0)
  hintCount             Int      @default(0)
  stuckCount            Int      @default(0)
  recoveryCount         Int      @default(0)
  masterySignal         String?
  summarySignalJson     Json     @default("{}")
  safeEvidenceRefsJson  Json     @default("[]")
  createdAt             DateTime @default(now())

  focusSession          FocusModeSessionRecord @relation(fields: [focusSessionId], references: [id], onDelete: Cascade)

  @@index([focusSessionId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([createdAt])
}

// ── Phase 2 Task 005: Exam Mode Runtime + Honest Attempt Exam Chamber ──

model ExamModeSessionRecord {
  id                  String   @id @default(cuid())
  schoolId            String
  studentId           String
  tutorLearnerId      String?
  modeSessionId       String
  conversationId      String?
  subjectId           String?
  topicId             String?
  skillId             String?
  approvedContentRef  String?
  paperRef            String?
  examSetRef          String?
  targetType          String
  examGoalCategory    String
  examSessionType     String
  timerMode           String   @default("untimed")
  status              String   @default("active")
  currentStage        String   @default("awaiting_exam_target")
  currentQuestionIndex Int     @default(0)
  questionCount       Int      @default(0)
  attemptCount        Int      @default(0)
  hintCount           Int      @default(0)
  stuckCount          Int      @default(0)
  recoveryCount       Int      @default(0)
  skippedCount        Int      @default(0)
  flaggedCount        Int      @default(0)
  safeEvidenceRefsJson Json    @default("[]")
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt
  startedAt           DateTime @default(now())
  submittedAt         DateTime?
  endedAt             DateTime?

  questionStates      ExamModeQuestionStateRecord[]
  attempts            ExamModeAttemptRecord[]
  summaries           ExamModeSummaryRecord[]

  @@index([schoolId, studentId])
  @@index([schoolId, tutorLearnerId])
  @@index([modeSessionId])
  @@index([conversationId])
  @@index([status])
  @@index([createdAt])
}

model ExamModeQuestionStateRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String
  examSessionId         String
  modeSessionId         String
  questionKey           String
  questionIndex         Int
  questionRef           String?
  questionFingerprint   String?
  topicId               String?
  skillId               String?
  difficultyBucket      String?
  status                String   @default("not_started")
  attemptNumber         Int      @default(0)
  answerQuality         String?
  isCorrect             Boolean?
  mistakeCategory       String?
  confidenceBucket      String?
  timeSpentBucket       String?
  scoreBucket           String?
  selectedTutorAction   String?
  hintLevel             String?
  safeReasonCodesJson   Json     @default("[]")
  safeEvidenceRefsJson  Json     @default("[]")
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt
  completedAt           DateTime?

  examSession           ExamModeSessionRecord @relation(fields: [examSessionId], references: [id], onDelete: Cascade)

  @@index([examSessionId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([questionIndex])
  @@index([status])
  @@index([createdAt])
}

model ExamModeAttemptRecord {
  id                String   @id @default(cuid())
  schoolId          String
  studentId         String
  examSessionId     String
  modeSessionId     String
  questionKey       String
  questionIndex     Int
  questionRef       String?
  stage             String
  attemptNumber     Int
  answerQuality     String?
  isCorrect         Boolean?
  mistakeCategory   String?
  usedHint          Boolean  @default(false)
  hintLevel         String?
  timeSpentBucket   String?
  confidenceBucket  String?
  scoreBucket       String?
  safeEvidenceRefsJson Json  @default("[]")
  createdAt         DateTime @default(now())

  examSession       ExamModeSessionRecord @relation(fields: [examSessionId], references: [id], onDelete: Cascade)

  @@index([examSessionId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([questionKey])
  @@index([questionIndex])
  @@index([createdAt])
}

model ExamModeSummaryRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String
  examSessionId         String
  modeSessionId         String
  finalStage            String
  exitReason            String
  questionCount         Int      @default(0)
  attemptCount          Int      @default(0)
  hintCount             Int      @default(0)
  stuckCount            Int      @default(0)
  recoveryCount         Int      @default(0)
  skippedCount          Int      @default(0)
  flaggedCount          Int      @default(0)
  estimatedReadinessBucket String?
  masterySignal         String?
  summarySignalJson     Json     @default("{}")
  safeEvidenceRefsJson  Json     @default("[]")
  createdAt             DateTime @default(now())

  examSession           ExamModeSessionRecord @relation(fields: [examSessionId], references: [id], onDelete: Cascade)

  @@index([examSessionId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([createdAt])
}

// �� Phase 2 Task 006: Quiz Mode Runtime + Safe Knowledge Check Chamber ��

model QuizModeSessionRecord {
  id                  String   @id @default(cuid())
  schoolId            String
  studentId           String
  tutorLearnerId      String?
  modeSessionId       String
  conversationId      String?
  subjectId           String?
  topicId             String?
  skillId             String?
  approvedContentRef  String?
  quizSetRef          String?
  targetType          String
  quizGoalCategory    String
  quizSessionType     String
  status              String   @default("active")
  currentStage        String   @default("awaiting_quiz_target")
  currentQuestionIndex Int     @default(0)
  questionCount       Int      @default(0)
  attemptCount        Int      @default(0)
  correctCount        Int      @default(0)
  partialCount        Int      @default(0)
  incorrectCount      Int      @default(0)
  hintCount           Int      @default(0)
  stuckCount          Int      @default(0)
  recoveryCount       Int      @default(0)
  skippedCount        Int      @default(0)
  flaggedCount        Int      @default(0)
  safeEvidenceRefsJson Json    @default("[]")
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt
  startedAt           DateTime @default(now())
  submittedAt         DateTime?
  endedAt             DateTime?

  questionStates      QuizModeQuestionStateRecord[]
  attempts            QuizModeAttemptRecord[]
  summaries           QuizModeSummaryRecord[]

  @@index([schoolId, studentId])
  @@index([schoolId, tutorLearnerId])
  @@index([modeSessionId])
  @@index([conversationId])
  @@index([status])
  @@index([createdAt])
}

model QuizModeQuestionStateRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String
  quizSessionId         String
  modeSessionId         String
  questionKey           String
  questionIndex         Int
  questionRef           String?
  questionFingerprint   String?
  topicId               String?
  skillId               String?
  difficultyBucket      String?
  status                String   @default("not_started")
  attemptNumber         Int      @default(0)
  answerQuality         String?
  isCorrect             Boolean?
  mistakeCategory       String?
  confidenceBucket      String?
  timeSpentBucket       String?
  scoreBucket           String?
  retrievalSignal       String?
  selectedTutorAction   String?
  hintLevel             String?
  safeReasonCodesJson   Json     @default("[]")
  safeEvidenceRefsJson  Json     @default("[]")
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt
  completedAt           DateTime?

  quizSession           QuizModeSessionRecord @relation(fields: [quizSessionId], references: [id], onDelete: Cascade)

  @@index([quizSessionId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([questionIndex])
  @@index([status])
  @@index([createdAt])
}

model QuizModeAttemptRecord {
  id                String   @id @default(cuid())
  schoolId          String
  studentId         String
  quizSessionId     String
  modeSessionId     String
  questionKey       String
  questionIndex     Int
  questionRef       String?
  stage             String
  attemptNumber     Int
  answerQuality     String?
  isCorrect         Boolean?
  mistakeCategory   String?
  usedHint          Boolean  @default(false)
  hintLevel         String?
  timeSpentBucket   String?
  confidenceBucket  String?
  scoreBucket       String?
  retrievalSignal   String?
  safeEvidenceRefsJson Json  @default("[]")
  createdAt         DateTime @default(now())

  quizSession       QuizModeSessionRecord @relation(fields: [quizSessionId], references: [id], onDelete: Cascade)

  @@index([quizSessionId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([questionKey])
  @@index([questionIndex])
  @@index([createdAt])
}

model QuizModeSummaryRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String
  quizSessionId         String
  modeSessionId         String
  finalStage            String
  exitReason            String
  questionCount         Int      @default(0)
  attemptCount          Int      @default(0)
  correctCount          Int      @default(0)
  partialCount          Int      @default(0)
  incorrectCount        Int      @default(0)
  hintCount             Int      @default(0)
  stuckCount            Int      @default(0)
  recoveryCount         Int      @default(0)
  skippedCount          Int      @default(0)
  flaggedCount          Int      @default(0)
  estimatedRecallStrengthBucket String?
  estimatedReadinessBucket String?
  masterySignal         String?
  summarySignalJson     Json     @default("{}")
  safeEvidenceRefsJson  Json     @default("[]")
  createdAt             DateTime @default(now())

  quizSession           QuizModeSessionRecord @relation(fields: [quizSessionId], references: [id], onDelete: Cascade)

  @@index([quizSessionId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([createdAt])
}

model TeachBackModeSessionRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String
  tutorLearnerId        String?
  modeSessionId         String
  conversationId        String?
  subjectId             String?
  topicId               String?
  skillId               String?
  approvedContentRef    String?
  targetRef             String?
  promptSetRef          String?
  targetType            String
  teachBackGoalCategory String
  teachBackSessionType  String
  status                String   @default("active")
  currentStage          String   @default("awaiting_teach_back_target")
  currentPromptIndex    Int      @default(0)
  promptCount           Int      @default(0)
  attemptCount          Int      @default(0)
  strongExplanationCount Int     @default(0)
  partialExplanationCount Int   @default(0)
  weakExplanationCount  Int      @default(0)
  misconceptionCount    Int      @default(0)
  hintCount             Int      @default(0)
  stuckCount            Int      @default(0)
  recoveryCount         Int      @default(0)
  reflectionCount       Int      @default(0)
  safeEvidenceRefsJson  Json     @default("[]")
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt
  startedAt             DateTime @default(now())
  submittedAt           DateTime?
  endedAt               DateTime?

  promptStates          TeachBackModePromptStateRecord[]
  attempts              TeachBackModeAttemptRecord[]
  summaries             TeachBackModeSummaryRecord[]

  @@index([schoolId, studentId])
  @@index([schoolId, tutorLearnerId])
  @@index([modeSessionId])
  @@index([conversationId])
  @@index([status])
  @@index([createdAt])
}

model TeachBackModePromptStateRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String
  teachBackSessionId    String
  modeSessionId         String
  promptKey             String
  promptIndex           Int
  promptRef             String?
  questionRef           String?
  contentFingerprint    String?
  topicId               String?
  skillId               String?
  difficultyBucket      String?
  status                String   @default("not_started")
  attemptNumber         Int      @default(0)
  explanationQuality    String?
  conceptCoverageBucket String?
  clarityBucket         String?
  confidenceBucket      String?
  misconceptionSignal   String?
  supportNeed           String?
  masterySignal         String?
  readinessSignal       String?
  selectedTutorAction   String?
  hintLevel             String?
  safeReasonCodesJson   Json     @default("[]")
  safeEvidenceRefsJson  Json     @default("[]")
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt
  completedAt           DateTime?

  teachBackSession      TeachBackModeSessionRecord @relation(fields: [teachBackSessionId], references: [id], onDelete: Cascade)

  @@index([teachBackSessionId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([promptIndex])
  @@index([status])
  @@index([createdAt])
}

model TeachBackModeAttemptRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String
  teachBackSessionId    String
  modeSessionId         String
  promptKey             String
  promptIndex           Int
  promptRef             String?
  stage                 String
  attemptNumber         Int
  explanationQuality    String
  conceptCoverageBucket String?
  clarityBucket         String?
  confidenceBucket      String?
  misconceptionSignal   String?
  supportNeed           String?
  masterySignal         String?
  readinessSignal       String?
  usedHint              Boolean  @default(false)
  hintLevel             String?
  safeEvidenceRefsJson  Json     @default("[]")
  createdAt             DateTime @default(now())

  teachBackSession      TeachBackModeSessionRecord @relation(fields: [teachBackSessionId], references: [id], onDelete: Cascade)

  @@index([teachBackSessionId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([promptKey])
  @@index([promptIndex])
  @@index([createdAt])
}

model TeachBackModeSummaryRecord {
  id                              String   @id @default(cuid())
  schoolId                        String
  studentId                       String
  teachBackSessionId              String
  modeSessionId                   String
  finalStage                      String
  exitReason                      String
  promptCount                     Int      @default(0)
  attemptCount                    Int      @default(0)
  strongExplanationCount          Int      @default(0)
  partialExplanationCount         Int      @default(0)
  weakExplanationCount            Int      @default(0)
  misconceptionCount              Int      @default(0)
  hintCount                       Int      @default(0)
  stuckCount                      Int      @default(0)
  recoveryCount                   Int      @default(0)
  reflectionCount                 Int      @default(0)
  estimatedExplanationStrengthBucket String?
  estimatedReadinessSignal        String?
  masterySignal                   String?
  summarySignalJson               Json     @default("{}")
  safeEvidenceRefsJson            Json     @default("[]")
  createdAt                       DateTime @default(now())

  teachBackSession                TeachBackModeSessionRecord @relation(fields: [teachBackSessionId], references: [id], onDelete: Cascade)

  @@index([teachBackSessionId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([createdAt])
}

// ── Phase 2 Task 008: Revision Mode Runtime + Living Revision Queue ──

model RevisionModeSessionRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String
  tutorLearnerId        String?
  modeSessionId         String
  conversationId        String?
  subjectId             String?
  topicId               String?
  skillId               String?
  approvedContentRef    String?
  targetRef             String?
  revisionGoalCategory  String
  revisionSessionType   String
  status                String   @default("active")
  currentStage          String   @default("awaiting_revision_target")
  currentItemIndex      Int      @default(0)
  itemCount             Int      @default(0)
  attemptCount          Int      @default(0)
  weakRecallCount       Int      @default(0)
  strongRecallCount     Int      @default(0)
  mistakeCount          Int      @default(0)
  hintCount             Int      @default(0)
  stuckCount            Int      @default(0)
  recoveryCount         Int      @default(0)
  reflectionCount       Int      @default(0)
  completedItemCount    Int      @default(0)
  skippedItemCount      Int      @default(0)
  pinnedItemCount       Int      @default(0)
  safeEvidenceRefsJson  Json     @default("[]")
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt
  startedAt             DateTime @default(now())
  submittedAt           DateTime?
  endedAt               DateTime?

  queues                RevisionModeQueueRecord[]
  itemStates            RevisionModeItemStateRecord[]
  attempts              RevisionModeAttemptRecord[]
  summaries             RevisionModeSummaryRecord[]

  @@index([schoolId, studentId])
  @@index([schoolId, tutorLearnerId])
  @@index([modeSessionId])
  @@index([conversationId])
  @@index([status])
  @@index([createdAt])
}

model RevisionModeQueueRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String
  revisionSessionId     String?
  modeSessionId         String?
  conversationId        String?
  subjectId             String?
  topicId               String?
  skillId               String?
  queueType             String
  queueStatus           String   @default("active")
  sourceType            String
  sourceRefsJson        Json     @default("[]")
  itemCount             Int      @default(0)
  currentItemIndex      Int      @default(0)
  dueItemCount          Int      @default(0)
  completedItemCount    Int      @default(0)
  skippedItemCount      Int      @default(0)
  pinnedItemCount       Int      @default(0)
  safeReasonCodesJson   Json     @default("[]")
  safeEvidenceRefsJson  Json     @default("[]")
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  revisionSession       RevisionModeSessionRecord? @relation(fields: [revisionSessionId], references: [id], onDelete: Cascade)
  itemStates            RevisionModeItemStateRecord[]

  @@index([schoolId, studentId])
  @@index([revisionSessionId])
  @@index([modeSessionId])
  @@index([queueStatus])
  @@index([queueType])
  @@index([createdAt])
}

model RevisionModeItemStateRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String
  revisionSessionId     String
  revisionQueueId       String?
  modeSessionId         String
  itemKey               String
  itemIndex             Int
  targetType            String
  targetRef             String?
  approvedContentRef    String?
  sourceMode            String?
  sourceSessionRef      String?
  sourceSummaryRef      String?
  contentFingerprint    String?
  topicId               String?
  skillId               String?
  difficultyBucket      String?
  priorityBucket        String?
  status                String   @default("not_started")
  attemptNumber         Int      @default(0)
  recallQuality         String?
  retrievalSignal       String?
  mistakeCategory       String?
  explanationQuality    String?
  supportNeed           String?
  masterySignal         String?
  readinessSignal       String?
  selectedTutorAction   String?
  hintLevel             String?
  dueAt                 DateTime?
  lastReviewedAt        DateTime?
  nextReviewAt          DateTime?
  reviewIntervalBucket  String?
  safeReasonCodesJson   Json     @default("[]")
  safeEvidenceRefsJson  Json     @default("[]")
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt
  completedAt           DateTime?

  revisionSession       RevisionModeSessionRecord @relation(fields: [revisionSessionId], references: [id], onDelete: Cascade)
  revisionQueue         RevisionModeQueueRecord?  @relation(fields: [revisionQueueId], references: [id], onDelete: SetNull)
  attempts              RevisionModeAttemptRecord[]

  @@index([revisionSessionId])
  @@index([revisionQueueId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([itemIndex])
  @@index([status])
  @@index([dueAt])
  @@index([nextReviewAt])
  @@index([createdAt])
}

model RevisionModeAttemptRecord {
  id                    String   @id @default(cuid())
  schoolId              String
  studentId             String
  revisionSessionId     String
  revisionQueueId       String?
  revisionItemStateId   String?
  modeSessionId         String
  itemKey               String
  itemIndex             Int
  targetRef             String?
  stage                 String
  attemptNumber         Int
  recallQuality         String
  retrievalSignal       String?
  mistakeCategory       String?
  explanationQuality    String?
  supportNeed           String?
  masterySignal         String?
  readinessSignal       String?
  usedHint              Boolean  @default(false)
  hintLevel             String?
  safeEvidenceRefsJson  Json     @default("[]")
  createdAt             DateTime @default(now())

  revisionSession       RevisionModeSessionRecord    @relation(fields: [revisionSessionId], references: [id], onDelete: Cascade)
  revisionItemState     RevisionModeItemStateRecord? @relation(fields: [revisionItemStateId], references: [id], onDelete: SetNull)

  @@index([revisionSessionId])
  @@index([revisionQueueId])
  @@index([revisionItemStateId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([itemKey])
  @@index([itemIndex])
  @@index([createdAt])
}

model RevisionModeSummaryRecord {
  id                              String   @id @default(cuid())
  schoolId                        String
  studentId                       String
  revisionSessionId               String
  revisionQueueId                 String?
  modeSessionId                   String
  finalStage                      String
  exitReason                      String
  itemCount                       Int      @default(0)
  attemptCount                    Int      @default(0)
  weakRecallCount                 Int      @default(0)
  strongRecallCount               Int      @default(0)
  mistakeCount                    Int      @default(0)
  hintCount                       Int      @default(0)
  stuckCount                      Int      @default(0)
  recoveryCount                   Int      @default(0)
  reflectionCount                 Int      @default(0)
  completedItemCount              Int      @default(0)
  skippedItemCount                Int      @default(0)
  pinnedItemCount                 Int      @default(0)
  estimatedRecallStrengthBucket   String?
  estimatedReadinessSignal        String?
  masterySignal                   String?
  nextReviewAt                    DateTime?
  summarySignalJson               Json     @default("{}")
  safeEvidenceRefsJson            Json     @default("[]")
  createdAt                       DateTime @default(now())

  revisionSession                 RevisionModeSessionRecord @relation(fields: [revisionSessionId], references: [id], onDelete: Cascade)

  @@index([revisionSessionId])
  @@index([revisionQueueId])
  @@index([modeSessionId])
  @@index([schoolId, studentId])
  @@index([createdAt])
}

model QuestionBankItemRecord {
  questionId         String   @id @default(uuid())
  schoolId           String
  status             String   @default("draft")
  subjectId          String
  topicId            String
  skillId            String
  curriculumVersionId String
  primaryObjectiveId String
  currentVersionId   String
  createdByActorId   String
  createdByRole      String
  sourceType         String
  securityClass      String   @default("practice_safe")
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt
  archivedAt         DateTime?

  @@index([schoolId])
  @@index([schoolId, status])
  @@index([schoolId, subjectId])
  @@index([status])
  @@index([subjectId])
}

model QuestionVersionRecord {
  questionVersionId    String   @id @default(uuid())
  questionId           String
  versionNumber        Int
  status               String   @default("draft")
  stemSafeText         String   @db.Text
  questionType         String
  difficultyBand       String   @default("recall")
  language             String   @default("English")
  studentSafeExplanation String @db.Text
  teacherExplanation   String   @db.Text
  estimatedTimeSeconds Int      @default(0)
  createdByActorId     String
  createdAt            DateTime @default(now())
  approvedAt           DateTime?
  supersededAt         DateTime?
  contentHash          String

  @@index([questionId])
  @@index([questionId, versionNumber])
  @@index([contentHash])
}

model QuestionPartVersionRecord {
  questionPartVersionId    String   @id @default(uuid())
  questionVersionId        String
  partKey                  String
  partOrder                Int
  promptSafeText           String   @db.Text
  marksAvailable           Int      @default(0)
  expectedWorkingVisibility Boolean @default(true)
  studentInputMode         String   @default("text")
  createdAt                DateTime @default(now())

  @@index([questionVersionId])
}

model QuestionAssetVersionRecord {
  assetVersionId    String   @id @default(uuid())
  questionVersionId String
  assetType         String
  assetRef          String
  assetFingerprint  String
  studentVisible    Boolean  @default(true)
  teacherOnly       Boolean  @default(false)
  altText           String
  createdAt         DateTime @default(now())

  @@index([questionVersionId])
}

model AnswerKeyVersionRecord {
  answerKeyVersionId   String   @id @default(uuid())
  questionVersionId    String
  status               String   @default("draft")
  answerKeySafeRef     String   @db.Text
  correctAnswerSummary String   @db.Text
  markingNotesTeacherOnly String @db.Text
  createdByActorId     String
  createdAt            DateTime @default(now())
  approvedAt           DateTime?

  @@index([questionVersionId])
}

model RubricVersionRecord {
  rubricVersionId     String   @id @default(uuid())
  questionVersionId   String
  status              String   @default("draft")
  rubricPublicSummary String   @db.Text
  rubricInternal      String   @db.Text
  marksTotal          Int      @default(0)
  criteriaJson        Json?
  createdByActorId    String
  createdAt           DateTime @default(now())
  approvedAt          DateTime?

  @@index([questionVersionId])
}

model QuestionObjectiveMappingRecord {
  mappingId          String   @id @default(uuid())
  questionVersionId  String
  objectiveId        String
  objectiveVersionId String
  mappingStrength    String   @default("primary")
  mappingReason      String
  createdAt          DateTime @default(now())

  @@index([questionVersionId])
  @@index([objectiveId])
}

model QuestionSourceRecordRecord {
  sourceRecordId    String   @id @default(uuid())
  questionId        String
  questionVersionId String
  sourceType        String
  sourceRef         String
  approvedSourceId  String?
  importBatchId     String?
  createdByActorId  String
  createdAt         DateTime @default(now())
  safeSummary       String   @db.Text

  @@index([questionId])
  @@index([questionVersionId])
  @@index([sourceType])
  @@index([approvedSourceId])
}

model QuestionCurriculumValidityRecord {
  id                  String   @id @default(uuid())
  questionVersionId   String
  schoolId            String
  curriculumVersionId String
  objectiveIdsJson    Json?
  valid               Boolean  @default(false)
  reasonCodesJson     Json?
  checkedAt           DateTime @default(now())

  @@index([questionVersionId])
  @@index([schoolId])
  @@index([curriculumVersionId])
}

model QuestionUsageEligibilityRecord {
  id                String   @id @default(uuid())
  questionVersionId String
  usageMode         String
  eligible          Boolean  @default(false)
  reasonCodesJson   Json?
  checkedAt         DateTime @default(now())

  @@index([questionVersionId])
  @@index([questionVersionId, usageMode])
  @@index([usageMode])
}

model QuestionApprovalRequestRecord {
  approvalRequestId    String   @id @default(uuid())
  schoolId             String
  questionId           String
  questionVersionId    String
  requestedByActorId   String
  requestedByRole      String
  status               String   @default("pending")
  requestReason        String
  policyVersionRefsJson Json?
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt
  closedAt             DateTime?

  @@index([schoolId])
  @@index([schoolId, status])
  @@index([questionId])
  @@index([questionVersionId])
}

model QuestionApprovalRecord {
  approvalRecordId  String   @id @default(uuid())
  schoolId          String
  approvalRequestId String
  questionId        String
  questionVersionId String
  decision          String
  decidedByActorId  String
  decidedByRole     String
  decisionReason    String
  reasonCodesJson   Json?
  createdAt         DateTime @default(now())

  @@index([schoolId])
  @@index([approvalRequestId])
  @@index([questionId])
  @@index([questionVersionId])
}

model QuestionDuplicateCandidateRecord {
  duplicateCandidateId       String   @id @default(uuid())
  schoolId                   String
  sourceQuestionVersionId    String
  candidateQuestionVersionId String
  contentHash                String
  similarityReason           String
  status                     String   @default("suspected")
  createdAt                  DateTime @default(now())
  resolvedAt                 DateTime?
  resolvedByActorId          String?
  resolutionReason           String?

  @@index([schoolId])
  @@index([sourceQuestionVersionId])
  @@index([candidateQuestionVersionId])
  @@index([contentHash])
}

model QuestionExposureHoldRecord {
  exposureHoldId    String   @id @default(uuid())
  schoolId          String
  questionId        String
  questionVersionId String
  holdType          String
  status            String   @default("active")
  reasonCode        String
  safeSummary       String
  createdByActorId  String
  createdAt         DateTime @default(now())
  releasedByActorId String?
  releasedAt        DateTime?
  releaseReason     String?

  @@index([schoolId])
  @@index([questionId])
  @@index([questionVersionId])
  @@index([schoolId, status])
}

model QuestionIngestionBatchRecord {
  ingestionBatchId   String   @id @default(uuid())
  schoolId           String
  sourceType         String
  approvedSourceId   String?
  importBatchRef     String?
  status             String   @default("draft")
  createdByActorId   String
  createdByRole      String
  candidateCount     Int      @default(0)
  acceptedCount      Int      @default(0)
  rejectedCount      Int      @default(0)
  warningCount       Int      @default(0)
  safeSummary        String
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt
  completedAt        DateTime?

  @@index([schoolId])
  @@index([schoolId, status])
  @@index([sourceType])
  @@index([approvedSourceId])
}

model QuestionIngestionCandidateRecord {
  candidateId               String   @id @default(uuid())
  ingestionBatchId          String
  schoolId                  String
  status                    String   @default("draft")
  candidateType             String
  stemSafeText              String   @db.Text
  questionType              String
  subjectId                 String
  topicId                   String
  skillId                   String
  curriculumVersionId       String
  primaryObjectiveId        String
  approvedSourceId          String?
  sourceRef                 String
  contentHash               String
  warningsJson              Json?
  safeMetadataJson          Json?
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt
  acceptedQuestionId        String?
  acceptedQuestionVersionId String?
  rejectedReasonCode        String?

  @@index([ingestionBatchId])
  @@index([schoolId])
  @@index([schoolId, status])
  @@index([contentHash])
}

// ─── Package 4: Exam Blueprinting, Question Selection, Draft Sets ──────────────

model ExamBlueprintRecord {
  blueprintId         String   @id @default(uuid())
  schoolId            String
  status              String   @default("draft")
  title               String
  subjectId           String
  curriculumVersionId String
  gradeBand           String
  examType            String
  createdByActorId    String
  createdByRole       String
  currentVersionId    String?
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt
  archivedAt          DateTime?

  @@index([schoolId])
  @@index([schoolId, status])
  @@index([schoolId, subjectId])
  @@index([curriculumVersionId])
  @@index([currentVersionId])
}

model ExamBlueprintVersionRecord {
  blueprintVersionId      String   @id @default(uuid())
  blueprintId             String
  versionNumber           Int
  status                  String   @default("draft")
  title                   String
  safeDescription         String   @db.Text
  durationMinutes         Int      @default(0)
  totalMarks              Int      @default(0)
  targetQuestionCount     Int      @default(0)
  difficultyMixJson       Json?
  questionTypeMixJson     Json?
  securityClassRequirement String  @default("practice_safe")
  coveragePolicy          String   @default("balanced_weighted")
  selectionStrategy       String   @default("balanced")
  createdByActorId        String
  createdAt               DateTime @default(now())
  approvedAt              DateTime?
  supersededAt            DateTime?

  @@index([blueprintId])
  @@index([blueprintId, versionNumber])
  @@index([status])
  @@index([securityClassRequirement])
}

model ExamBlueprintRequirementRecord {
  requirementId        String   @id @default(uuid())
  blueprintVersionId   String
  schoolId             String
  requirementType      String
  subjectId            String
  topicId              String
  skillId              String
  objectiveId          String
  requiredQuestionCount Int     @default(0)
  requiredMarks        Int      @default(0)
  minimumDifficulty    String   @default("recall")
  maximumDifficulty    String   @default("creation")
  questionType         String   @default("multiple_choice")
  weight               Int      @default(1)
  isMandatory          Boolean  @default(true)
  createdAt            DateTime @default(now())

  @@index([blueprintVersionId])
  @@index([schoolId])
  @@index([objectiveId])
  @@index([topicId])
  @@index([skillId])
  @@index([questionType])
}

model ExamDraftSetRecord {
  draftSetId          String   @id @default(uuid())
  schoolId            String
  blueprintId         String
  blueprintVersionId  String
  status              String   @default("draft")
  requestedDraftCount Int      @default(3)
  generatedDraftCount Int      @default(0)
  selectionStrategy   String   @default("balanced")
  createdByActorId    String
  createdByRole       String
  safeSummary         String   @db.Text
  coverageGapSummary  String   @db.Text
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt
  completedAt         DateTime?

  @@index([schoolId])
  @@index([schoolId, status])
  @@index([blueprintId])
  @@index([blueprintVersionId])
  @@index([createdAt])
}

model ExamDraftRecord {
  draftId                   String   @id @default(uuid())
  draftSetId                String
  schoolId                  String
  blueprintId               String
  blueprintVersionId        String
  rank                      Int      @default(0)
  status                    String   @default("candidate")
  draftTitle                String
  totalMarks                Int      @default(0)
  estimatedDurationMinutes  Int      @default(0)
  questionCount             Int      @default(0)
  coverageScore             Float    @default(0)
  difficultyBalanceScore    Float    @default(0)
  securityScore             Float    @default(0)
  freshnessScore            Float    @default(0)
  overallScore              Float    @default(0)
  recommendationReason      String   @db.Text
  safeTeacherSummary        String   @db.Text
  differenceFromPreviousDraft String @db.Text
  warningCodesJson          Json?
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt

  @@index([draftSetId])
  @@index([schoolId])
  @@index([rank])
  @@index([status])
  @@index([overallScore])
}

model ExamDraftQuestionRecord {
  draftQuestionId   String   @id @default(uuid())
  draftId           String
  schoolId          String
  questionId        String
  questionVersionId String
  position          Int      @default(0)
  sectionKey        String   @default("default")
  marksAllocated    Int      @default(0)
  selectionReason   String   @db.Text
  requirementId     String
  coverageTagsJson  Json?
  warningCodesJson  Json?
  createdAt         DateTime @default(now())

  @@index([draftId])
  @@index([schoolId])
  @@index([questionId])
  @@index([questionVersionId])
  @@index([requirementId])
  @@index([position])
}

model QuestionSelectionRunRecord {
  selectionRunId       String   @id @default(uuid())
  schoolId             String
  blueprintId          String
  blueprintVersionId   String
  draftSetId           String
  status               String   @default("started")
  strategy             String   @default("balanced")
  candidatePoolSize    Int      @default(0)
  eligiblePoolSize     Int      @default(0)
  selectedCount        Int      @default(0)
  rejectedCount        Int      @default(0)
  gapCount             Int      @default(0)
  safeSummary          String   @db.Text
  createdAt            DateTime @default(now())
  completedAt          DateTime?

  @@index([schoolId])
  @@index([blueprintVersionId])
  @@index([draftSetId])
  @@index([status])
  @@index([createdAt])
}

model QuestionSelectionCandidateRecord {
  selectionCandidateId   String   @id @default(uuid())
  selectionRunId         String
  schoolId               String
  questionId             String
  questionVersionId      String
  eligible               Boolean  @default(false)
  selected               Boolean  @default(false)
  rejectionReasonCode    String
  score                  Float    @default(0)
  coverageContributionJson Json?
  riskFlagsJson          Json?
  createdAt              DateTime @default(now())

  @@index([selectionRunId])
  @@index([schoolId])
  @@index([questionId])
  @@index([questionVersionId])
  @@index([eligible])
  @@index([selected])
}

// ─── Package 6: Exam Paper Assembly, Versioning, Variant Planning, Access, Approval, Delivery Bridge ──

model ExamPaperRecord {
  paperId             String   @id @default(uuid())
  schoolId            String
  status              String   @default("draft")
  sourceDraftSetId    String
  sourceDraftId       String
  blueprintId         String
  blueprintVersionId  String
  title               String
  subjectId           String
  curriculumVersionId String
  gradeBand           String
  examType            String
  createdByActorId    String
  createdByRole       String
  currentVersionId    String?
  safeSummary         String   @db.Text
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt
  archivedAt          DateTime?

  @@index([schoolId])
  @@index([schoolId, status])
  @@index([sourceDraftId])
  @@index([sourceDraftSetId])
  @@index([blueprintVersionId])
  @@index([subjectId])
  @@index([curriculumVersionId])
  @@index([createdAt])
}

model ExamPaperVersionRecord {
  paperVersionId      String   @id @default(uuid())
  paperId             String
  schoolId            String
  versionNumber       Int
  status              String   @default("draft")
  title               String
  instructionsSafeText String  @db.Text
  durationMinutes     Int      @default(0)
  totalMarks          Int      @default(0)
  questionCount       Int      @default(0)
  sectionCount        Int      @default(0)
  variantCount        Int      @default(0)
  assemblyPolicyJson  Json?
  securityClass       String   @default("practice_safe")
  createdByActorId    String
  createdAt           DateTime @default(now())
  approvedAt          DateTime?
  supersededAt        DateTime?

  @@index([paperId])
  @@index([paperId, versionNumber])
  @@index([schoolId])
  @@index([status])
  @@index([securityClass])
  @@index([createdAt])
}

model ExamPaperSectionRecord {
  sectionId              String   @id @default(uuid())
  schoolId               String
  paperVersionId         String
  sectionKey             String
  sectionTitle           String
  sectionOrder           Int      @default(0)
  instructionsSafeText   String   @db.Text
  marksAvailable         Int      @default(0)
  questionCount          Int      @default(0)
  timingGuidanceMinutes  Int      @default(0)
  createdAt              DateTime @default(now())

  @@index([schoolId])
  @@index([paperVersionId])
  @@index([sectionKey])
  @@index([sectionOrder])
}

model ExamPaperQuestionRecord {
  paperQuestionId      String   @id @default(uuid())
  schoolId             String
  paperVersionId       String
  sectionId            String
  questionId           String
  questionVersionId    String
  sourceDraftQuestionId String
  position             Int      @default(0)
  marksAllocated       Int      @default(0)
  required             Boolean  @default(true)
  studentVisible       Boolean  @default(true)
  answerKeyLinked      Boolean  @default(false)
  rubricLinked         Boolean  @default(false)
  selectionReason      String   @db.Text
  safeTeacherSummary   String   @db.Text
  createdAt            DateTime @default(now())

  @@index([schoolId])
  @@index([paperVersionId])
  @@index([sectionId])
  @@index([questionId])
  @@index([questionVersionId])
  @@index([sourceDraftQuestionId])
  @@index([position])
}

model ExamVariantRecord {
  variantId            String   @id @default(uuid())
  schoolId             String
  paperVersionId       String
  variantCode          String
  status               String   @default("draft")
  variantStrategy      String   @default("same_questions_reordered")
  questionCount        Int      @default(0)
  totalMarks           Int      @default(0)
  shuffleSections      Boolean  @default(false)
  shuffleQuestionsWithinSections Boolean @default(true)
  safeSummary          String   @db.Text
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt
  approvedAt           DateTime?

  @@index([schoolId])
  @@index([paperVersionId])
  @@index([variantCode])
  @@index([status])
  @@index([createdAt])
}

model ExamVariantQuestionRecord {
  variantQuestionId    String   @id @default(uuid())
  schoolId             String
  variantId            String
  paperQuestionId      String
  questionId           String
  questionVersionId    String
  sectionKey           String
  variantPosition      Int      @default(0)
  marksAllocated       Int      @default(0)
  mappingReason        String   @db.Text
  createdAt            DateTime @default(now())

  @@index([schoolId])
  @@index([variantId])
  @@index([paperQuestionId])
  @@index([questionId])
  @@index([questionVersionId])
  @@index([sectionKey])
  @@index([variantPosition])
}

model ExamAccessPolicyRecord {
  accessPolicyId           String   @id @default(uuid())
  schoolId                 String
  paperId                  String
  paperVersionId           String
  status                   String   @default("draft")
  intendedAudienceType     String   @default("class")
  classScopeRefsJson       Json?
  roleScopeRefsJson        Json?
  availabilityMode         String   @default("manual_teacher_activation")
  requiresTeacherActivation Boolean @default(true)
  allowStudentSelfStart    Boolean  @default(false)
  allowRetake              Boolean  @default(false)
  maxAttempts              Int      @default(1)
  safePolicySummary        String   @db.Text
  createdByActorId         String
  createdAt                DateTime @default(now())
  updatedAt                DateTime @updatedAt

  @@index([schoolId])
  @@index([paperId])
  @@index([paperVersionId])
  @@index([status])
  @@index([intendedAudienceType])
  @@index([createdAt])
}

model ExamPaperApprovalRecord {
  paperApprovalId     String   @id @default(uuid())
  schoolId            String
  paperId             String
  paperVersionId      String
  decision            String
  decisionReasonCode  String
  safeReason          String   @db.Text
  decidedByActorId    String
  decidedByRole       String
  createdAt           DateTime @default(now())

  @@index([schoolId])
  @@index([paperId])
  @@index([paperVersionId])
  @@index([decision])
  @@index([decidedByActorId])
  @@index([createdAt])
}

model ExamPaperAssemblyRunRecord {
  assemblyRunId        String   @id @default(uuid())
  schoolId             String
  sourceDraftSetId     String
  sourceDraftId        String
  paperId              String
  paperVersionId       String
  status               String   @default("started")
  assemblyStrategy     String   @default("from_selected_draft")
  inputQuestionCount   Int      @default(0)
  assembledQuestionCount Int   @default(0)
  warningCount         Int      @default(0)
  blockedCount         Int      @default(0)
  safeSummary          String   @db.Text
  createdByActorId     String
  createdByRole        String
  createdAt            DateTime @default(now())
  completedAt          DateTime?

  @@index([schoolId])
  @@index([sourceDraftId])
  @@index([sourceDraftSetId])
  @@index([paperId])
  @@index([paperVersionId])
  @@index([status])
  @@index([createdAt])
}

model ExamPaperDeliveryBridgeRecord {
  deliveryBridgeId     String   @id @default(uuid())
  schoolId             String
  paperId              String
  paperVersionId       String
  status               String   @default("draft")
  bridgeType           String   @default("exam_mode_contract")
  compatibleRuntime    String   @default("exam_mode_future")
  contractVersion      String   @default("1.0")
  safeContractSummary  String   @db.Text
  blockedReasonCode    String?
  createdAt            DateTime @default(now())
  validatedAt          DateTime?

  @@index([schoolId])
  @@index([paperId])
  @@index([paperVersionId])
  @@index([status])
  @@index([bridgeType])
  @@index([compatibleRuntime])
  @@index([createdAt])
}

// ─── Package 7: Exam Delivery Session, Variant Assignment, Student Attempt, Capture ──

model ExamDeliverySessionRecord {
  deliverySessionId   String   @id @default(uuid())
  schoolId            String
  paperId             String
  paperVersionId      String
  deliveryBridgeId    String
  accessPolicyId      String
  status              String   @default("draft")
  sessionMode         String   @default("teacher_controlled")
  title               String
  safeInstructions    String   @db.Text
  intendedAudienceType String  @default("class")
  classScopeRefsJson  Json?
  roleScopeRefsJson   Json?
  activationMode      String   @default("manual_teacher_activation")
  createdByActorId    String
  createdByRole       String
  openedAt            DateTime?
  closedAt            DateTime?
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt
  archivedAt          DateTime?

  @@index([schoolId])
  @@index([schoolId, status])
  @@index([paperId])
  @@index([paperVersionId])
  @@index([deliveryBridgeId])
  @@index([accessPolicyId])
  @@index([createdAt])
  @@index([openedAt])
  @@index([closedAt])
}

model ExamDeliverySessionStateRecord {
  sessionStateId        String   @id @default(uuid())
  schoolId              String
  deliverySessionId     String
  status                String   @default("draft")
  activeAttemptCount    Int      @default(0)
  submittedAttemptCount Int      @default(0)
  pausedAttemptCount    Int      @default(0)
  blockedAttemptCount   Int      @default(0)
  lastStateChangeReason String?
  safeStateSummary      String   @db.Text
  version               Int      @default(1)
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt

  @@index([schoolId])
  @@index([deliverySessionId])
  @@index([status])
  @@index([updatedAt])
}

model ExamVariantAssignmentRecord {
  variantAssignmentId   String   @id @default(uuid())
  schoolId              String
  deliverySessionId     String
  paperId               String
  paperVersionId        String
  variantId             String
  studentRef            String
  learnerRefType        String   @default("mock_student_ref")
  assignmentStatus      String   @default("assigned")
  assignmentStrategy    String   @default("manual_teacher_assignment")
  assignedByActorId     String
  assignedByRole        String
  safeAssignmentSummary String   @db.Text
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt
  revokedAt             DateTime?

  @@index([schoolId])
  @@index([deliverySessionId])
  @@index([studentRef])
  @@index([variantId])
  @@index([assignmentStatus])
  @@index([createdAt])
}

model ExamAttemptRecord {
  attemptId               String   @id @default(uuid())
  schoolId                String
  deliverySessionId       String
  variantAssignmentId     String
  paperId                 String
  paperVersionId          String
  variantId               String
  studentRef              String
  status                  String   @default("not_started")
  attemptNumber           Int      @default(1)
  startedAt               DateTime?
  lastSeenAt              DateTime?
  submittedAt             DateTime?
  autoSubmittedAt         DateTime?
  cancelledAt             DateTime?
  blockedAt               DateTime?
  durationSecondsAllowed  Int      @default(0)
  durationSecondsUsed     Int      @default(0)
  safeAttemptSummary      String   @db.Text
  createdAt               DateTime @default(now())
  updatedAt               DateTime @updatedAt

  @@index([schoolId])
  @@index([deliverySessionId])
  @@index([studentRef])
  @@index([variantAssignmentId])
  @@index([paperVersionId])
  @@index([variantId])
  @@index([status])
  @@index([startedAt])
  @@index([submittedAt])
}

model ExamAttemptQuestionSnapshotRecord {
  attemptQuestionSnapshotId String   @id @default(uuid())
  schoolId                  String
  attemptId                 String
  deliverySessionId         String
  paperQuestionId           String
  variantQuestionId         String
  questionId                String
  questionVersionId         String
  sectionKey                String
  displayOrder              Int      @default(0)
  marksAvailable            Int      @default(0)
  studentVisiblePromptSafe  String   @db.Text
  answerInputType           String   @default("text")
  snapshotStatus            String   @default("active")
  createdAt                 DateTime @default(now())

  @@index([schoolId])
  @@index([attemptId])
  @@index([deliverySessionId])
  @@index([questionId])
  @@index([questionVersionId])
  @@index([paperQuestionId])
  @@index([variantQuestionId])
  @@index([displayOrder])
}

model ExamAnswerSubmissionRecord {
  answerSubmissionId      String   @id @default(uuid())
  schoolId                String
  attemptId               String
  attemptQuestionSnapshotId String
  deliverySessionId       String
  studentRef              String
  answerStatus            String   @default("draft_saved")
  answerTextSafe          String?  @db.Text
  answerPayloadJson       Json?
  attachmentRefsJson      Json?
  clientSavedAt           DateTime?
  serverReceivedAt        DateTime @default(now())
  revisionNumber          Int      @default(1)
  isFinal                 Boolean  @default(false)
  safeSubmissionSummary   String   @db.Text
  createdAt               DateTime @default(now())
  updatedAt               DateTime @updatedAt

  @@index([schoolId])
  @@index([attemptId])
  @@index([attemptQuestionSnapshotId])
  @@index([studentRef])
  @@index([answerStatus])
  @@index([isFinal])
  @@index([serverReceivedAt])
}

model ExamAttemptTimingEventRecord {
  timingEventId         String   @id @default(uuid())
  schoolId              String
  attemptId             String
  deliverySessionId     String
  eventType             String
  eventAt               DateTime @default(now())
  durationSecondsUsed   Int      @default(0)
  durationSecondsRemaining Int  @default(0)
  safeTimingSummary     String   @db.Text
  metadataJson          Json?
  createdAt             DateTime @default(now())

  @@index([schoolId])
  @@index([attemptId])
  @@index([deliverySessionId])
  @@index([eventType])
  @@index([eventAt])
  @@index([createdAt])
}

model ExamAttemptSubmissionSnapshotRecord {
  submissionSnapshotId    String   @id @default(uuid())
  schoolId                String
  attemptId               String
  deliverySessionId       String
  paperId                 String
  paperVersionId          String
  variantId               String
  studentRef              String
  snapshotStatus          String   @default("draft")
  submittedAnswerCount    Int      @default(0)
  questionSnapshotCount   Int      @default(0)
  totalMarksAvailable     Int      @default(0)
  submissionPayloadJson   Json?
  safeSnapshotSummary     String   @db.Text
  createdAt               DateTime @default(now())
  sealedAt                DateTime?

  @@index([schoolId])
  @@index([attemptId])
  @@index([deliverySessionId])
  @@index([studentRef])
  @@index([snapshotStatus])
  @@index([createdAt])
  @@index([sealedAt])
}

model ExamDeliveryAuditRecord {
  deliveryAuditId   String   @id @default(uuid())
  schoolId          String
  deliverySessionId String?
  attemptId         String?
  actorId           String
  actorRole         String
  eventType         String
  decision          String
  safeSummary       String   @db.Text
  reasonCodesJson   Json?
  metadataJson      Json?
  requestId         String?
  correlationId     String?
  createdAt         DateTime @default(now())

  @@index([schoolId])
  @@index([deliverySessionId])
  @@index([attemptId])
  @@index([actorId])
  @@index([eventType])
  @@index([createdAt])
  @@index([requestId])
  @@index([correlationId])
}

model ExamDeliveryIdempotencyRecord {
  deliveryIdempotencyId String   @id @default(uuid())
  schoolId              String
  operation             String
  idempotencyKey        String
  requestHash           String
  status                String   @default("in_progress")
  resourceType          String?
  resourceId            String?
  safeResultSummary     String?
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt
  expiresAt             DateTime?

  @@unique([schoolId, operation, idempotencyKey])
  @@index([schoolId])
  @@index([operation])
  @@index([idempotencyKey])
  @@index([status])
  @@index([expiresAt])
  @@index([createdAt])
}

// ─── Package 5: Marking, Teacher Review, Moderation, Student Challenge ──────────────

model MarkingRunRecord {
  markingRunId         String   @id @default(uuid())
  schoolId             String
  status               String   @default("draft")
  sourceType           String
  sourceRef            String
  blueprintId          String?
  blueprintVersionId   String?
  draftId              String?
  createdByActorId     String
  createdByRole        String
  policyVersionRefsJson Json?
  inputSnapshotCount   Int      @default(0)
  markedCount          Int      @default(0)
  reviewRequiredCount  Int      @default(0)
  blockedCount         Int      @default(0)
  safeSummary          String   @db.Text
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt
  completedAt          DateTime?

  @@index([schoolId])
  @@index([schoolId, status])
  @@index([sourceType])
  @@index([sourceRef])
  @@index([blueprintVersionId])
  @@index([draftId])
  @@index([createdAt])
}

model MarkingResultVersionRecord {
  markingResultVersionId String   @id @default(uuid())
  schoolId               String
  markingRunId           String
  questionId             String
  questionVersionId      String
  answerSnapshotRef      String   @db.Text
  resultVersionNumber    Int      @default(1)
  status                 String   @default("draft")
  questionType           String
  markingMethod          String
  marksAwarded           Float    @default(0)
  marksAvailable         Float    @default(0)
  confidence             Float    @default(0)
  requiresTeacherReview  Boolean  @default(false)
  reviewReasonCode       String
  safeStudentFeedback    String   @db.Text
  safeTeacherSummary     String   @db.Text
  rubricVersionId        String?
  answerKeyVersionId     String?
  createdByActorId       String
  createdByRole          String
  createdAt              DateTime @default(now())
  supersededAt           DateTime?

  @@index([schoolId])
  @@index([markingRunId])
  @@index([questionId])
  @@index([questionVersionId])
  @@index([status])
  @@index([requiresTeacherReview])
  @@index([rubricVersionId])
  @@index([answerKeyVersionId])
  @@index([createdAt])
}

model MarkingBreakdownItemRecord {
  breakdownItemId           String   @id @default(uuid())
  schoolId                  String
  markingResultVersionId    String
  criterionKey              String
  criterionLabel            String
  marksAwarded              Float    @default(0)
  marksAvailable            Float    @default(0)
  confidence                Float    @default(0)
  decision                  String   @default("uncertain")
  safeReason                String   @db.Text
  teacherOnlyNotes          String   @db.Text
  createdAt                 DateTime @default(now())

  @@index([schoolId])
  @@index([markingResultVersionId])
  @@index([criterionKey])
  @@index([decision])
}

model ScoringSuggestionRecord {
  scoringSuggestionId String   @id @default(uuid())
  schoolId            String
  markingResultVersionId String
  suggestionSource    String
  status              String   @default("suggested")
  suggestedMarks      Float    @default(0)
  confidence          Float    @default(0)
  safeRationale       String   @db.Text
  reviewRequired      Boolean  @default(false)
  reasonCodesJson     Json?
  createdAt           DateTime @default(now())
  acceptedAt          DateTime?
  rejectedAt          DateTime?
  decidedByActorId    String?

  @@index([schoolId])
  @@index([markingResultVersionId])
  @@index([suggestionSource])
  @@index([status])
  @@index([createdAt])
}

model TeacherReviewGroupRecord {
  teacherReviewGroupId String   @id @default(uuid())
  schoolId             String
  markingRunId         String
  status               String   @default("open")
  groupType            String
  reasonCode           String
  questionId           String
  questionVersionId    String
  itemCount            Int      @default(0)
  safeSummary          String   @db.Text
  recommendedAction    String
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt
  closedAt             DateTime?

  @@index([schoolId])
  @@index([markingRunId])
  @@index([status])
  @@index([groupType])
  @@index([questionVersionId])
  @@index([createdAt])
}

model TeacherReviewItemRecord {
  teacherReviewItemId   String   @id @default(uuid())
  schoolId              String
  teacherReviewGroupId  String
  markingRunId          String
  markingResultVersionId String
  status                String   @default("open")
  reviewReasonCode      String
  priority              Int      @default(0)
  assignedToActorId     String?
  safeSummary           String   @db.Text
  createdAt             DateTime @default(now())
  updatedAt             DateTime @updatedAt
  resolvedAt            DateTime?

  @@index([schoolId])
  @@index([teacherReviewGroupId])
  @@index([markingRunId])
  @@index([markingResultVersionId])
  @@index([status])
  @@index([priority])
  @@index([assignedToActorId])
}

model TeacherOverrideRecord {
  teacherOverrideId   String   @id @default(uuid())
  schoolId            String
  markingResultVersionId String
  teacherReviewItemId String?
  decision            String
  previousMarks       Float    @default(0)
  newMarks            Float    @default(0)
  overrideReasonCode  String
  safeReason          String   @db.Text
  decidedByActorId    String
  decidedByRole       String
  createdAt           DateTime @default(now())

  @@index([schoolId])
  @@index([markingResultVersionId])
  @@index([teacherReviewItemId])
  @@index([decision])
  @@index([decidedByActorId])
  @@index([createdAt])
}

model ModerationDecisionRecord {
  moderationDecisionId String   @id @default(uuid())
  schoolId             String
  markingResultVersionId String
  teacherOverrideId    String?
  status               String   @default("pending")
  decision             String
  safeReason           String   @db.Text
  decidedByActorId     String
  decidedByRole        String
  createdAt            DateTime @default(now())

  @@index([schoolId])
  @@index([markingResultVersionId])
  @@index([teacherOverrideId])
  @@index([status])
  @@index([decision])
  @@index([createdAt])
}

model StudentMarkChallengeRecord {
  studentMarkChallengeId String   @id @default(uuid())
  schoolId               String
  studentId              String
  markingResultVersionId String
  status                 String   @default("submitted")
  challengeReasonCode    String
  safeStudentStatement   String   @db.Text
  createdAt              DateTime @default(now())
  reviewedByActorId      String?
  reviewedByRole         String?
  reviewedAt             DateTime?
  resolution             String?
  safeResolutionSummary  String   @db.Text

  @@index([schoolId])
  @@index([studentId])
  @@index([markingResultVersionId])
  @@index([status])
  @@index([createdAt])
}

// ─── Package 8: Marking Invocation Bridge, Snapshot Intake, Batch Orchestration ──────

model MarkingInvocationRequestRecord {
  markingInvocationRequestId String   @id @default(uuid())
  schoolId                   String
  deliverySessionId          String
  paperId                    String
  paperVersionId             String
  requestedByActorId         String
  requestedByRole            String
  invocationStatus           String   @default("draft")
  invocationMode             String   @default("deterministic_plus_teacher_review")
  sourceType                 String   @default("delivery_session_snapshot_batch")
  submittedSnapshotRefsJson   Json?
  safeRequestSummary         String   @db.Text
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  cancelledAt                DateTime?

  @@index([schoolId])
  @@index([schoolId, invocationStatus])
  @@index([deliverySessionId])
  @@index([paperId])
  @@index([paperVersionId])
  @@index([createdAt])
  @@index([updatedAt])
}

model SubmittedSnapshotIntakeRecord {
  snapshotIntakeId         String   @id @default(uuid())
  schoolId                 String
  markingInvocationRequestId String
  submissionSnapshotId     String
  attemptId                String
  deliverySessionId        String
  paperId                  String
  paperVersionId           String
  variantId                String
  studentRef               String
  intakeStatus             String   @default("received")
  readinessStatus          String   @default("ready")
  readinessReasonCodesJson Json?
  safeIntakeSummary        String   @db.Text
  createdAt                DateTime @default(now())
  updatedAt                DateTime @updatedAt
  blockedAt                DateTime?

  @@unique([schoolId, submissionSnapshotId])
  @@index([schoolId])
  @@index([markingInvocationRequestId])
  @@index([submissionSnapshotId])
  @@index([attemptId])
  @@index([deliverySessionId])
  @@index([studentRef])
  @@index([intakeStatus])
  @@index([readinessStatus])
  @@index([createdAt])
}

model MarkingBatchRecord {
  markingBatchId            String   @id @default(uuid())
  schoolId                  String
  markingInvocationRequestId String
  markingRunId              String
  batchStatus               String   @default("planned")
  batchMode                 String   @default("deterministic_plus_teacher_review")
  batchSequence             Int      @default(1)
  totalItems                Int      @default(0)
  deterministicItemCount    Int      @default(0)
  teacherReviewItemCount    Int      @default(0)
  blockedItemCount          Int      @default(0)
  safeBatchSummary          String   @db.Text
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt
  startedAt                 DateTime?
  completedAt               DateTime?

  @@index([schoolId])
  @@index([markingInvocationRequestId])
  @@index([markingRunId])
  @@index([batchStatus])
  @@index([batchMode])
  @@index([createdAt])
}

model MarkingBatchItemRecord {
  markingBatchItemId        String   @id @default(uuid())
  schoolId                  String
  markingBatchId            String
  snapshotIntakeId          String
  submissionSnapshotId      String
  attemptId                 String
  attemptQuestionSnapshotId String
  answerSubmissionId        String
  questionId                String
  questionVersionId         String
  paperQuestionId           String
  variantQuestionId         String
  studentRef                String
  itemStatus                String   @default("planned")
  itemMode                  String   @default("deterministic")
  marksAvailable            Float    @default(0)
  safeItemSummary           String   @db.Text
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt
  completedAt               DateTime?

  @@index([schoolId])
  @@index([markingBatchId])
  @@index([snapshotIntakeId])
  @@index([submissionSnapshotId])
  @@index([attemptId])
  @@index([answerSubmissionId])
  @@index([questionVersionId])
  @@index([itemStatus])
  @@index([itemMode])
  @@index([createdAt])
}

model MarkingResultLinkRecord {
  markingResultLinkId    String   @id @default(uuid())
  schoolId               String
  markingInvocationRequestId String
  markingBatchId         String
  markingBatchItemId     String
  markingRunId           String
  markingResultVersionId String
  submissionSnapshotId   String
  attemptId              String
  answerSubmissionId     String
  linkStatus             String   @default("linked")
  safeLinkSummary        String   @db.Text
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt

  @@index([schoolId])
  @@index([markingInvocationRequestId])
  @@index([markingBatchId])
  @@index([markingBatchItemId])
  @@index([markingRunId])
  @@index([markingResultVersionId])
  @@index([submissionSnapshotId])
  @@index([attemptId])
  @@index([createdAt])
}

model MarkingDispatchAuditRecord {
  markingDispatchAuditId String   @id @default(uuid())
  schoolId               String
  markingInvocationRequestId String?
  markingBatchId         String?
  markingBatchItemId     String?
  markingRunId           String?
  actorId                String
  actorRole              String
  eventType              String
  decision               String
  safeSummary            String   @db.Text
  reasonCodesJson        Json?
  metadataJson           Json?
  requestId              String?
  correlationId          String?
  createdAt              DateTime @default(now())

  @@index([schoolId])
  @@index([markingInvocationRequestId])
  @@index([markingBatchId])
  @@index([markingBatchItemId])
  @@index([markingRunId])
  @@index([actorId])
  @@index([eventType])
  @@index([createdAt])
  @@index([requestId])
  @@index([correlationId])
}

model MarkingInvocationIdempotencyRecord {
  markingInvocationIdempotencyId String   @id @default(uuid())
  schoolId                       String
  operation                      String
  idempotencyKey                 String
  requestHash                    String
  status                         String   @default("in_progress")
  resourceType                   String?
  resourceId                     String?
  safeResultSummary              String?
  createdAt                      DateTime @default(now())
  updatedAt                      DateTime @updatedAt
  expiresAt                      DateTime?

  @@unique([schoolId, operation, idempotencyKey])
  @@index([schoolId])
  @@index([operation])
  @@index([idempotencyKey])
  @@index([status])
  @@index([expiresAt])
  @@index([createdAt])
}

model MarkingReadinessCheckRecord {
  markingReadinessCheckId String   @id @default(uuid())
  schoolId                String
  markingInvocationRequestId String?
  submissionSnapshotId    String?
  markingBatchId          String?
  checkType               String
  checkStatus             String   @default("passed")
  reasonCodesJson         Json?
  safeCheckSummary        String   @db.Text
  createdAt               DateTime @default(now())

  @@index([schoolId])
  @@index([markingInvocationRequestId])
  @@index([submissionSnapshotId])
  @@index([markingBatchId])
  @@index([checkType])
  @@index([checkStatus])
  @@index([createdAt])
}

// ─── Package 9: Result Finalization Governance, Release Readiness, Regrade Foundation ──

model ResultFinalizationReviewRecord {
  resultFinalizationReviewId String   @id @default(uuid())
  schoolId                   String
  markingInvocationRequestId String?
  markingRunId               String?
  deliverySessionId          String?
  paperId                    String?
  paperVersionId             String?
  reviewStatus               String   @default("draft")
  reviewMode                 String   @default("teacher_reviewed")
  reviewedResultVersionRefsJson Json?
  requiredCheckRefsJson      Json?
  safeReviewSummary          String   @db.Text
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  completedAt                DateTime?
  blockedAt                  DateTime?

  @@index([schoolId])
  @@index([markingInvocationRequestId])
  @@index([markingRunId])
  @@index([deliverySessionId])
  @@index([paperId])
  @@index([paperVersionId])
  @@index([reviewStatus])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultFinalizationDecisionRecord {
  resultFinalizationDecisionId String   @id @default(uuid())
  schoolId                     String
  resultFinalizationReviewId   String
  markingInvocationRequestId   String?
  markingRunId                 String?
  decisionStatus               String   @default("approved_for_finalization")
  decisionType                 String   @default("teacher_finalization")
  decidedByActorId             String
  decidedByRole                String
  safeDecisionSummary          String   @db.Text
  reasonCodesJson              Json?
  affectedResultVersionRefsJson Json?
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId])
  @@index([resultFinalizationReviewId])
  @@index([markingInvocationRequestId])
  @@index([markingRunId])
  @@index([decisionStatus])
  @@index([decidedByActorId])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultReleaseReadinessRecord {
  resultReleaseReadinessId   String   @id @default(uuid())
  schoolId                   String
  resultFinalizationDecisionId String
  resultFinalizationReviewId String?
  markingInvocationRequestId String?
  releaseReadinessStatus     String   @default("not_ready")
  releaseAudienceType        String   @default("internal_school")
  safeReadinessSummary       String   @db.Text
  blockingReasonCodesJson    Json?
  allowedChannelRefsJson     Json?
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  expiresAt                  DateTime?

  @@index([schoolId])
  @@index([resultFinalizationDecisionId])
  @@index([resultFinalizationReviewId])
  @@index([markingInvocationRequestId])
  @@index([releaseReadinessStatus])
  @@index([releaseAudienceType])
  @@index([createdAt])
  @@index([expiresAt])
}

model ResultReleaseBoundaryRecord {
  resultReleaseBoundaryId  String   @id @default(uuid())
  schoolId                 String
  resultReleaseReadinessId String
  resultFinalizationDecisionId String?
  audienceType             String   @default("student")
  boundaryStatus           String   @default("draft")
  allowedFieldsJson        Json?
  blockedFieldsJson        Json?
  redactionRulesJson       Json?
  safeBoundarySummary      String   @db.Text
  createdByActorId         String
  createdByRole            String
  createdAt                DateTime @default(now())
  updatedAt                DateTime @updatedAt
  voidedAt                 DateTime?

  @@index([schoolId])
  @@index([resultReleaseReadinessId])
  @@index([resultFinalizationDecisionId])
  @@index([audienceType])
  @@index([boundaryStatus])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultRegradeRequestRecord {
  resultRegradeRequestId String   @id @default(uuid())
  schoolId               String
  resultFinalizationDecisionId String?
  markingResultVersionId String
  markingRunId           String?
  studentRef             String
  requesterActorId       String
  requesterRole          String
  requestStatus          String   @default("submitted")
  requestType            String   @default("student_challenge_escalation")
  safeRequestSummary     String   @db.Text
  reasonCodesJson        Json?
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt
  cancelledAt            DateTime?
  resolvedAt             DateTime?

  @@index([schoolId])
  @@index([resultFinalizationDecisionId])
  @@index([markingResultVersionId])
  @@index([markingRunId])
  @@index([studentRef])
  @@index([requesterActorId])
  @@index([requestStatus])
  @@index([requestType])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultRegradeIntakeRecord {
  resultRegradeIntakeId     String   @id @default(uuid())
  schoolId                  String
  resultRegradeRequestId    String
  intakeStatus              String   @default("received")
  assignedReviewerActorId   String?
  assignedReviewerRole      String?
  safeIntakeSummary         String   @db.Text
  triageReasonCodesJson     Json?
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt
  completedAt               DateTime?
  blockedAt                 DateTime?

  @@index([schoolId])
  @@index([resultRegradeRequestId])
  @@index([intakeStatus])
  @@index([assignedReviewerActorId])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultGovernanceAuditRecord {
  resultGovernanceAuditId  String   @id @default(uuid())
  schoolId                 String
  resultFinalizationReviewId String?
  resultFinalizationDecisionId String?
  resultReleaseReadinessId String?
  resultReleaseBoundaryId  String?
  resultRegradeRequestId   String?
  actorId                  String
  actorRole                String
  eventType                String
  decision                 String
  safeSummary              String   @db.Text
  reasonCodesJson          Json?
  metadataJson             Json?
  requestId                String?
  correlationId            String?
  createdAt                DateTime @default(now())

  @@index([schoolId])
  @@index([resultFinalizationReviewId])
  @@index([resultFinalizationDecisionId])
  @@index([resultReleaseReadinessId])
  @@index([resultReleaseBoundaryId])
  @@index([resultRegradeRequestId])
  @@index([actorId])
  @@index([eventType])
  @@index([createdAt])
  @@index([requestId])
  @@index([correlationId])
}

model ResultGovernanceIdempotencyRecord {
  resultGovernanceIdempotencyId String   @id @default(uuid())
  schoolId                       String
  operation                      String
  idempotencyKey                 String
  requestHash                    String
  status                         String   @default("in_progress")
  resourceType                   String?
  resourceId                     String?
  safeResultSummary              String?
  createdAt                      DateTime @default(now())
  updatedAt                      DateTime @updatedAt
  expiresAt                      DateTime?

  @@unique([schoolId, operation, idempotencyKey])
  @@index([schoolId])
  @@index([operation])
  @@index([idempotencyKey])
  @@index([status])
  @@index([expiresAt])
  @@index([createdAt])
}

model ResultLearningEvidenceBridgeRecord {
  resultLearningEvidenceBridgeId String   @id @default(uuid())
  schoolId                       String
  resultFinalizationDecisionId   String
  resultReleaseReadinessId       String
  markingRunId                   String?
  markingResultVersionId         String
  studentRef                     String
  paperId                        String?
  paperVersionId                 String?
  deliverySessionId              String?
  bridgeStatus                   String   @default("draft")
  bridgeMode                     String   @default("teacher_approved_result")
  sourceRefsJson                 Json?
  safeEvidenceSummary            String   @db.Text
  createdByActorId               String
  createdByRole                  String
  createdAt                      DateTime @default(now())
  updatedAt                      DateTime @updatedAt
  completedAt                    DateTime?
  blockedAt                      DateTime?
  cancelledAt                    DateTime?

  @@index([schoolId])
  @@index([resultFinalizationDecisionId])
  @@index([resultReleaseReadinessId])
  @@index([markingRunId])
  @@index([markingResultVersionId])
  @@index([studentRef])
  @@index([paperId])
  @@index([paperVersionId])
  @@index([deliverySessionId])
  @@index([bridgeStatus])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultMasteryMutationPlanRecord {
  resultMasteryMutationPlanId     String   @id @default(uuid())
  schoolId                        String
  resultLearningEvidenceBridgeId String
  resultFinalizationDecisionId   String
  markingResultVersionId         String
  studentRef                     String
  planStatus                     String   @default("draft")
  planMode                       String   @default("objective_level_mastery")
  objectiveImpactRefsJson        Json?
  targetMasterySnapshotRefsJson  Json?
  safePlanSummary                String   @db.Text
  approvalRequired               Boolean  @default(true)
  approvedByActorId              String?
  approvedByRole                 String?
  createdByActorId               String
  createdByRole                  String
  createdAt                      DateTime @default(now())
  updatedAt                      DateTime @updatedAt
  approvedAt                     DateTime?
  blockedAt                      DateTime?
  cancelledAt                    DateTime?

  @@index([schoolId])
  @@index([resultLearningEvidenceBridgeId])
  @@index([resultFinalizationDecisionId])
  @@index([markingResultVersionId])
  @@index([studentRef])
  @@index([planStatus])
  @@index([planMode])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultMasteryMutationEventRecord {
  resultMasteryMutationEventId String   @id @default(uuid())
  schoolId                     String
  resultMasteryMutationPlanId String
  resultLearningEvidenceBridgeId String
  studentRef                   String
  targetSnapshotRef            String?
  mutationStatus               String   @default("planned")
  mutationType                 String   @default("objective_mastery_update")
  beforeStateJson              Json?
  afterStateJson               Json?
  deltaJson                    Json?
  safeMutationSummary          String   @db.Text
  appliedByActorId             String?
  appliedByRole                String?
  createdAt                    DateTime @default(now())
  appliedAt                    DateTime?
  voidedAt                     DateTime?

  @@index([schoolId])
  @@index([resultMasteryMutationPlanId])
  @@index([resultLearningEvidenceBridgeId])
  @@index([studentRef])
  @@index([targetSnapshotRef])
  @@index([mutationStatus])
  @@index([mutationType])
  @@index([createdAt])
  @@index([appliedAt])
}

model ResultObjectiveMasteryImpactRecord {
  resultObjectiveMasteryImpactId String   @id @default(uuid())
  schoolId                       String
  resultLearningEvidenceBridgeId String
  resultMasteryMutationPlanId    String
  studentRef                     String
  learningObjectiveId            String
  questionVersionId              String?
  markingResultVersionId         String
  impactStatus                   String   @default("draft")
  impactType                     String   @default("correct_evidence")
  evidenceStrength               String   @default("moderate")
  masteryDelta                   String   @default("no_change")
  confidenceLevel                String   @default("moderate")
  safeImpactSummary              String   @db.Text
  sourceRefsJson                 Json?
  createdAt                      DateTime @default(now())
  updatedAt                      DateTime @updatedAt
  voidedAt                       DateTime?

  @@index([schoolId])
  @@index([resultLearningEvidenceBridgeId])
  @@index([resultMasteryMutationPlanId])
  @@index([studentRef])
  @@index([learningObjectiveId])
  @@index([questionVersionId])
  @@index([markingResultVersionId])
  @@index([impactStatus])
  @@index([impactType])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultRevisionSignalRecord {
  resultRevisionSignalId        String   @id @default(uuid())
  schoolId                      String
  resultLearningEvidenceBridgeId String
  resultMasteryMutationPlanId   String
  studentRef                    String
  learningObjectiveId           String
  signalStatus                  String   @default("draft")
  signalType                    String   @default("revise_objective")
  priority                      Int      @default(0)
  safeSignalSummary             String   @db.Text
  recommendedActionRefsJson     Json?
  sourceRefsJson                Json?
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt
  dispatchedAt                  DateTime?
  blockedAt                     DateTime?
  voidedAt                      DateTime?

  @@index([schoolId])
  @@index([resultLearningEvidenceBridgeId])
  @@index([resultMasteryMutationPlanId])
  @@index([studentRef])
  @@index([learningObjectiveId])
  @@index([signalStatus])
  @@index([signalType])
  @@index([priority])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultGrowthSignalRecord {
  resultGrowthSignalId          String   @id @default(uuid())
  schoolId                      String
  resultLearningEvidenceBridgeId String
  resultMasteryMutationPlanId   String
  studentRef                    String
  learningObjectiveId           String
  signalStatus                  String   @default("draft")
  signalType                    String   @default("evidence_only_no_action")
  safeGrowthSummary             String   @db.Text
  growthMetricRefsJson          Json?
  sourceRefsJson                Json?
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt
  dispatchedAt                  DateTime?
  blockedAt                     DateTime?
  voidedAt                      DateTime?

  @@index([schoolId])
  @@index([resultLearningEvidenceBridgeId])
  @@index([resultMasteryMutationPlanId])
  @@index([studentRef])
  @@index([learningObjectiveId])
  @@index([signalStatus])
  @@index([signalType])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultLearningEvidenceAuditRecord {
  resultLearningEvidenceAuditId String   @id @default(uuid())
  schoolId                      String
  resultLearningEvidenceBridgeId String?
  resultMasteryMutationPlanId   String?
  resultMasteryMutationEventId  String?
  resultObjectiveMasteryImpactId String?
  resultRevisionSignalId        String?
  resultGrowthSignalId          String?
  actorId                       String
  actorRole                     String
  eventType                     String
  decision                      String
  safeSummary                   String   @db.Text
  reasonCodesJson               Json?
  metadataJson                  Json?
  requestId                     String?
  correlationId                 String?
  createdAt                     DateTime @default(now())

  @@index([schoolId])
  @@index([resultLearningEvidenceBridgeId])
  @@index([resultMasteryMutationPlanId])
  @@index([resultMasteryMutationEventId])
  @@index([resultObjectiveMasteryImpactId])
  @@index([resultRevisionSignalId])
  @@index([resultGrowthSignalId])
  @@index([actorId])
  @@index([eventType])
  @@index([createdAt])
  @@index([requestId])
  @@index([correlationId])
}

model ResultLearningEvidenceIdempotencyRecord {
  resultLearningEvidenceIdempotencyId String   @id @default(uuid())
  schoolId                             String
  operation                            String
  idempotencyKey                       String
  requestHash                          String
  status                               String   @default("in_progress")
  resourceType                         String?
  resourceId                           String?
  safeResultSummary                    String?
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  expiresAt                            DateTime?

  @@unique([schoolId, operation, idempotencyKey])
  @@index([schoolId])
  @@index([operation])
  @@index([idempotencyKey])
  @@index([status])
  @@index([expiresAt])
  @@index([createdAt])
}

// ── Package 11: Parent-Safe Result Release, Student Report Snapshots, Audience Boundary Enforcement ──

model ResultReleasePacketRecord {
  resultReleasePacketId              String   @id @default(uuid())
  schoolId                           String
  resultFinalizationDecisionId       String
  resultReleaseReadinessId           String
  resultReleaseBoundaryId            String
  resultLearningEvidenceBridgeId     String?
  markingResultVersionId             String
  studentRef                         String
  paperId                            String?
  paperVersionId                     String?
  deliverySessionId                  String?
  packetStatus                       String   @default("draft")
  packetAudience                     String   @default("student")
  packetMode                         String   @default("student_safe_result")
  safePacketSummary                  String   @db.Text
  allowedFieldsJson                  Json?
  blockedFieldsJson                  Json?
  sourceRefsJson                     Json?
  createdByActorId                   String
  createdByRole                      String
  createdAt                          DateTime @default(now())
  updatedAt                          DateTime @updatedAt
  approvedAt                         DateTime?
  blockedAt                          DateTime?
  cancelledAt                        DateTime?
  voidedAt                           DateTime?

  @@index([schoolId])
  @@index([resultFinalizationDecisionId])
  @@index([resultReleaseReadinessId])
  @@index([resultReleaseBoundaryId])
  @@index([resultLearningEvidenceBridgeId])
  @@index([markingResultVersionId])
  @@index([studentRef])
  @@index([packetStatus])
  @@index([packetAudience])
  @@index([packetMode])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultReleaseApprovalRecord {
  resultReleaseApprovalId    String   @id @default(uuid())
  schoolId                   String
  resultReleasePacketId      String
  resultFinalizationDecisionId String
  studentRef                 String
  approvalStatus             String   @default("draft")
  approvalType               String   @default("teacher_release_approval")
  approvedAudience           String   @default("student")
  approvedByActorId          String
  approvedByRole             String
  safeApprovalSummary        String   @db.Text
  reasonCodesJson            Json?
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  approvedAt                 DateTime?
  rejectedAt                 DateTime?
  voidedAt                   DateTime?

  @@index([schoolId])
  @@index([resultReleasePacketId])
  @@index([resultFinalizationDecisionId])
  @@index([studentRef])
  @@index([approvalStatus])
  @@index([approvalType])
  @@index([approvedByActorId])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultAudienceProjectionRecord {
  resultAudienceProjectionId String   @id @default(uuid())
  schoolId                   String
  resultReleasePacketId      String
  studentRef                 String
  audienceType               String   @default("student")
  projectionStatus           String   @default("draft")
  projectionVersion          Int      @default(1)
  safeProjectionJson         Json?
  allowedFieldsJson          Json?
  blockedFieldsJson          Json?
  redactionRulesJson         Json?
  safeProjectionSummary      String   @db.Text
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  voidedAt                   DateTime?

  @@index([schoolId])
  @@index([resultReleasePacketId])
  @@index([studentRef])
  @@index([audienceType])
  @@index([projectionStatus])
  @@index([createdAt])
  @@index([updatedAt])
}

model StudentResultReportSnapshotRecord {
  studentResultReportSnapshotId String   @id @default(uuid())
  schoolId                      String
  resultReleasePacketId         String
  resultAudienceProjectionId    String
  studentRef                    String
  snapshotStatus                String   @default("draft")
  snapshotType                  String   @default("student_safe_exam_result")
  safeReportTitle               String
  safeReportSummary             String   @db.Text
  safeStrengthsJson             Json?
  safeGrowthAreasJson           Json?
  safeNextStepsJson             Json?
  safeSupportGuidanceJson       Json?
  sourceRefsJson                Json?
  createdByActorId              String
  createdByRole                 String
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt
  approvedAt                    DateTime?
  voidedAt                      DateTime?

  @@index([schoolId])
  @@index([resultReleasePacketId])
  @@index([resultAudienceProjectionId])
  @@index([studentRef])
  @@index([snapshotStatus])
  @@index([snapshotType])
  @@index([createdAt])
  @@index([updatedAt])
}

model ParentSafeResultSummaryRecord {
  parentSafeResultSummaryId   String   @id @default(uuid())
  schoolId                    String
  resultReleasePacketId       String
  resultAudienceProjectionId  String
  studentRef                  String
  summaryStatus               String   @default("draft")
  safeProgressSummary         String   @db.Text
  safeSupportSummary          String   @db.Text
  safeStrengthsJson           Json?
  safeGrowthAreasJson         Json?
  safeRecommendedSupportJson  Json?
  notYetReleasedReason        String?
  allowedFieldNamesJson       Json?
  blockedFieldNamesJson       Json?
  createdByActorId            String
  createdByRole               String
  createdAt                   DateTime @default(now())
  updatedAt                   DateTime @updatedAt
  approvedAt                  DateTime?
  voidedAt                    DateTime?

  @@index([schoolId])
  @@index([resultReleasePacketId])
  @@index([resultAudienceProjectionId])
  @@index([studentRef])
  @@index([summaryStatus])
  @@index([createdAt])
  @@index([updatedAt])
}

model StudentSafeResultSummaryRecord {
  studentSafeResultSummaryId   String   @id @default(uuid())
  schoolId                     String
  resultReleasePacketId        String
  resultAudienceProjectionId   String
  studentRef                   String
  summaryStatus                String   @default("draft")
  safeAchievementSummary       String   @db.Text
  safeLearningProgressSummary  String   @db.Text
  safeNextPracticeSummary      String   @db.Text
  safeConfidenceGuidanceJson   Json?
  safeRevisionGuidanceJson     Json?
  allowedFieldNamesJson        Json?
  blockedFieldNamesJson        Json?
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  approvedAt                   DateTime?
  voidedAt                     DateTime?

  @@index([schoolId])
  @@index([resultReleasePacketId])
  @@index([resultAudienceProjectionId])
  @@index([studentRef])
  @@index([summaryStatus])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultReleaseDeliveryIntentRecord {
  resultReleaseDeliveryIntentId String   @id @default(uuid())
  schoolId                      String
  resultReleasePacketId         String
  resultReleaseApprovalId       String
  studentRef                    String
  audienceType                  String   @default("student")
  deliveryChannel               String   @default("student_portal_future")
  intentStatus                  String   @default("draft")
  safeIntentSummary             String   @db.Text
  blockedReasonCodesJson        Json?
  createdByActorId              String
  createdByRole                 String
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt
  approvedAt                    DateTime?
  blockedAt                     DateTime?
  voidedAt                      DateTime?

  @@index([schoolId])
  @@index([resultReleasePacketId])
  @@index([resultReleaseApprovalId])
  @@index([studentRef])
  @@index([audienceType])
  @@index([deliveryChannel])
  @@index([intentStatus])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultReleaseAuditRecord {
  resultReleaseAuditId             String   @id @default(uuid())
  schoolId                         String
  resultReleasePacketId            String?
  resultReleaseApprovalId          String?
  resultAudienceProjectionId       String?
  studentResultReportSnapshotId    String?
  parentSafeResultSummaryId        String?
  studentSafeResultSummaryId       String?
  resultReleaseDeliveryIntentId    String?
  actorId                          String
  actorRole                        String
  eventType                        String
  decision                         String
  safeSummary                      String   @db.Text
  reasonCodesJson                  Json?
  metadataJson                     Json?
  requestId                        String?
  correlationId                    String?
  createdAt                        DateTime @default(now())

  @@index([schoolId])
  @@index([resultReleasePacketId])
  @@index([resultReleaseApprovalId])
  @@index([resultAudienceProjectionId])
  @@index([studentResultReportSnapshotId])
  @@index([parentSafeResultSummaryId])
  @@index([studentSafeResultSummaryId])
  @@index([resultReleaseDeliveryIntentId])
  @@index([actorId])
  @@index([eventType])
  @@index([createdAt])
  @@index([requestId])
  @@index([correlationId])
}

model ResultReleaseIdempotencyRecord {
  resultReleaseIdempotencyId String   @id @default(uuid())
  schoolId                   String
  operation                  String
  idempotencyKey             String
  requestHash                String
  status                     String   @default("in_progress")
  resourceType               String?
  resourceId                 String?
  safeResultSummary          String?
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  expiresAt                  DateTime?

  @@unique([schoolId, operation, idempotencyKey])
  @@index([schoolId])
  @@index([operation])
  @@index([idempotencyKey])
  @@index([status])
  @@index([expiresAt])
  @@index([createdAt])
}

// ── Package 12: Controlled Result Delivery Adapter, Mock Channel Outbox, Recipient Boundary Resolution, Dry-Run Delivery Receipts ──

model ResultDeliveryJobRecord {
  resultDeliveryJobId              String   @id @default(uuid())
  schoolId                         String
  resultReleaseDeliveryIntentId    String
  resultReleasePacketId            String
  resultReleaseApprovalId          String
  resultAudienceProjectionId       String
  studentRef                       String
  audienceType                     String   @default("student")
  deliveryChannel                  String   @default("student_portal_mock")
  jobStatus                        String   @default("draft")
  jobMode                          String   @default("dry_run_only")
  safeJobSummary                   String   @db.Text
  sourceRefsJson                   Json?
  allowedFieldsJson                Json?
  blockedFieldsJson                Json?
  createdByActorId                 String
  createdByRole                    String
  createdAt                        DateTime @default(now())
  updatedAt                        DateTime @updatedAt
  validatedAt                      DateTime?
  queuedAt                         DateTime?
  completedAt                      DateTime?
  blockedAt                        DateTime?
  cancelledAt                      DateTime?
  voidedAt                         DateTime?

  @@index([schoolId])
  @@index([resultReleaseDeliveryIntentId])
  @@index([resultReleasePacketId])
  @@index([resultReleaseApprovalId])
  @@index([resultAudienceProjectionId])
  @@index([studentRef])
  @@index([audienceType])
  @@index([deliveryChannel])
  @@index([jobStatus])
  @@index([jobMode])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultDeliveryRecipientRecord {
  resultDeliveryRecipientId   String   @id @default(uuid())
  schoolId                    String
  resultDeliveryJobId         String
  studentRef                  String
  audienceType                String   @default("student")
  recipientScope              String   @default("student_self")
  recipientStatus             String   @default("draft")
  recipientRefHash            String
  recipientDisplayLabel       String
  relationshipToStudent       String   @default("self")
  resolutionSource            String   @default("system")
  safeRecipientSummary        String   @db.Text
  blockedReasonCodesJson      Json?
  createdByActorId            String
  createdByRole               String
  createdAt                   DateTime @default(now())
  updatedAt                   DateTime @updatedAt
  verifiedAt                  DateTime?
  blockedAt                   DateTime?
  voidedAt                    DateTime?

  @@index([schoolId])
  @@index([resultDeliveryJobId])
  @@index([studentRef])
  @@index([audienceType])
  @@index([recipientScope])
  @@index([recipientStatus])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultDeliveryChannelEnvelopeRecord {
  resultDeliveryChannelEnvelopeId String   @id @default(uuid())
  schoolId                         String
  resultDeliveryJobId              String
  resultDeliveryRecipientId        String
  resultAudienceProjectionId       String
  envelopeStatus                   String   @default("draft")
  audienceType                     String   @default("student")
  deliveryChannel                  String   @default("student_portal_mock")
  safeSubject                      String
  safePreview                      String
  safeBodyJson                     Json?
  allowedFieldNamesJson            Json?
  blockedFieldNamesJson            Json?
  redactionRulesJson               Json?
  sourceRefsJson                   Json?
  createdByActorId                 String
  createdByRole                    String
  createdAt                        DateTime @default(now())
  updatedAt                        DateTime @updatedAt
  sealedAt                         DateTime?
  blockedAt                        DateTime?
  voidedAt                         DateTime?

  @@index([schoolId])
  @@index([resultDeliveryJobId])
  @@index([resultDeliveryRecipientId])
  @@index([resultAudienceProjectionId])
  @@index([audienceType])
  @@index([deliveryChannel])
  @@index([envelopeStatus])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultDeliverySuppressionRecord {
  resultDeliverySuppressionId     String   @id @default(uuid())
  schoolId                        String
  resultDeliveryJobId             String
  resultDeliveryRecipientId       String?
  resultDeliveryChannelEnvelopeId String?
  suppressionStatus               String   @default("active")
  suppressionType                 String   @default("privacy_boundary")
  suppressionReasonCode           String
  safeSuppressionSummary          String   @db.Text
  sourceRefsJson                  Json?
  createdByActorId                String
  createdByRole                   String
  createdAt                       DateTime @default(now())
  updatedAt                       DateTime @updatedAt
  clearedAt                       DateTime?
  voidedAt                        DateTime?

  @@index([schoolId])
  @@index([resultDeliveryJobId])
  @@index([resultDeliveryRecipientId])
  @@index([resultDeliveryChannelEnvelopeId])
  @@index([suppressionStatus])
  @@index([suppressionType])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultDeliveryAttemptRecord {
  resultDeliveryAttemptId        String   @id @default(uuid())
  schoolId                       String
  resultDeliveryJobId            String
  resultDeliveryRecipientId      String
  resultDeliveryChannelEnvelopeId String
  deliveryChannel                String   @default("student_portal_mock")
  attemptStatus                  String   @default("created")
  attemptMode                    String   @default("dry_run_only")
  mockProviderName               String
  safeAttemptSummary             String   @db.Text
  attemptNumber                  Int      @default(1)
  blockedReasonCodesJson         Json?
  createdByActorId               String
  createdByRole                  String
  createdAt                      DateTime @default(now())
  updatedAt                      DateTime @updatedAt
  startedAt                      DateTime?
  completedAt                    DateTime?
  failedAt                       DateTime?
  blockedAt                      DateTime?
  voidedAt                       DateTime?

  @@index([schoolId])
  @@index([resultDeliveryJobId])
  @@index([resultDeliveryRecipientId])
  @@index([resultDeliveryChannelEnvelopeId])
  @@index([deliveryChannel])
  @@index([attemptStatus])
  @@index([attemptMode])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultDeliveryReceiptRecord {
  resultDeliveryReceiptId        String   @id @default(uuid())
  schoolId                       String
  resultDeliveryAttemptId        String
  resultDeliveryJobId            String
  resultDeliveryRecipientId      String
  resultDeliveryChannelEnvelopeId String
  receiptStatus                  String   @default("created")
  receiptType                    String   @default("dry_run_preview")
  safeReceiptSummary             String   @db.Text
  providerSimulationJson         Json?
  blockedReasonCodesJson         Json?
  createdByActorId               String
  createdByRole                  String
  createdAt                      DateTime @default(now())
  updatedAt                      DateTime @updatedAt
  voidedAt                       DateTime?

  @@index([schoolId])
  @@index([resultDeliveryAttemptId])
  @@index([resultDeliveryJobId])
  @@index([resultDeliveryRecipientId])
  @@index([resultDeliveryChannelEnvelopeId])
  @@index([receiptStatus])
  @@index([receiptType])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultDeliveryRetryPlanRecord {
  resultDeliveryRetryPlanId String   @id @default(uuid())
  schoolId                  String
  resultDeliveryJobId       String
  resultDeliveryAttemptId   String
  retryStatus               String   @default("draft")
  retryPolicy               String   @default("linear_backoff")
  nextMockRetryAt           DateTime?
  maxMockAttempts           Int      @default(3)
  attemptsUsed              Int      @default(0)
  safeRetrySummary          String   @db.Text
  blockedReasonCodesJson    Json?
  createdByActorId          String
  createdByRole             String
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt
  cancelledAt               DateTime?
  voidedAt                  DateTime?

  @@index([schoolId])
  @@index([resultDeliveryJobId])
  @@index([resultDeliveryAttemptId])
  @@index([retryStatus])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultDeliveryMockProviderRecord {
  resultDeliveryMockProviderId String   @id @default(uuid())
  schoolId                     String
  providerName                 String
  providerStatus               String   @default("active")
  supportedChannelsJson        Json?
  simulationMode               String   @default("always_success")
  safeProviderSummary          String   @db.Text
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  disabledAt                   DateTime?
  voidedAt                     DateTime?

  @@index([schoolId])
  @@index([providerName])
  @@index([providerStatus])
  @@index([simulationMode])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultDeliveryAuditRecord {
  resultDeliveryAuditId          String   @id @default(uuid())
  schoolId                       String
  resultDeliveryJobId            String?
  resultDeliveryRecipientId      String?
  resultDeliveryChannelEnvelopeId String?
  resultDeliveryAttemptId        String?
  resultDeliveryReceiptId        String?
  resultDeliverySuppressionId    String?
  resultDeliveryRetryPlanId      String?
  actorId                        String
  actorRole                      String
  eventType                      String
  decision                       String
  safeSummary                    String   @db.Text
  reasonCodesJson                Json?
  metadataJson                   Json?
  requestId                      String?
  correlationId                  String?
  createdAt                      DateTime @default(now())

  @@index([schoolId])
  @@index([resultDeliveryJobId])
  @@index([resultDeliveryRecipientId])
  @@index([resultDeliveryChannelEnvelopeId])
  @@index([resultDeliveryAttemptId])
  @@index([resultDeliveryReceiptId])
  @@index([resultDeliverySuppressionId])
  @@index([resultDeliveryRetryPlanId])
  @@index([actorId])
  @@index([eventType])
  @@index([createdAt])
  @@index([requestId])
  @@index([correlationId])
}

model ResultDeliveryIdempotencyRecord {
  resultDeliveryIdempotencyId String   @id @default(uuid())
  schoolId                    String
  operation                   String
  idempotencyKey              String
  requestHash                 String
  status                      String   @default("in_progress")
  resourceType                String?
  resourceId                  String?
  safeResultSummary           String?
  createdAt                   DateTime @default(now())
  updatedAt                   DateTime @updatedAt
  expiresAt                   DateTime?

  @@unique([schoolId, operation, idempotencyKey])
  @@index([schoolId])
  @@index([operation])
  @@index([idempotencyKey])
  @@index([status])
  @@index([expiresAt])
  @@index([createdAt])
}

model ResultReportCardTemplateRecord {
  resultReportCardTemplateId String   @id @default(uuid())
  schoolId                   String
  templateKey                String
  templateName               String
  templateStatus             String   @default("draft")
  templateAudience           String   @default("student")
  templatePurpose            String
  safeTemplateSummary        String   @db.Text
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  disabledAt                 DateTime?
  voidedAt                   DateTime?

  @@index([schoolId])
  @@index([templateKey])
  @@index([templateStatus])
  @@index([templateAudience])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultReportCardTemplateVersionRecord {
  resultReportCardTemplateVersionId String   @id @default(uuid())
  schoolId                          String
  resultReportCardTemplateId        String
  templateVersion                   String
  versionStatus                     String   @default("draft")
  layoutMode                        String   @default("exam_result_summary")
  sectionSchemaJson                 Json?
  allowedSectionTypesJson           Json?
  blockedFieldNamesJson             Json?
  redactionRulesJson                Json?
  safeVersionSummary                String   @db.Text
  createdByActorId                  String
  createdByRole                     String
  createdAt                         DateTime @default(now())
  updatedAt                         DateTime @updatedAt
  activatedAt                       DateTime?
  retiredAt                         DateTime?
  voidedAt                          DateTime?

  @@index([schoolId])
  @@index([resultReportCardTemplateId])
  @@index([templateVersion])
  @@index([versionStatus])
  @@index([layoutMode])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultReportCardAssemblyRecord {
  resultReportCardAssemblyId        String   @id @default(uuid())
  schoolId                          String
  resultReportCardTemplateId        String
  resultReportCardTemplateVersionId String
  resultReleasePacketId             String
  resultReleaseApprovalId           String
  resultAudienceProjectionId        String
  studentResultReportSnapshotId     String
  parentSafeResultSummaryId         String?
  studentSafeResultSummaryId        String?
  resultDeliveryJobId               String?
  resultDeliveryReceiptId           String?
  resultFinalizationDecisionId      String
  resultReleaseBoundaryId           String
  resultLearningEvidenceBridgeId    String?
  markingResultVersionId            String
  studentRef                        String
  paperId                           String
  paperVersionId                    String
  deliverySessionId                 String
  assemblyStatus                    String   @default("draft")
  assemblyMode                      String   @default("student_safe_report_card")
  audienceType                      String   @default("student")
  safeReportTitle                   String   @db.Text
  safeReportSummary                 String   @db.Text
  sourceRefsJson                    Json?
  allowedFieldsJson                 Json?
  blockedFieldsJson                 Json?
  createdByActorId                  String
  createdByRole                     String
  createdAt                         DateTime @default(now())
  updatedAt                         DateTime @updatedAt
  assembledAt                       DateTime?
  safetyCheckedAt                   DateTime?
  sealedAt                          DateTime?
  blockedAt                         DateTime?
  cancelledAt                       DateTime?
  voidedAt                          DateTime?

  @@index([schoolId])
  @@index([resultReportCardTemplateId])
  @@index([resultReleasePacketId])
  @@index([resultReleaseApprovalId])
  @@index([studentResultReportSnapshotId])
  @@index([studentRef])
  @@index([paperId])
  @@index([deliverySessionId])
  @@index([assemblyStatus])
  @@index([assemblyMode])
  @@index([audienceType])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultReportCardSectionRecord {
  resultReportCardSectionId String   @id @default(uuid())
  schoolId                  String
  resultReportCardAssemblyId String
  sectionKey                String
  sectionType               String
  sectionStatus             String   @default("draft")
  sectionOrder              Int      @default(0)
  safeHeading               String   @db.Text
  safeSummary               String   @db.Text
  safeBodyJson              Json?
  sourceRefsJson            Json?
  allowedFieldNamesJson     Json?
  blockedFieldNamesJson     Json?
  redactionRulesJson        Json?
  createdByActorId          String
  createdByRole             String
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt
  sealedAt                  DateTime?
  blockedAt                 DateTime?
  voidedAt                  DateTime?

  @@index([schoolId])
  @@index([resultReportCardAssemblyId])
  @@index([sectionKey])
  @@index([sectionType])
  @@index([sectionStatus])
  @@index([sectionOrder])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultReportCardEvidenceLinkRecord {
  resultReportCardEvidenceLinkId String   @id @default(uuid())
  schoolId                       String
  resultReportCardAssemblyId     String
  resultReportCardSectionId      String?
  sourceRecordType               String
  sourceRecordId                 String
  sourcePackage                  String
  evidenceStatus                 String   @default("active")
  evidenceUse                    String   @default("reference")
  safeEvidenceSummary            String   @db.Text
  allowedUseJson                 Json?
  blockedUseJson                 Json?
  createdByActorId               String
  createdByRole                  String
  createdAt                      DateTime @default(now())
  updatedAt                      DateTime @updatedAt
  voidedAt                       DateTime?

  @@index([schoolId])
  @@index([resultReportCardAssemblyId])
  @@index([resultReportCardSectionId])
  @@index([sourceRecordType])
  @@index([sourceRecordId])
  @@index([sourcePackage])
  @@index([evidenceStatus])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultReportCardAudienceProjectionRecord {
  resultReportCardAudienceProjectionId String   @id @default(uuid())
  schoolId                             String
  resultReportCardAssemblyId           String
  audienceType                         String
  projectionStatus                     String   @default("draft")
  projectionVersion                    String   @default("v1")
  safeProjectionJson                   Json?
  allowedFieldNamesJson                Json?
  blockedFieldNamesJson                Json?
  redactionRulesJson                   Json?
  safeProjectionSummary                String   @db.Text
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  sealedAt                             DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId])
  @@index([resultReportCardAssemblyId])
  @@index([audienceType])
  @@index([projectionStatus])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultReportCardReviewRecord {
  resultReportCardReviewId            String   @id @default(uuid())
  schoolId                            String
  resultReportCardAssemblyId          String
  resultReportCardAudienceProjectionId String?
  reviewStatus                        String   @default("draft")
  reviewType                          String   @default("teacher_report_review")
  reviewDecision                      String   @default("pending")
  reviewedByActorId                   String
  reviewedByRole                      String
  safeReviewSummary                   String   @db.Text
  reasonCodesJson                     Json?
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  approvedAt                          DateTime?
  rejectedAt                          DateTime?
  blockedAt                           DateTime?
  voidedAt                            DateTime?

  @@index([schoolId])
  @@index([resultReportCardAssemblyId])
  @@index([resultReportCardAudienceProjectionId])
  @@index([reviewStatus])
  @@index([reviewType])
  @@index([reviewDecision])
  @@index([reviewedByActorId])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultReportCardExportIntentRecord {
  resultReportCardExportIntentId  String   @id @default(uuid())
  schoolId                        String
  resultReportCardAssemblyId      String
  resultReportCardReviewId        String
  resultReportCardAudienceProjectionId String
  exportStatus                    String   @default("draft")
  exportChannel                   String   @default("pdf_export_future")
  exportMode                      String   @default("intent_only")
  safeExportIntentSummary         String   @db.Text
  blockedReasonCodesJson          Json?
  sourceRefsJson                  Json?
  createdByActorId                String
  createdByRole                   String
  createdAt                       DateTime @default(now())
  updatedAt                       DateTime @updatedAt
  eligibleAt                      DateTime?
  blockedAt                       DateTime?
  voidedAt                        DateTime?

  @@index([schoolId])
  @@index([resultReportCardAssemblyId])
  @@index([resultReportCardReviewId])
  @@index([resultReportCardAudienceProjectionId])
  @@index([exportStatus])
  @@index([exportChannel])
  @@index([exportMode])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultReportCardRenderManifestRecord {
  resultReportCardRenderManifestId String   @id @default(uuid())
  schoolId                         String
  resultReportCardAssemblyId       String
  resultReportCardTemplateVersionId String
  manifestStatus                   String   @default("draft")
  renderMode                       String   @default("preview_only")
  safeManifestSummary              String   @db.Text
  layoutJson                       Json?
  sectionOrderJson                 Json?
  assetRefsJson                    Json?
  blockedFieldNamesJson            Json?
  createdByActorId                 String
  createdByRole                    String
  createdAt                        DateTime @default(now())
  updatedAt                        DateTime @updatedAt
  sealedAt                         DateTime?
  blockedAt                        DateTime?
  voidedAt                         DateTime?

  @@index([schoolId])
  @@index([resultReportCardAssemblyId])
  @@index([resultReportCardTemplateVersionId])
  @@index([manifestStatus])
  @@index([renderMode])
  @@index([createdAt])
  @@index([updatedAt])
}

model ResultReportCardAuditRecord {
  resultReportCardAuditId            String   @id @default(uuid())
  schoolId                           String
  resultReportCardTemplateId         String?
  resultReportCardTemplateVersionId  String?
  resultReportCardAssemblyId         String?
  resultReportCardSectionId          String?
  resultReportCardEvidenceLinkId     String?
  resultReportCardAudienceProjectionId String?
  resultReportCardReviewId           String?
  resultReportCardExportIntentId     String?
  resultReportCardRenderManifestId   String?
  actorId                            String
  actorRole                          String
  eventType                          String
  decision                           String
  safeSummary                        String   @db.Text
  reasonCodesJson                    Json?
  metadataJson                       Json?
  requestId                          String?
  correlationId                      String?
  createdAt                          DateTime @default(now())

  @@index([schoolId])
  @@index([resultReportCardTemplateId])
  @@index([resultReportCardAssemblyId])
  @@index([resultReportCardSectionId])
  @@index([resultReportCardEvidenceLinkId])
  @@index([resultReportCardAudienceProjectionId])
  @@index([resultReportCardReviewId])
  @@index([resultReportCardExportIntentId])
  @@index([resultReportCardRenderManifestId])
  @@index([actorId])
  @@index([eventType])
  @@index([createdAt])
  @@index([requestId])
  @@index([correlationId])
}

model ResultReportCardIdempotencyRecord {
  resultReportCardIdempotencyId String   @id @default(uuid())
  schoolId                      String
  operation                     String
  idempotencyKey                String
  requestHash                   String
  status                        String   @default("in_progress")
  resourceType                  String?
  resourceId                    String?
  safeResultSummary             String?
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt
  expiresAt                     DateTime?

  @@unique([schoolId, operation, idempotencyKey])
  @@index([schoolId])
  @@index([operation])
  @@index([idempotencyKey])
  @@index([status])
  @@index([expiresAt])
  @@index([createdAt])
}

// ── Package 14: Controlled Report Card Export Adapter, Mock Export Jobs, Archive-Ready Manifests, Dry-Run Receipts, Suppression Rules, Retry Plans, and No-Live-Export Safety Gate ──

model ResultReportCardExportJobRecord {
  resultReportCardExportJobId           String    @id @default(uuid())
  schoolId                             String
  resultReportCardAssemblyId           String
  resultReportCardAudienceProjectionId String
  resultReportCardReviewId             String
  resultReportCardExportIntentId       String
  resultReportCardRenderManifestId     String
  resultReleasePacketId                String
  resultDeliveryReceiptId              String?
  studentRef                           String
  paperId                              String
  paperVersionId                       String
  deliverySessionId                    String
  exportJobStatus                      String    @default("draft")
  exportJobMode                        String    @default("mock_export_only")
  exportJobPurpose                     String
  safeExportJobSummary                 String    @db.Text
  sourceRefsJson                       Json?
  allowedChannelsJson                  Json?
  blockedChannelsJson                  Json?
  blockedReasonCodesJson               Json?
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime  @default(now())
  updatedAt                            DateTime  @updatedAt
  validatedAt                          DateTime?
  queuedAt                             DateTime?
  completedAt                          DateTime?
  blockedAt                            DateTime?
  cancelledAt                          DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "rc_export_job_school")
  @@index([resultReportCardAssemblyId], map: "rc_export_job_assembly")
  @@index([resultReportCardAudienceProjectionId], map: "rc_export_job_proj")
  @@index([resultReportCardReviewId], map: "rc_export_job_review")
  @@index([resultReportCardExportIntentId], map: "rc_export_job_intent")
  @@index([resultReportCardRenderManifestId], map: "rc_export_job_manifest")
  @@index([studentRef], map: "rc_export_job_student")
  @@index([exportJobStatus], map: "rc_export_job_status")
  @@index([exportJobMode], map: "rc_export_job_mode")
  @@index([createdAt], map: "rc_export_job_cat")
  @@index([updatedAt], map: "rc_export_job_uat")
}

model ResultReportCardExportTargetRecord {
  resultReportCardExportTargetId String   @id @default(uuid())
  schoolId                      String
  resultReportCardExportJobId   String
  targetType                    String
  targetStatus                  String   @default("draft")
  targetMode                    String
  audienceType                  String
  safeTargetSummary             String   @db.Text
  targetDescriptorJson          Json?
  blockedReasonCodesJson        Json?
  createdByActorId              String
  createdByRole                 String
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt
  validatedAt                   DateTime?
  blockedAt                     DateTime?
  voidedAt                      DateTime?

  @@index([schoolId], map: "rc_export_tgt_school")
  @@index([resultReportCardExportJobId], map: "rc_export_tgt_job")
  @@index([targetType], map: "rc_export_tgt_type")
  @@index([targetStatus], map: "rc_export_tgt_status")
  @@index([targetMode], map: "rc_export_tgt_mode")
  @@index([audienceType], map: "rc_export_tgt_aud")
  @@index([createdAt], map: "rc_export_tgt_cat")
  @@index([updatedAt], map: "rc_export_tgt_uat")
}

model ResultReportCardExportEnvelopeRecord {
  resultReportCardExportEnvelopeId         String    @id @default(uuid())
  schoolId                                 String
  resultReportCardExportJobId              String
  resultReportCardExportTargetId           String
  resultReportCardAudienceProjectionId     String
  resultReportCardRenderManifestId         String
  envelopeStatus                           String    @default("draft")
  envelopeMode                             String    @default("mock_payload_only")
  safeEnvelopeSummary                      String    @db.Text
  safePayloadJson                          Json?
  redactionRulesJson                       Json?
  allowedFieldNamesJson                    Json?
  blockedFieldNamesJson                    Json?
  blockedReasonCodesJson                   Json?
  createdByActorId                         String
  createdByRole                            String
  createdAt                                DateTime  @default(now())
  updatedAt                                DateTime  @updatedAt
  sealedAt                                 DateTime?
  blockedAt                                DateTime?
  voidedAt                                 DateTime?

  @@index([schoolId], map: "rc_export_env_school")
  @@index([resultReportCardExportJobId], map: "rc_export_env_job")
  @@index([resultReportCardExportTargetId], map: "rc_export_env_tgt")
  @@index([resultReportCardAudienceProjectionId], map: "rc_export_env_proj")
  @@index([resultReportCardRenderManifestId], map: "rc_export_env_manifest")
  @@index([envelopeStatus], map: "rc_export_env_status")
  @@index([envelopeMode], map: "rc_export_env_mode")
  @@index([createdAt], map: "rc_export_env_cat")
  @@index([updatedAt], map: "rc_export_env_uat")
}

model ResultReportCardMockExportAttemptRecord {
  resultReportCardMockExportAttemptId String    @id @default(uuid())
  schoolId                            String
  resultReportCardExportJobId         String
  resultReportCardExportTargetId      String
  resultReportCardExportEnvelopeId    String
  attemptStatus                       String    @default("created")
  attemptMode                         String    @default("dry_run_only")
  mockProviderName                    String
  targetType                          String
  attemptNumber                       Int       @default(1)
  safeAttemptSummary                  String    @db.Text
  providerSimulationJson              Json?
  blockedReasonCodesJson              Json?
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime  @default(now())
  updatedAt                           DateTime  @updatedAt
  startedAt                           DateTime?
  completedAt                         DateTime?
  failedAt                            DateTime?
  blockedAt                           DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "rc_export_attempt_school")
  @@index([resultReportCardExportJobId], map: "rc_export_attempt_job")
  @@index([resultReportCardExportTargetId], map: "rc_export_attempt_tgt")
  @@index([resultReportCardExportEnvelopeId], map: "rc_export_attempt_env")
  @@index([attemptStatus], map: "rc_export_attempt_status")
  @@index([attemptMode], map: "rc_export_attempt_mode")
  @@index([createdAt], map: "rc_export_attempt_cat")
  @@index([updatedAt], map: "rc_export_attempt_uat")
}

model ResultReportCardExportReceiptRecord {
  resultReportCardExportReceiptId       String   @id @default(uuid())
  schoolId                              String
  resultReportCardExportJobId           String
  resultReportCardExportTargetId        String
  resultReportCardExportEnvelopeId      String
  resultReportCardMockExportAttemptId   String
  receiptStatus                         String   @default("created")
  receiptType                           String   @default("dry_run_preview")
  safeReceiptSummary                    String   @db.Text
  providerSimulationJson                Json?
  blockedReasonCodesJson                Json?
  createdByActorId                      String
  createdByRole                         String
  createdAt                             DateTime @default(now())
  updatedAt                             DateTime @updatedAt
  voidedAt                              DateTime?

  @@index([schoolId], map: "rc_export_recpt_school")
  @@index([resultReportCardExportJobId], map: "rc_export_recpt_job")
  @@index([resultReportCardExportTargetId], map: "rc_export_recpt_tgt")
  @@index([resultReportCardExportEnvelopeId], map: "rc_export_recpt_env")
  @@index([resultReportCardMockExportAttemptId], map: "rc_export_recpt_attempt")
  @@index([receiptStatus], map: "rc_export_recpt_status")
  @@index([receiptType], map: "rc_export_recpt_type")
  @@index([createdAt], map: "rc_export_recpt_cat")
  @@index([updatedAt], map: "rc_export_recpt_uat")
}

model ResultReportCardExportSuppressionRecord {
  resultReportCardExportSuppressionId String    @id @default(uuid())
  schoolId                            String
  resultReportCardExportJobId         String
  resultReportCardExportTargetId      String?
  resultReportCardExportEnvelopeId    String?
  suppressionStatus                   String    @default("active")
  suppressionReason                   String
  suppressionScope                    String
  safeSuppressionSummary              String    @db.Text
  reasonCodesJson                     Json?
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime  @default(now())
  updatedAt                           DateTime  @updatedAt
  liftedAt                            DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "rc_export_supp_school")
  @@index([resultReportCardExportJobId], map: "rc_export_supp_job")
  @@index([resultReportCardExportTargetId], map: "rc_export_supp_tgt")
  @@index([resultReportCardExportEnvelopeId], map: "rc_export_supp_env")
  @@index([suppressionStatus], map: "rc_export_supp_status")
  @@index([suppressionReason], map: "rc_export_supp_reason")
  @@index([suppressionScope], map: "rc_export_supp_scope")
  @@index([createdAt], map: "rc_export_supp_cat")
  @@index([updatedAt], map: "rc_export_supp_uat")
}

model ResultReportCardExportRetryPlanRecord {
  resultReportCardExportRetryPlanId  String    @id @default(uuid())
  schoolId                           String
  resultReportCardExportJobId        String
  resultReportCardMockExportAttemptId String
  retryStatus                        String    @default("draft")
  retryPolicy                        String    @default("linear_backoff")
  nextMockRetryAt                    DateTime?
  maxMockAttempts                    Int       @default(3)
  attemptsUsed                       Int       @default(0)
  safeRetrySummary                   String    @db.Text
  blockedReasonCodesJson             Json?
  createdByActorId                   String
  createdByRole                      String
  createdAt                          DateTime  @default(now())
  updatedAt                          DateTime  @updatedAt
  cancelledAt                        DateTime?
  voidedAt                           DateTime?

  @@index([schoolId], map: "rc_export_retry_school")
  @@index([resultReportCardExportJobId], map: "rc_export_retry_job")
  @@index([resultReportCardMockExportAttemptId], map: "rc_export_retry_attempt")
  @@index([retryStatus], map: "rc_export_retry_status")
  @@index([createdAt], map: "rc_export_retry_cat")
  @@index([updatedAt], map: "rc_export_retry_uat")
}

model ResultReportCardArchiveManifestRecord {
  resultReportCardArchiveManifestId String    @id @default(uuid())
  schoolId                          String
  resultReportCardExportJobId       String
  resultReportCardExportEnvelopeId  String?
  resultReportCardExportReceiptId   String?
  manifestStatus                    String    @default("draft")
  manifestMode                      String    @default("metadata_only")
  safeArchiveSummary                String    @db.Text
  archiveMetadataJson               Json?
  retentionPolicyJson               Json?
  allowedFieldNamesJson             Json?
  blockedFieldNamesJson             Json?
  blockedReasonCodesJson            Json?
  createdByActorId                  String
  createdByRole                     String
  createdAt                         DateTime  @default(now())
  updatedAt                         DateTime  @updatedAt
  sealedAt                          DateTime?
  blockedAt                         DateTime?
  voidedAt                          DateTime?

  @@index([schoolId], map: "rc_export_arch_school")
  @@index([resultReportCardExportJobId], map: "rc_export_arch_job")
  @@index([resultReportCardExportEnvelopeId], map: "rc_export_arch_env")
  @@index([resultReportCardExportReceiptId], map: "rc_export_arch_recpt")
  @@index([manifestStatus], map: "rc_export_arch_status")
  @@index([manifestMode], map: "rc_export_arch_mode")
  @@index([createdAt], map: "rc_export_arch_cat")
  @@index([updatedAt], map: "rc_export_arch_uat")
}

model ResultReportCardExportAuditRecord {
  resultReportCardExportAuditId         String   @id @default(uuid())
  schoolId                              String
  resultReportCardExportJobId           String?
  resultReportCardExportTargetId        String?
  resultReportCardExportEnvelopeId      String?
  resultReportCardMockExportAttemptId   String?
  resultReportCardExportReceiptId       String?
  resultReportCardExportSuppressionId   String?
  resultReportCardExportRetryPlanId     String?
  resultReportCardArchiveManifestId     String?
  actorId                               String
  actorRole                             String
  eventType                             String
  decision                              String
  safeSummary                           String   @db.Text
  reasonCodesJson                       Json?
  metadataJson                          Json?
  requestId                             String?
  correlationId                         String?
  createdAt                             DateTime @default(now())

  @@index([schoolId], map: "rc_export_audit_school")
  @@index([resultReportCardExportJobId], map: "rc_export_audit_job")
  @@index([resultReportCardExportTargetId], map: "rc_export_audit_tgt")
  @@index([resultReportCardExportEnvelopeId], map: "rc_export_audit_env")
  @@index([resultReportCardMockExportAttemptId], map: "rc_export_audit_attempt")
  @@index([resultReportCardExportReceiptId], map: "rc_export_audit_recpt")
  @@index([resultReportCardExportSuppressionId], map: "rc_export_audit_supp")
  @@index([resultReportCardExportRetryPlanId], map: "rc_export_audit_retry")
  @@index([resultReportCardArchiveManifestId], map: "rc_export_audit_arch")
  @@index([actorId], map: "rc_export_audit_actor")
  @@index([eventType], map: "rc_export_audit_event")
  @@index([createdAt], map: "rc_export_audit_cat")
  @@index([requestId], map: "rc_export_audit_req")
  @@index([correlationId], map: "rc_export_audit_corr")
}

model ResultReportCardExportIdempotencyRecord {
  resultReportCardExportIdempotencyId String   @id @default(uuid())
  schoolId                            String
  operation                           String
  idempotencyKey                      String
  requestHash                         String
  status                              String   @default("in_progress")
  resourceType                        String?
  resourceId                          String?
  safeResultSummary                   String?
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  expiresAt                           DateTime?

  @@unique([schoolId, operation, idempotencyKey], map: "rc_export_idem_unique")
  @@index([schoolId], map: "rc_export_idem_school")
  @@index([operation], map: "rc_export_idem_op")
  @@index([idempotencyKey], map: "rc_export_idem_key")
  @@index([status], map: "rc_export_idem_status")
  @@index([expiresAt], map: "rc_export_idem_expires")
  @@index([createdAt], map: "rc_export_idem_cat")
}

// ── Package 15: Controlled Report Card Access Readiness, Mock Portal Preview, Recipient Access Grants, Acknowledgement Ledger, Revocation, Expiry, Audit Read Model, and No-Live-Portal Gate ──

model ResultReportCardAccessGrantRecord {
  resultReportCardAccessGrantId           String    @id @default(uuid())
  schoolId                                String
  resultReportCardAssemblyId              String
  resultReportCardAudienceProjectionId    String
  resultReportCardReviewId                String
  resultReportCardExportJobId             String
  resultReportCardExportTargetId          String
  resultReportCardExportEnvelopeId        String
  resultReportCardExportReceiptId         String?
  resultReportCardArchiveManifestId       String?
  resultReleasePacketId                   String
  studentRef                              String
  audienceType                            String
  accessGrantStatus                       String    @default("draft")
  accessGrantMode                         String    @default("mock_portal_preview_only")
  safeAccessSummary                       String    @db.Text
  sourceRefsJson                          Json?
  allowedActionsJson                      Json?
  blockedActionsJson                      Json?
  blockedReasonCodesJson                  Json?
  createdByActorId                        String
  createdByRole                           String
  createdAt                               DateTime  @default(now())
  updatedAt                               DateTime  @updatedAt
  validatedAt                             DateTime?
  readyAt                                 DateTime?
  suppressedAt                            DateTime?
  revokedAt                               DateTime?
  expiredAt                               DateTime?
  voidedAt                                DateTime?

  @@index([schoolId], map: "rc_acgrant_school")
  @@index([resultReportCardAssemblyId], map: "rc_acgrant_assembly")
  @@index([resultReportCardAudienceProjectionId], map: "rc_acgrant_proj")
  @@index([resultReportCardReviewId], map: "rc_acgrant_review")
  @@index([resultReportCardExportJobId], map: "rc_acgrant_job")
  @@index([resultReportCardExportTargetId], map: "rc_acgrant_tgt")
  @@index([resultReportCardExportEnvelopeId], map: "rc_acgrant_env")
  @@index([resultReportCardExportReceiptId], map: "rc_acgrant_rcpt")
  @@index([resultReportCardArchiveManifestId], map: "rc_acgrant_arch")
  @@index([studentRef], map: "rc_acgrant_student")
  @@index([audienceType], map: "rc_acgrant_aud")
  @@index([accessGrantStatus], map: "rc_acgrant_status")
  @@index([accessGrantMode], map: "rc_acgrant_mode")
  @@index([createdAt], map: "rc_acgrant_cat")
  @@index([updatedAt], map: "rc_acgrant_uat")
}

model ResultReportCardAccessRecipientRecord {
  resultReportCardAccessRecipientId String   @id @default(uuid())
  schoolId                          String
  resultReportCardAccessGrantId     String
  studentRef                        String
  audienceType                      String
  recipientRole                     String
  recipientStatus                   String   @default("draft")
  recipientMode                     String   @default("future_access_only")
  safeRecipientSummary              String   @db.Text
  recipientDescriptorJson           Json?
  allowedFieldNamesJson             Json?
  blockedFieldNamesJson             Json?
  blockedReasonCodesJson            Json?
  createdByActorId                  String
  createdByRole                     String
  createdAt                         DateTime @default(now())
  updatedAt                         DateTime @updatedAt
  validatedAt                       DateTime?
  suppressedAt                      DateTime?
  revokedAt                         DateTime?
  voidedAt                          DateTime?

  @@index([schoolId], map: "rca_recpt_school")
  @@index([resultReportCardAccessGrantId], map: "rca_recpt_grant")
  @@index([studentRef], map: "rca_recpt_student")
  @@index([audienceType], map: "rca_recpt_aud")
  @@index([recipientRole], map: "rca_recpt_role")
  @@index([recipientStatus], map: "rca_recpt_status")
  @@index([recipientMode], map: "rca_recpt_mode")
  @@index([createdAt], map: "rca_recpt_cat")
  @@index([updatedAt], map: "rca_recpt_uat")
}

model ResultReportCardPortalPreviewRecord {
  resultReportCardPortalPreviewId   String    @id @default(uuid())
  schoolId                          String
  resultReportCardAccessGrantId     String
  resultReportCardAccessRecipientId String
  resultReportCardExportEnvelopeId  String
  resultReportCardArchiveManifestId String?
  previewStatus                     String    @default("draft")
  previewMode                       String    @default("mock_portal_preview_only")
  audienceType                      String
  safePreviewSummary                String    @db.Text
  safePreviewPayloadJson            Json?
  redactionRulesJson                Json?
  allowedFieldNamesJson             Json?
  blockedFieldNamesJson             Json?
  blockedReasonCodesJson            Json?
  createdByActorId                  String
  createdByRole                     String
  createdAt                         DateTime  @default(now())
  updatedAt                         DateTime  @updatedAt
  sealedAt                          DateTime?
  suppressedAt                      DateTime?
  blockedAt                         DateTime?
  voidedAt                          DateTime?

  @@index([schoolId], map: "rca_preview_school")
  @@index([resultReportCardAccessGrantId], map: "rca_preview_grant")
  @@index([resultReportCardAccessRecipientId], map: "rca_preview_recip")
  @@index([resultReportCardExportEnvelopeId], map: "rca_preview_env")
  @@index([resultReportCardArchiveManifestId], map: "rca_preview_arch")
  @@index([previewStatus], map: "rca_preview_status")
  @@index([previewMode], map: "rca_preview_mode")
  @@index([audienceType], map: "rca_preview_aud")
  @@index([createdAt], map: "rca_preview_cat")
  @@index([updatedAt], map: "rca_preview_uat")
}

model ResultReportCardAccessTokenIntentRecord {
  resultReportCardAccessTokenIntentId String   @id @default(uuid())
  schoolId                            String
  resultReportCardAccessGrantId       String
  resultReportCardAccessRecipientId   String
  intentStatus                        String   @default("draft")
  intentMode                          String   @default("no_token_created")
  tokenPurpose                        String
  safeTokenIntentSummary              String   @db.Text
  tokenIntentMetadataJson             Json?
  blockedReasonCodesJson              Json?
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  validatedAt                         DateTime?
  blockedAt                           DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "rca_token_school")
  @@index([resultReportCardAccessGrantId], map: "rca_token_grant")
  @@index([resultReportCardAccessRecipientId], map: "rca_token_recip")
  @@index([intentStatus], map: "rca_token_status")
  @@index([intentMode], map: "rca_token_mode")
  @@index([tokenPurpose], map: "rca_token_purpose")
  @@index([createdAt], map: "rca_token_cat")
  @@index([updatedAt], map: "rca_token_uat")
}

model ResultReportCardAccessAcknowledgementRecord {
  resultReportCardAccessAcknowledgementId String   @id @default(uuid())
  schoolId                                String
  resultReportCardAccessGrantId           String
  resultReportCardAccessRecipientId       String
  resultReportCardPortalPreviewId         String?
  acknowledgementStatus                   String   @default("created")
  acknowledgementType                     String   @default("dry_run_acknowledgement")
  safeAcknowledgementSummary              String   @db.Text
  acknowledgementMetadataJson             Json?
  blockedReasonCodesJson                  Json?
  createdByActorId                        String
  createdByRole                           String
  createdAt                               DateTime @default(now())
  updatedAt                               DateTime @updatedAt
  recordedAt                              DateTime?
  voidedAt                                DateTime?

  @@index([schoolId], map: "rca_ack_school")
  @@index([resultReportCardAccessGrantId], map: "rca_ack_grant")
  @@index([resultReportCardAccessRecipientId], map: "rca_ack_recip")
  @@index([resultReportCardPortalPreviewId], map: "rca_ack_preview")
  @@index([acknowledgementStatus], map: "rca_ack_status")
  @@index([acknowledgementType], map: "rca_ack_type")
  @@index([createdAt], map: "rca_ack_cat")
  @@index([updatedAt], map: "rca_ack_uat")
}

model ResultReportCardAccessRevocationRecord {
  resultReportCardAccessRevocationId String    @id @default(uuid())
  schoolId                           String
  resultReportCardAccessGrantId      String
  resultReportCardAccessRecipientId  String?
  revocationStatus                   String    @default("draft")
  revocationScope                    String    @default("grant")
  revocationReason                   String
  safeRevocationSummary              String    @db.Text
  reasonCodesJson                    Json?
  createdByActorId                   String
  createdByRole                      String
  createdAt                          DateTime  @default(now())
  updatedAt                          DateTime  @updatedAt
  appliedAt                          DateTime?
  voidedAt                           DateTime?

  @@index([schoolId], map: "rca_rev_school")
  @@index([resultReportCardAccessGrantId], map: "rca_rev_grant")
  @@index([resultReportCardAccessRecipientId], map: "rca_rev_recip")
  @@index([revocationStatus], map: "rca_rev_status")
  @@index([revocationScope], map: "rca_rev_scope")
  @@index([createdAt], map: "rca_rev_cat")
  @@index([updatedAt], map: "rca_rev_uat")
}

model ResultReportCardAccessExpiryRecord {
  resultReportCardAccessExpiryId String    @id @default(uuid())
  schoolId                       String
  resultReportCardAccessGrantId  String
  resultReportCardAccessRecipientId String?
  expiryStatus                   String    @default("draft")
  expiryScope                    String    @default("grant")
  expiresAt                      DateTime
  safeExpirySummary              String    @db.Text
  reasonCodesJson                Json?
  createdByActorId               String
  createdByRole                  String
  createdAt                      DateTime  @default(now())
  updatedAt                      DateTime  @updatedAt
  appliedAt                      DateTime?
  cancelledAt                    DateTime?
  voidedAt                       DateTime?

  @@index([schoolId], map: "rca_exp_school")
  @@index([resultReportCardAccessGrantId], map: "rca_exp_grant")
  @@index([resultReportCardAccessRecipientId], map: "rca_exp_recip")
  @@index([expiryStatus], map: "rca_exp_status")
  @@index([expiryScope], map: "rca_exp_scope")
  @@index([expiresAt], map: "rca_exp_expires")
  @@index([createdAt], map: "rca_exp_cat")
  @@index([updatedAt], map: "rca_exp_uat")
}

model ResultReportCardAccessTimelineRecord {
  resultReportCardAccessTimelineId String   @id @default(uuid())
  schoolId                         String
  resultReportCardAccessGrantId    String?
  resultReportCardAccessRecipientId String?
  timelineStatus                   String   @default("recorded")
  eventType                        String
  safeTimelineSummary              String   @db.Text
  eventRefsJson                    Json?
  metadataJson                     Json?
  createdByActorId                 String
  createdByRole                    String
  createdAt                        DateTime @default(now())

  @@index([schoolId], map: "rca_tl_school")
  @@index([resultReportCardAccessGrantId], map: "rca_tl_grant")
  @@index([resultReportCardAccessRecipientId], map: "rca_tl_recip")
  @@index([timelineStatus], map: "rca_tl_status")
  @@index([eventType], map: "rca_tl_event")
  @@index([createdAt], map: "rca_tl_cat")
}

model ResultReportCardAccessSummaryRecord {
  resultReportCardAccessSummaryId String   @id @default(uuid())
  schoolId                        String
  studentRef                      String?
  resultReportCardAssemblyId      String?
  resultReportCardExportJobId     String?
  summaryStatus                   String   @default("active")
  summaryScope                    String   @default("school")
  safeSummary                     String   @db.Text
  audienceCountsJson              Json?
  statusCountsJson                Json?
  blockedReasonCodesJson          Json?
  createdAt                       DateTime @default(now())
  updatedAt                       DateTime @updatedAt
  refreshedAt                     DateTime?
  voidedAt                        DateTime?

  @@index([schoolId], map: "rca_sum_school")
  @@index([studentRef], map: "rca_sum_student")
  @@index([resultReportCardAssemblyId], map: "rca_sum_assembly")
  @@index([resultReportCardExportJobId], map: "rca_sum_job")
  @@index([summaryStatus], map: "rca_sum_status")
  @@index([summaryScope], map: "rca_sum_scope")
  @@index([createdAt], map: "rca_sum_cat")
  @@index([updatedAt], map: "rca_sum_uat")
}

model ResultReportCardAccessAuditRecord {
  resultReportCardAccessAuditId               String   @id @default(uuid())
  schoolId                                    String
  resultReportCardAccessGrantId               String?
  resultReportCardAccessRecipientId           String?
  resultReportCardPortalPreviewId             String?
  resultReportCardAccessTokenIntentId         String?
  resultReportCardAccessAcknowledgementId     String?
  resultReportCardAccessRevocationId          String?
  resultReportCardAccessExpiryId              String?
  resultReportCardAccessTimelineId            String?
  resultReportCardAccessSummaryId             String?
  actorId                                     String
  actorRole                                   String
  eventType                                   String
  decision                                    String
  safeSummary                                 String   @db.Text
  reasonCodesJson                             Json?
  metadataJson                                Json?
  requestId                                   String?
  correlationId                               String?
  createdAt                                   DateTime @default(now())

  @@index([schoolId], map: "rca_audit_school")
  @@index([resultReportCardAccessGrantId], map: "rca_audit_grant")
  @@index([resultReportCardAccessRecipientId], map: "rca_audit_recip")
  @@index([resultReportCardPortalPreviewId], map: "rca_audit_preview")
  @@index([resultReportCardAccessTokenIntentId], map: "rca_audit_token")
  @@index([resultReportCardAccessAcknowledgementId], map: "rca_audit_ack")
  @@index([resultReportCardAccessRevocationId], map: "rca_audit_rev")
  @@index([resultReportCardAccessExpiryId], map: "rca_audit_exp")
  @@index([resultReportCardAccessTimelineId], map: "rca_audit_tl")
  @@index([resultReportCardAccessSummaryId], map: "rca_audit_sum")
  @@index([actorId], map: "rca_audit_actor")
  @@index([eventType], map: "rca_audit_event")
  @@index([createdAt], map: "rca_audit_cat")
  @@index([requestId], map: "rca_audit_req")
  @@index([correlationId], map: "rca_audit_corr")
}

model ResultReportCardAccessIdempotencyRecord {
  resultReportCardAccessIdempotencyId String   @id @default(uuid())
  schoolId                            String
  operation                           String
  idempotencyKey                      String
  requestHash                         String
  status                              String   @default("in_progress")
  resourceType                        String?
  resourceId                          String?
  safeResultSummary                   String?
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  expiresAt                           DateTime?

  @@unique([schoolId, operation, idempotencyKey], map: "rca_idem_unique")
  @@index([schoolId], map: "rca_idem_school")
  @@index([operation], map: "rca_idem_op")
  @@index([idempotencyKey], map: "rca_idem_key")
  @@index([status], map: "rca_idem_status")
  @@index([expiresAt], map: "rca_idem_expires")
  @@index([createdAt], map: "rca_idem_cat")
}

model ResultFollowUpCaseRecord {
  resultFollowUpCaseId                  String    @id @default(uuid())
  schoolId                              String
  studentRef                            String
  resultFinalizationDecisionId          String?
  resultReleaseReadinessId              String?
  resultReleasePacketId                 String?
  resultReportCardAssemblyId            String?
  resultReportCardAudienceProjectionId  String?
  resultReportCardAccessGrantId         String?
  resultReportCardAccessSummaryId       String?
  caseStatus                            String    @default("draft")
  caseType                              String    @default("academic_support")
  casePriority                          String    @default("medium")
  caseMode                              String    @default("mock_action_only")
  safeCaseSummary                       String
  sourceRefsJson                        Json      @default("[]")
  triggerReasonsJson                    Json      @default("[]")
  allowedActionsJson                    Json      @default("[]")
  blockedActionsJson                    Json      @default("[]")
  blockedReasonCodesJson                Json      @default("[]")
  createdByActorId                      String
  createdByRole                         String
  createdAt                             DateTime  @default(now())
  updatedAt                             DateTime  @updatedAt
  openedAt                              DateTime?
  triagedAt                             DateTime?
  plannedAt                             DateTime?
  reviewedAt                            DateTime?
  closedAt                              DateTime?
  blockedAt                             DateTime?
  voidedAt                              DateTime?

  @@index([schoolId], map: "rfu_case_school")
  @@index([schoolId, studentRef], map: "rfu_case_school_student")
  @@index([caseStatus], map: "rfu_case_status")
  @@index([caseType], map: "rfu_case_type")
  @@index([casePriority], map: "rfu_case_priority")
  @@index([createdByActorId], map: "rfu_case_actor")
  @@index([createdAt], map: "rfu_case_cat")
}

model ResultFollowUpSignalRecord {
  resultFollowUpSignalId    String    @id @default(uuid())
  schoolId                  String
  resultFollowUpCaseId      String
  studentRef                String
  signalStatus              String    @default("active")
  signalType                String    @default("weak_objective_pattern")
  signalSeverity            String    @default("low")
  signalSource              String
  safeSignalSummary         String
  evidenceRefsJson          Json      @default("[]")
  reasonCodesJson           Json      @default("[]")
  blockedReasonCodesJson    Json      @default("[]")
  createdByActorId          String
  createdByRole             String
  createdAt                 DateTime  @default(now())
  updatedAt                 DateTime  @updatedAt
  suppressedAt              DateTime?
  voidedAt                  DateTime?

  @@index([schoolId], map: "rfu_sig_school")
  @@index([resultFollowUpCaseId], map: "rfu_sig_case")
  @@index([schoolId, studentRef], map: "rfu_sig_student")
  @@index([signalStatus], map: "rfu_sig_status")
  @@index([signalType], map: "rfu_sig_type")
  @@index([signalSeverity], map: "rfu_sig_severity")
  @@index([createdAt], map: "rfu_sig_cat")
}

model ResultFollowUpActionPlanRecord {
  resultFollowUpActionPlanId    String    @id @default(uuid())
  schoolId                      String
  resultFollowUpCaseId          String
  studentRef                    String
  planStatus                    String    @default("draft")
  planMode                      String    @default("mock_plan_only")
  safePlanSummary               String
  recommendedActionsJson        Json      @default("[]")
  teacherReviewNotesJson        Json      @default("[]")
  parentSafeGuidanceRefsJson    Json      @default("[]")
  studentReflectionRefsJson     Json      @default("[]")
  reviewWindowRefsJson          Json      @default("[]")
  blockedReasonCodesJson        Json      @default("[]")
  createdByActorId              String
  createdByRole                 String
  createdAt                     DateTime  @default(now())
  updatedAt                     DateTime  @updatedAt
  draftedAt                     DateTime?
  approvedForFutureUseAt        DateTime?
  suppressedAt                  DateTime?
  voidedAt                      DateTime?

  @@index([schoolId], map: "rfu_plan_school")
  @@index([resultFollowUpCaseId], map: "rfu_plan_case")
  @@index([schoolId, studentRef], map: "rfu_plan_student")
  @@index([planStatus], map: "rfu_plan_status")
  @@index([createdAt], map: "rfu_plan_cat")
}

model TeacherFollowUpQueueItemRecord {
  teacherFollowUpQueueItemId    String    @id @default(uuid())
  schoolId                      String
  resultFollowUpCaseId          String
  resultFollowUpActionPlanId    String?
  studentRef                    String
  teacherRef                    String
  queueStatus                   String    @default("draft")
  queueMode                     String    @default("mock_queue_only")
  queuePriority                 String    @default("medium")
  safeQueueSummary              String
  suggestedNextActionsJson      Json      @default("[]")
  blockedReasonCodesJson        Json      @default("[]")
  createdByActorId              String
  createdByRole                 String
  createdAt                     DateTime  @default(now())
  updatedAt                     DateTime  @updatedAt
  queuedAt                      DateTime?
  acknowledgedAt                DateTime?
  completedAt                   DateTime?
  suppressedAt                  DateTime?
  voidedAt                      DateTime?

  @@index([schoolId], map: "rfu_q_school")
  @@index([resultFollowUpCaseId], map: "rfu_q_case")
  @@index([resultFollowUpActionPlanId], map: "rfu_q_plan")
  @@index([schoolId, studentRef], map: "rfu_q_student")
  @@index([schoolId, teacherRef], map: "rfu_q_teacher")
  @@index([queueStatus], map: "rfu_q_status")
  @@index([queuePriority], map: "rfu_q_priority")
  @@index([createdAt], map: "rfu_q_cat")
}

model ParentGuidanceDraftRecord {
  parentGuidanceDraftId     String    @id @default(uuid())
  schoolId                  String
  resultFollowUpCaseId      String
  resultFollowUpActionPlanId String?
  studentRef                String
  audienceType              String    @default("parent")
  draftStatus               String    @default("draft")
  draftMode                 String    @default("mock_draft_only")
  safeGuidanceSummary       String
  safeGuidanceBodyJson      Json      @default("{}")
  allowedFieldNamesJson     Json      @default("[]")
  blockedFieldNamesJson     Json      @default("[]")
  blockedReasonCodesJson    Json      @default("[]")
  createdByActorId          String
  createdByRole             String
  createdAt                 DateTime  @default(now())
  updatedAt                 DateTime  @updatedAt
  reviewedAt                DateTime?
  approvedForFutureUseAt    DateTime?
  suppressedAt              DateTime?
  voidedAt                  DateTime?

  @@index([schoolId], map: "rfu_pg_school")
  @@index([resultFollowUpCaseId], map: "rfu_pg_case")
  @@index([resultFollowUpActionPlanId], map: "rfu_pg_plan")
  @@index([schoolId, studentRef], map: "rfu_pg_student")
  @@index([draftStatus], map: "rfu_pg_status")
  @@index([createdAt], map: "rfu_pg_cat")
}

model StudentReflectionTaskDraftRecord {
  studentReflectionTaskDraftId    String    @id @default(uuid())
  schoolId                        String
  resultFollowUpCaseId            String
  resultFollowUpActionPlanId      String?
  studentRef                      String
  draftStatus                     String    @default("draft")
  draftMode                       String    @default("mock_draft_only")
  safeReflectionPrompt            String
  reflectionObjectiveRefsJson     Json      @default("[]")
  scaffoldStepsJson               Json      @default("[]")
  blockedReasonCodesJson          Json      @default("[]")
  createdByActorId                String
  createdByRole                   String
  createdAt                       DateTime  @default(now())
  updatedAt                       DateTime  @updatedAt
  reviewedAt                      DateTime?
  approvedForFutureUseAt          DateTime?
  suppressedAt                    DateTime?
  voidedAt                        DateTime?

  @@index([schoolId], map: "rfu_sr_school")
  @@index([resultFollowUpCaseId], map: "rfu_sr_case")
  @@index([resultFollowUpActionPlanId], map: "rfu_sr_plan")
  @@index([schoolId, studentRef], map: "rfu_sr_student")
  @@index([draftStatus], map: "rfu_sr_status")
  @@index([createdAt], map: "rfu_sr_cat")
}

model FollowUpReviewWindowRecord {
  followUpReviewWindowId    String    @id @default(uuid())
  schoolId                  String
  resultFollowUpCaseId      String
  studentRef                String
  windowStatus              String    @default("draft")
  windowMode                String    @default("metadata_only")
  reviewWindowStartAt       DateTime?
  reviewWindowEndAt         DateTime?
  safeWindowSummary         String
  reviewCriteriaJson        Json      @default("[]")
  blockedReasonCodesJson    Json      @default("[]")
  createdByActorId          String
  createdByRole             String
  createdAt                 DateTime  @default(now())
  updatedAt                 DateTime  @updatedAt
  scheduledMockAt           DateTime?
  completedMockAt           DateTime?
  cancelledAt               DateTime?
  voidedAt                  DateTime?

  @@index([schoolId], map: "rfu_rw_school")
  @@index([resultFollowUpCaseId], map: "rfu_rw_case")
  @@index([schoolId, studentRef], map: "rfu_rw_student")
  @@index([windowStatus], map: "rfu_rw_status")
  @@index([createdAt], map: "rfu_rw_cat")
}

model FollowUpEscalationPlanRecord {
  followUpEscalationPlanId    String    @id @default(uuid())
  schoolId                    String
  resultFollowUpCaseId        String
  studentRef                  String
  escalationStatus            String    @default("draft")
  escalationMode              String    @default("metadata_only")
  escalationLevel             String    @default("low")
  safeEscalationSummary       String
  reviewerRoleTargetsJson     Json      @default("[]")
  allowedDisclosureJson       Json      @default("[]")
  blockedDisclosureJson       Json      @default("[]")
  blockedReasonCodesJson      Json      @default("[]")
  createdByActorId            String
  createdByRole               String
  createdAt                   DateTime  @default(now())
  updatedAt                   DateTime  @updatedAt
  reviewedAt                  DateTime?
  approvedForFutureUseAt      DateTime?
  suppressedAt                DateTime?
  voidedAt                    DateTime?

  @@index([schoolId], map: "rfu_ep_school")
  @@index([resultFollowUpCaseId], map: "rfu_ep_case")
  @@index([schoolId, studentRef], map: "rfu_ep_student")
  @@index([escalationStatus], map: "rfu_ep_status")
  @@index([escalationLevel], map: "rfu_ep_level")
  @@index([createdAt], map: "rfu_ep_cat")
}

model FollowUpSummaryRecord {
  followUpSummaryId     String    @id @default(uuid())
  schoolId              String
  studentRef            String?
  summaryScope          String    @default("school")
  summaryStatus         String    @default("active")
  safeSummary           String
  caseCountsJson        Json      @default("{}")
  priorityCountsJson    Json      @default("{}")
  statusCountsJson      Json      @default("{}")
  blockedReasonCodesJson Json     @default("[]")
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt
  refreshedAt           DateTime?
  voidedAt              DateTime?

  @@index([schoolId], map: "rfu_sum_school")
  @@index([schoolId, studentRef], map: "rfu_sum_student")
  @@index([summaryScope], map: "rfu_sum_scope")
  @@index([summaryStatus], map: "rfu_sum_status")
  @@index([createdAt], map: "rfu_sum_cat")
}

model FollowUpAuditRecord {
  followUpAuditId                 String    @id @default(uuid())
  schoolId                        String
  resultFollowUpCaseId            String?
  resultFollowUpSignalId          String?
  resultFollowUpActionPlanId      String?
  teacherFollowUpQueueItemId      String?
  parentGuidanceDraftId           String?
  studentReflectionTaskDraftId    String?
  followUpReviewWindowId          String?
  followUpEscalationPlanId        String?
  followUpSummaryId               String?
  actorId                         String
  actorRole                       String
  eventType                       String
  decision                        String
  safeSummary                     String
  reasonCodesJson                 Json      @default("[]")
  metadataJson                    Json      @default("{}")
  requestId                       String?
  correlationId                   String?
  createdAt                       DateTime  @default(now())

  @@index([schoolId], map: "rfu_audit_school")
  @@index([resultFollowUpCaseId], map: "rfu_audit_case")
  @@index([resultFollowUpSignalId], map: "rfu_audit_signal")
  @@index([resultFollowUpActionPlanId], map: "rfu_audit_plan")
  @@index([teacherFollowUpQueueItemId], map: "rfu_audit_queue")
  @@index([parentGuidanceDraftId], map: "rfu_audit_pg")
  @@index([studentReflectionTaskDraftId], map: "rfu_audit_sr")
  @@index([followUpReviewWindowId], map: "rfu_audit_rw")
  @@index([followUpEscalationPlanId], map: "rfu_audit_ep")
  @@index([followUpSummaryId], map: "rfu_audit_sum")
  @@index([eventType], map: "rfu_audit_evt")
  @@index([createdAt], map: "rfu_audit_cat")
}

model FollowUpIdempotencyRecord {
  followUpIdempotencyId String    @id @default(uuid())
  schoolId              String
  operation             String
  idempotencyKey        String
  requestHash           String
  status                String    @default("in_progress")
  resourceType          String?
  resourceId            String?
  safeResultSummary     String?
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt
  expiresAt             DateTime?

  @@unique([schoolId, operation, idempotencyKey], map: "rfu_idem_unique")
  @@index([schoolId], map: "rfu_idem_school")
  @@index([operation], map: "rfu_idem_op")
  @@index([idempotencyKey], map: "rfu_idem_key")
  @@index([status], map: "rfu_idem_status")
  @@index([expiresAt], map: "rfu_idem_expires")
  @@index([createdAt], map: "rfu_idem_cat")
}

// ─── Package 17: Result Recovery Planner Models ──────────────────────────

model ResultRecoveryPlanRecord {
  resultRecoveryPlanId             String    @id @default(uuid())
  schoolId                         String
  studentRef                       String
  resultFollowUpCaseId             String?
  resultFollowUpActionPlanId       String?
  resultFollowUpSummaryId          String?
  resultReportCardAssemblyId       String?
  resultReportCardAudienceProjectionId String?
  resultReportCardAccessGrantId    String?
  resultLearningEvidenceSnapshotId String?
  planStatus                       String    @default("draft")
  planMode                         String    @default("mock_plan_only")
  planPriority                     String    @default("medium")
  safePlanSummary                  String
  sourceRefsJson                   Json      @default("[]")
  objectiveRefsJson                Json      @default("[]")
  recommendedSequenceJson          Json      @default("[]")
  allowedActionsJson               Json      @default("[]")
  blockedActionsJson               Json      @default("[]")
  blockedReasonCodesJson           Json      @default("[]")
  createdByActorId                 String
  createdByRole                    String
  createdAt                        DateTime  @default(now())
  updatedAt                        DateTime  @updatedAt
  draftedAt                        DateTime?
  reviewReadyAt                    DateTime?
  approvedForFutureUseAt           DateTime?
  suppressedAt                     DateTime?
  blockedAt                        DateTime?
  voidedAt                         DateTime?

  @@index([schoolId], map: "rrc_plan_school")
  @@index([schoolId, studentRef], map: "rrc_plan_student")
  @@index([planStatus], map: "rrc_plan_status")
  @@index([planMode], map: "rrc_plan_mode")
  @@index([planPriority], map: "rrc_plan_priority")
  @@index([resultFollowUpCaseId], map: "rrc_plan_fu_case")
  @@index([resultFollowUpActionPlanId], map: "rrc_plan_fu_action")
  @@index([resultFollowUpSummaryId], map: "rrc_plan_fu_summary")
  @@index([createdAt], map: "rrc_plan_cat")
}

model ResultRecoveryObjectiveRecord {
  resultRecoveryObjectiveId String    @id @default(uuid())
  schoolId                  String
  resultRecoveryPlanId      String
  studentRef                String
  objectiveStatus           String    @default("draft")
  objectiveType             String    @default("concept_repair")
  objectivePriority         String    @default("medium")
  learningObjectiveRef      String?
  skillRef                  String?
  topicRef                  String?
  safeObjectiveSummary      String
  evidenceRefsJson          Json      @default("[]")
  successCriteriaJson       Json      @default("[]")
  blockedReasonCodesJson    Json      @default("[]")
  createdByActorId          String
  createdByRole             String
  createdAt                 DateTime  @default(now())
  updatedAt                 DateTime  @updatedAt
  readyAt                   DateTime?
  completedMockAt           DateTime?
  suppressedAt              DateTime?
  voidedAt                  DateTime?

  @@index([schoolId], map: "rrc_obj_school")
  @@index([resultRecoveryPlanId], map: "rrc_obj_plan")
  @@index([schoolId, studentRef], map: "rrc_obj_student")
  @@index([objectiveStatus], map: "rrc_obj_status")
  @@index([objectiveType], map: "rrc_obj_type")
  @@index([createdAt], map: "rrc_obj_cat")
}

model ResultRecoveryStepRecord {
  resultRecoveryStepId      String    @id @default(uuid())
  schoolId                  String
  resultRecoveryPlanId      String
  resultRecoveryObjectiveId String?
  studentRef                String
  stepStatus                String    @default("draft")
  stepType                  String    @default("review_concept")
  stepOrder                 Int       @default(0)
  stepMode                  String    @default("metadata_only")
  safeStepSummary           String
  stepInstructionsJson      Json      @default("[]")
  teacherNotesJson          Json      @default("[]")
  studentSafeNotesJson      Json      @default("[]")
  blockedReasonCodesJson    Json      @default("[]")
  createdByActorId          String
  createdByRole             String
  createdAt                 DateTime  @default(now())
  updatedAt                 DateTime  @updatedAt
  reviewReadyAt             DateTime?
  approvedForFutureUseAt    DateTime?
  completedMockAt           DateTime?
  suppressedAt              DateTime?
  voidedAt                  DateTime?

  @@index([schoolId], map: "rrc_step_school")
  @@index([resultRecoveryPlanId], map: "rrc_step_plan")
  @@index([resultRecoveryObjectiveId], map: "rrc_step_obj")
  @@index([schoolId, studentRef], map: "rrc_step_student")
  @@index([stepStatus], map: "rrc_step_status")
  @@index([stepType], map: "rrc_step_type")
  @@index([stepOrder], map: "rrc_step_order")
  @@index([createdAt], map: "rrc_step_cat")
}

model ResultRecoveryPracticeDraftRecord {
  resultRecoveryPracticeDraftId String    @id @default(uuid())
  schoolId                      String
  resultRecoveryPlanId          String
  resultRecoveryObjectiveId     String?
  resultRecoveryStepId          String?
  studentRef                    String
  draftStatus                   String    @default("draft")
  draftMode                     String    @default("existing_question_refs_only")
  practiceType                  String    @default("existing_question_refs_only")
  safePracticeSummary           String
  questionRefsJson              Json      @default("[]")
  objectiveRefsJson             Json      @default("[]")
  difficultyHintsJson           Json      @default("[]")
  selectionReasonCodesJson      Json      @default("[]")
  blockedReasonCodesJson        Json      @default("[]")
  createdByActorId              String
  createdByRole                 String
  createdAt                     DateTime  @default(now())
  updatedAt                     DateTime  @updatedAt
  reviewReadyAt                 DateTime?
  approvedForFutureUseAt        DateTime?
  suppressedAt                  DateTime?
  voidedAt                      DateTime?

  @@index([schoolId], map: "rrc_pd_school")
  @@index([resultRecoveryPlanId], map: "rrc_pd_plan")
  @@index([resultRecoveryObjectiveId], map: "rrc_pd_obj")
  @@index([resultRecoveryStepId], map: "rrc_pd_step")
  @@index([schoolId, studentRef], map: "rrc_pd_student")
  @@index([draftStatus], map: "rrc_pd_status")
  @@index([practiceType], map: "rrc_pd_type")
  @@index([createdAt], map: "rrc_pd_cat")
}

model ResultRecoveryResourceRecommendationRecord {
  resultRecoveryResourceRecommendationId String    @id @default(uuid())
  schoolId                               String
  resultRecoveryPlanId                   String
  resultRecoveryObjectiveId              String?
  studentRef                             String
  recommendationStatus                   String    @default("draft")
  recommendationMode                     String    @default("metadata_only")
  resourceType                           String    @default("metadata_only")
  safeResourceSummary                    String
  resourceRefsJson                       Json      @default("[]")
  selectionReasonCodesJson               Json      @default("[]")
  allowedAudienceJson                    Json      @default("[]")
  blockedReasonCodesJson                 Json      @default("[]")
  createdByActorId                       String
  createdByRole                          String
  createdAt                              DateTime  @default(now())
  updatedAt                              DateTime  @updatedAt
  reviewReadyAt                          DateTime?
  approvedForFutureUseAt                 DateTime?
  suppressedAt                           DateTime?
  voidedAt                               DateTime?

  @@index([schoolId], map: "rrc_rr_school")
  @@index([resultRecoveryPlanId], map: "rrc_rr_plan")
  @@index([resultRecoveryObjectiveId], map: "rrc_rr_obj")
  @@index([schoolId, studentRef], map: "rrc_rr_student")
  @@index([recommendationStatus], map: "rrc_rr_status")
  @@index([resourceType], map: "rrc_rr_type")
  @@index([createdAt], map: "rrc_rr_cat")
}

model ResultRecoveryTeacherReviewPacketRecord {
  resultRecoveryTeacherReviewPacketId String    @id @default(uuid())
  schoolId                            String
  resultRecoveryPlanId                String
  studentRef                          String
  teacherRef                          String
  packetStatus                        String    @default("draft")
  packetMode                          String    @default("metadata_only")
  safePacketSummary                   String
  caseRefsJson                        Json      @default("[]")
  objectiveRefsJson                   Json      @default("[]")
  stepRefsJson                        Json      @default("[]")
  practiceDraftRefsJson               Json      @default("[]")
  resourceRecommendationRefsJson      Json      @default("[]")
  reviewQuestionsJson                 Json      @default("[]")
  blockedReasonCodesJson              Json      @default("[]")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime  @default(now())
  updatedAt                           DateTime  @updatedAt
  reviewReadyAt                       DateTime?
  acknowledgedMockAt                  DateTime?
  approvedForFutureUseAt              DateTime?
  suppressedAt                        DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "rrc_trp_school")
  @@index([resultRecoveryPlanId], map: "rrc_trp_plan")
  @@index([schoolId, studentRef], map: "rrc_trp_student")
  @@index([teacherRef], map: "rrc_trp_teacher")
  @@index([packetStatus], map: "rrc_trp_status")
  @@index([createdAt], map: "rrc_trp_cat")
}

model ResultRecoveryStudentSupportDraftRecord {
  resultRecoveryStudentSupportDraftId String    @id @default(uuid())
  schoolId                            String
  resultRecoveryPlanId                String
  studentRef                          String
  draftStatus                         String    @default("draft")
  draftMode                           String    @default("metadata_only")
  safeSupportSummary                  String
  studentSupportBodyJson              Json      @default("{}")
  reflectionPromptRefsJson            Json      @default("[]")
  practiceDraftRefsJson               Json      @default("[]")
  blockedFieldNamesJson               Json      @default("[]")
  blockedReasonCodesJson              Json      @default("[]")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime  @default(now())
  updatedAt                           DateTime  @updatedAt
  reviewReadyAt                       DateTime?
  approvedForFutureUseAt              DateTime?
  suppressedAt                        DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "rrc_ssd_school")
  @@index([resultRecoveryPlanId], map: "rrc_ssd_plan")
  @@index([schoolId, studentRef], map: "rrc_ssd_student")
  @@index([draftStatus], map: "rrc_ssd_status")
  @@index([createdAt], map: "rrc_ssd_cat")
}

model ResultRecoveryParentSupportNoteDraftRecord {
  resultRecoveryParentSupportNoteDraftId String    @id @default(uuid())
  schoolId                                String
  resultRecoveryPlanId                    String
  studentRef                              String
  audienceType                            String    @default("parent")
  draftStatus                             String    @default("draft")
  draftMode                               String    @default("metadata_only")
  safeSupportSummary                      String
  parentSupportBodyJson                   Json      @default("{}")
  allowedFieldNamesJson                   Json      @default("[]")
  blockedFieldNamesJson                   Json      @default("[]")
  blockedReasonCodesJson                  Json      @default("[]")
  createdByActorId                        String
  createdByRole                           String
  createdAt                               DateTime  @default(now())
  updatedAt                               DateTime  @updatedAt
  reviewReadyAt                           DateTime?
  approvedForFutureUseAt                  DateTime?
  suppressedAt                            DateTime?
  voidedAt                                DateTime?

  @@index([schoolId], map: "rrc_psnd_school")
  @@index([resultRecoveryPlanId], map: "rrc_psnd_plan")
  @@index([schoolId, studentRef], map: "rrc_psnd_student")
  @@index([audienceType], map: "rrc_psnd_audience")
  @@index([draftStatus], map: "rrc_psnd_status")
  @@index([createdAt], map: "rrc_psnd_cat")
}

model ResultRecoveryCheckpointRecord {
  resultRecoveryCheckpointId String    @id @default(uuid())
  schoolId                   String
  resultRecoveryPlanId       String
  studentRef                 String
  checkpointStatus           String    @default("draft")
  checkpointMode             String    @default("metadata_only")
  checkpointType             String    @default("teacher_review")
  safeCheckpointSummary      String
  checkpointCriteriaJson     Json      @default("[]")
  scheduledMockAt            DateTime?
  completedMockAt            DateTime?
  cancelledAt                DateTime?
  blockedReasonCodesJson     Json      @default("[]")
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime  @default(now())
  updatedAt                  DateTime  @updatedAt
  voidedAt                   DateTime?

  @@index([schoolId], map: "rrc_chk_school")
  @@index([resultRecoveryPlanId], map: "rrc_chk_plan")
  @@index([schoolId, studentRef], map: "rrc_chk_student")
  @@index([checkpointStatus], map: "rrc_chk_status")
  @@index([checkpointType], map: "rrc_chk_type")
  @@index([createdAt], map: "rrc_chk_cat")
}

model ResultRecoverySummaryRecord {
  resultRecoverySummaryId String    @id @default(uuid())
  schoolId                String
  studentRef              String?
  summaryScope            String    @default("school")
  summaryStatus           String    @default("active")
  safeSummary             String
  planCountsJson          Json      @default("{}")
  objectiveCountsJson     Json      @default("{}")
  checkpointCountsJson    Json      @default("{}")
  blockedReasonCodesJson  Json      @default("[]")
  createdAt               DateTime  @default(now())
  updatedAt               DateTime  @updatedAt
  refreshedAt             DateTime?
  voidedAt                DateTime?

  @@index([schoolId], map: "rrc_sum_school")
  @@index([schoolId, studentRef], map: "rrc_sum_student")
  @@index([summaryScope], map: "rrc_sum_scope")
  @@index([summaryStatus], map: "rrc_sum_status")
  @@index([createdAt], map: "rrc_sum_cat")
}

model ResultRecoveryAuditRecord {
  resultRecoveryAuditId                     String    @id @default(uuid())
  schoolId                                  String
  resultRecoveryPlanId                      String?
  resultRecoveryObjectiveId                 String?
  resultRecoveryStepId                      String?
  resultRecoveryPracticeDraftId             String?
  resultRecoveryResourceRecommendationId    String?
  resultRecoveryTeacherReviewPacketId       String?
  resultRecoveryStudentSupportDraftId       String?
  resultRecoveryParentSupportNoteDraftId    String?
  resultRecoveryCheckpointId                String?
  resultRecoverySummaryId                   String?
  actorId                                   String
  actorRole                                 String
  eventType                                 String
  decision                                  String
  safeSummary                               String
  reasonCodesJson                           Json      @default("[]")
  metadataJson                              Json      @default("{}")
  requestId                                 String?
  correlationId                             String?
  createdAt                                 DateTime  @default(now())

  @@index([schoolId], map: "rrc_audit_school")
  @@index([resultRecoveryPlanId], map: "rrc_audit_plan")
  @@index([resultRecoveryObjectiveId], map: "rrc_audit_obj")
  @@index([resultRecoveryStepId], map: "rrc_audit_step")
  @@index([resultRecoveryPracticeDraftId], map: "rrc_audit_pd")
  @@index([resultRecoveryResourceRecommendationId], map: "rrc_audit_rr")
  @@index([resultRecoveryTeacherReviewPacketId], map: "rrc_audit_trp")
  @@index([resultRecoveryStudentSupportDraftId], map: "rrc_audit_ssd")
  @@index([resultRecoveryParentSupportNoteDraftId], map: "rrc_audit_psnd")
  @@index([resultRecoveryCheckpointId], map: "rrc_audit_chk")
  @@index([resultRecoverySummaryId], map: "rrc_audit_sum")
  @@index([eventType], map: "rrc_audit_evt")
  @@index([createdAt], map: "rrc_audit_cat")
}

model ResultRecoveryIdempotencyRecord {
  resultRecoveryIdempotencyId String    @id @default(uuid())
  schoolId                    String
  operation                   String
  idempotencyKey              String
  requestHash                 String
  status                      String    @default("in_progress")
  resourceType                String?
  resourceId                  String?
  safeResultSummary           String?
  createdAt                   DateTime  @default(now())
  updatedAt                   DateTime  @updatedAt
  expiresAt                   DateTime?

  @@unique([schoolId, operation, idempotencyKey], map: "rrc_idem_unique")
  @@index([schoolId], map: "rrc_idem_school")
  @@index([operation], map: "rrc_idem_op")
  @@index([idempotencyKey], map: "rrc_idem_key")
  @@index([status], map: "rrc_idem_status")
  @@index([expiresAt], map: "rrc_idem_expires")
  @@index([createdAt], map: "rrc_idem_cat")
}

// ─── Package 18: Controlled Recovery Progress Observation v1 ──

model RecoveryProgressObservationRecord {
  recoveryProgressObservationId String   @id @default(uuid())
  schoolId                      String
  studentRef                    String
  resultRecoveryPlanId          String
  resultRecoveryObjectiveId     String?
  resultRecoveryStepId          String?
  resultRecoveryCheckpointId    String?
  resultFollowUpCaseId          String?
  recoveryProgressSummaryId     String?
  observationStatus             String   @default("draft")
  observationMode               String
  observationType               String
  observationConfidence         String
  safeObservationSummary        String   @db.Text
  sourceRefsJson                Json     @default("{}")
  observedSignalsJson           Json     @default("{}")
  allowedUseJson                Json     @default("{}")
  blockedUseJson                Json     @default("{}")
  blockedReasonCodesJson        String[] @default([])
  createdByActorId              String
  createdByRole                 String
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt
  recordedAt                    DateTime?
  reviewReadyAt                 DateTime?
  approvedForFutureUseAt        DateTime?
  suppressedAt                  DateTime?
  blockedAt                     DateTime?
  voidedAt                      DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([resultRecoveryCheckpointId])
  @@index([observationStatus])
  @@index([observationType])
  @@index([createdAt])
}

model RecoveryCheckpointEvaluationRecord {
  recoveryCheckpointEvaluationId String   @id @default(uuid())
  schoolId                       String
  studentRef                     String
  resultRecoveryPlanId           String
  resultRecoveryCheckpointId     String
  recoveryProgressObservationId  String?
  evaluationStatus               String   @default("draft")
  evaluationMode                 String
  evaluationResult               String
  safeEvaluationSummary          String   @db.Text
  criteriaRefsJson               Json     @default("{}")
  criteriaResultsJson            Json     @default("{}")
  evidenceRefsJson               Json     @default("{}")
  recommendedNextStateJson       Json     @default("{}")
  blockedReasonCodesJson         String[] @default([])
  createdByActorId               String
  createdByRole                  String
  createdAt                      DateTime @default(now())
  updatedAt                      DateTime @updatedAt
  evaluatedAt                    DateTime?
  reviewReadyAt                  DateTime?
  approvedForFutureUseAt         DateTime?
  suppressedAt                   DateTime?
  blockedAt                      DateTime?
  voidedAt                       DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([resultRecoveryCheckpointId])
  @@index([recoveryProgressObservationId])
  @@index([evaluationStatus])
  @@index([evaluationResult])
  @@index([createdAt])
}

model RecoveryOutcomeEvidenceRecord {
  recoveryOutcomeEvidenceId String   @id @default(uuid())
  schoolId                  String
  studentRef                String
  resultRecoveryPlanId      String
  resultRecoveryObjectiveId String?
  recoveryProgressObservationId String?
  recoveryCheckpointEvaluationId String?
  evidenceStatus            String   @default("draft")
  evidenceType              String
  safeEvidenceSummary       String   @db.Text
  sourceEvidenceRefsJson    Json     @default("{}")
  learningObjectiveRefsJson Json     @default("{}")
  questionRefsJson          Json     @default("{}")
  resourceRefsJson          Json     @default("{}")
  allowedAudienceJson       Json     @default("{}")
  blockedReasonCodesJson    String[] @default([])
  createdByActorId          String
  createdByRole             String
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt
  reviewReadyAt             DateTime?
  approvedForFutureUseAt    DateTime?
  suppressedAt              DateTime?
  voidedAt                  DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([resultRecoveryObjectiveId])
  @@index([recoveryProgressObservationId])
  @@index([recoveryCheckpointEvaluationId])
  @@index([evidenceStatus])
  @@index([createdAt])
}

model RecoveryPlanAdjustmentDraftRecord {
  recoveryPlanAdjustmentDraftId String   @id @default(uuid())
  schoolId                      String
  studentRef                    String
  resultRecoveryPlanId          String
  recoveryCheckpointEvaluationId String?
  recoveryProgressObservationId String?
  adjustmentStatus              String   @default("draft")
  adjustmentType                String
  safeAdjustmentSummary         String   @db.Text
  proposedChangesJson           Json     @default("{}")
  reasonCodesJson               Json     @default("{}")
  teacherReviewNotesJson        Json     @default("{}")
  blockedReasonCodesJson        String[] @default([])
  createdByActorId              String
  createdByRole                 String
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt
  reviewReadyAt                 DateTime?
  approvedForFutureUseAt        DateTime?
  suppressedAt                  DateTime?
  blockedAt                     DateTime?
  voidedAt                      DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([recoveryCheckpointEvaluationId])
  @@index([recoveryProgressObservationId])
  @@index([adjustmentStatus])
  @@index([createdAt])
}

model RecoveryTeacherReviewDecisionRecord {
  recoveryTeacherReviewDecisionId String   @id @default(uuid())
  schoolId                        String
  studentRef                      String
  teacherRef                      String
  resultRecoveryPlanId            String
  recoveryPlanAdjustmentDraftId   String?
  recoveryCheckpointEvaluationId  String?
  recoveryEvidenceRollupId        String?
  decisionStatus                  String   @default("draft")
  decisionType                    String
  safeDecisionSummary             String   @db.Text
  decisionReasonCodesJson         Json     @default("{}")
  approvedFutureUseRefsJson       Json     @default("{}")
  blockedReasonCodesJson          String[] @default([])
  createdByActorId                String
  createdByRole                   String
  createdAt                       DateTime @default(now())
  updatedAt                       DateTime @updatedAt
  reviewedAt                      DateTime?
  approvedForFutureUseAt          DateTime?
  suppressedAt                    DateTime?
  blockedAt                       DateTime?
  voidedAt                        DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, teacherRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([recoveryPlanAdjustmentDraftId])
  @@index([recoveryCheckpointEvaluationId])
  @@index([decisionStatus])
  @@index([createdAt])
}

model RecoveryStudentProgressReflectionDraftRecord {
  recoveryStudentProgressReflectionDraftId String   @id @default(uuid())
  schoolId                                  String
  studentRef                                String
  resultRecoveryPlanId                      String
  recoveryProgressObservationId             String?
  recoveryCheckpointEvaluationId            String?
  draftStatus                               String   @default("draft")
  safeReflectionSummary                     String   @db.Text
  studentReflectionPromptJson               Json     @default("{}")
  scaffoldStepsJson                         Json     @default("{}")
  blockedFieldNamesJson                     String[] @default([])
  blockedReasonCodesJson                    String[] @default([])
  createdByActorId                          String
  createdByRole                             String
  createdAt                                 DateTime @default(now())
  updatedAt                                 DateTime @updatedAt
  reviewReadyAt                             DateTime?
  approvedForFutureUseAt                    DateTime?
  suppressedAt                              DateTime?
  blockedAt                                 DateTime?
  voidedAt                                  DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([recoveryProgressObservationId])
  @@index([recoveryCheckpointEvaluationId])
  @@index([draftStatus])
  @@index([createdAt])
}

model RecoveryParentProgressNoteDraftRecord {
  recoveryParentProgressNoteDraftId String   @id @default(uuid())
  schoolId                          String
  studentRef                        String
  resultRecoveryPlanId              String
  recoveryProgressObservationId     String?
  recoveryCheckpointEvaluationId    String?
  audienceType                      String
  draftStatus                       String   @default("draft")
  safeProgressSummary               String   @db.Text
  parentProgressBodyJson            Json     @default("{}")
  allowedFieldNamesJson             String[] @default([])
  blockedFieldNamesJson             String[] @default([])
  blockedReasonCodesJson            String[] @default([])
  createdByActorId                  String
  createdByRole                     String
  createdAt                         DateTime @default(now())
  updatedAt                         DateTime @updatedAt
  reviewReadyAt                     DateTime?
  approvedForFutureUseAt            DateTime?
  suppressedAt                      DateTime?
  blockedAt                         DateTime?
  voidedAt                          DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([recoveryProgressObservationId])
  @@index([recoveryCheckpointEvaluationId])
  @@index([draftStatus])
  @@index([createdAt])
}

model RecoveryEvidenceRollupRecord {
  recoveryEvidenceRollupId String   @id @default(uuid())
  schoolId                 String
  studentRef               String
  resultRecoveryPlanId     String
  rollupStatus             String   @default("draft")
  rollupScope              String
  safeRollupSummary        String   @db.Text
  observationCountsJson    Json     @default("{}")
  evaluationCountsJson     Json     @default("{}")
  evidenceCountsJson       Json     @default("{}")
  adjustmentCountsJson     Json     @default("{}")
  sourceRefsJson           Json     @default("{}")
  blockedReasonCodesJson   String[] @default([])
  createdByActorId         String
  createdByRole            String
  createdAt                DateTime @default(now())
  updatedAt                DateTime @updatedAt
  refreshedAt              DateTime?
  suppressedAt             DateTime?
  blockedAt                DateTime?
  voidedAt                 DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([rollupScope])
  @@index([createdAt])
}

model RecoveryProgressSummaryRecord {
  recoveryProgressSummaryId         String   @id @default(uuid())
  schoolId                          String
  studentRef                        String?
  teacherRef                        String?
  resultRecoveryPlanId              String?
  summaryScope                      String
  summaryStatus                     String   @default("active")
  safeSummary                       String   @db.Text
  progressStateJson                 Json     @default("{}")
  observationCountsJson             Json     @default("{}")
  checkpointEvaluationCountsJson    Json     @default("{}")
  rollupRefsJson                    Json     @default("{}")
  blockedReasonCodesJson            String[] @default([])
  createdAt                         DateTime @default(now())
  updatedAt                         DateTime @updatedAt
  refreshedAt                       DateTime?
  voidedAt                          DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, teacherRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([summaryScope])
  @@index([summaryStatus])
  @@index([createdAt])
}

model RecoveryProgressAuditRecord {
  recoveryProgressAuditId              String   @id @default(uuid())
  schoolId                             String
  recoveryProgressObservationId        String?
  recoveryCheckpointEvaluationId       String?
  recoveryOutcomeEvidenceId            String?
  recoveryPlanAdjustmentDraftId        String?
  recoveryTeacherReviewDecisionId      String?
  recoveryStudentProgressReflectionDraftId String?
  recoveryParentProgressNoteDraftId    String?
  recoveryEvidenceRollupId             String?
  recoveryProgressSummaryId            String?
  actorId                              String
  actorRole                            String
  eventType                            String
  decision                             String
  safeSummary                          String   @db.Text
  reasonCodesJson                      Json     @default("{}")
  metadataJson                         Json     @default("{}")
  requestId                            String?
  correlationId                        String?
  createdAt                            DateTime @default(now())

  @@index([schoolId])
  @@index([schoolId, eventType])
  @@index([createdAt])
}

model RecoveryProgressIdempotencyRecord {
  recoveryProgressIdempotencyId String   @id @default(uuid())
  schoolId                      String
  operation                     String
  idempotencyKey                String
  requestHash                   String
  status                        String   @default("in_progress")
  resourceType                  String?
  resourceId                    String?
  safeResultSummary             String?
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt
  expiresAt                     DateTime?

  @@unique([schoolId, operation, idempotencyKey])
  @@index([schoolId])
  @@index([operation])
  @@index([idempotencyKey])
  @@index([status])
  @@index([expiresAt])
  @@index([createdAt])
}

// ─── PACKAGE 19: Recovery Outcome Decision Gate ─────────────────────

model RecoveryOutcomeDecisionReadinessRecord {
  recoveryOutcomeDecisionReadinessId String   @id @default(uuid())
  schoolId                           String
  studentRef                         String
  resultRecoveryPlanId               String
  recoveryProgressSummaryId          String?
  recoveryEvidenceRollupId           String?
  readinessStatus                    String   @default("draft")
  safeReadinessSummary               String   @db.Text
  readinessChecksJson                Json     @default("{}")
  blockedReasonCodesJson             String[] @default([])
  sourceRefsJson                     Json     @default("{}")
  createdByActorId                   String
  createdByRole                      String
  createdAt                          DateTime @default(now())
  updatedAt                          DateTime @updatedAt
  reviewReadyAt                      DateTime?
  approvedForFutureUseAt             DateTime?
  suppressedAt                       DateTime?
  blockedAt                          DateTime?
  voidedAt                           DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([readinessStatus])
  @@index([createdAt])
}

model RecoveryExitCriteriaRecord {
  recoveryExitCriteriaId String   @id @default(uuid())
  schoolId               String
  resultRecoveryPlanId   String
  criteriaStatus         String   @default("draft")
  criteriaType           String
  safeCriteriaSummary    String   @db.Text
  criteriaDetailsJson    Json     @default("{}")
  blockedReasonCodesJson String[] @default([])
  createdByActorId       String
  createdByRole          String
  createdAt              DateTime @default(now())
  updatedAt              DateTime @updatedAt
  reviewReadyAt          DateTime?
  approvedForFutureUseAt DateTime?
  suppressedAt           DateTime?
  blockedAt              DateTime?
  voidedAt               DateTime?

  @@index([schoolId])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([criteriaStatus])
  @@index([createdAt])
}

model RecoveryExitCriteriaEvaluationRecord {
  recoveryExitCriteriaEvaluationId String   @id @default(uuid())
  schoolId                         String
  studentRef                       String
  resultRecoveryPlanId             String
  recoveryExitCriteriaId           String
  evaluationStatus                 String   @default("draft")
  evaluationResult                 String
  safeEvaluationSummary            String   @db.Text
  evidenceRefsJson                 Json     @default("{}")
  blockedReasonCodesJson           String[] @default([])
  sourceRefsJson                   Json     @default("{}")
  createdByActorId                 String
  createdByRole                    String
  createdAt                        DateTime @default(now())
  updatedAt                        DateTime @updatedAt
  reviewReadyAt                    DateTime?
  approvedForFutureUseAt           DateTime?
  suppressedAt                     DateTime?
  blockedAt                        DateTime?
  voidedAt                         DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([recoveryExitCriteriaId])
  @@index([evaluationStatus])
  @@index([evaluationResult])
  @@index([createdAt])
}

model RecoveryContinuationDecisionDraftRecord {
  recoveryContinuationDecisionDraftId String   @id @default(uuid())
  schoolId                            String
  studentRef                          String
  resultRecoveryPlanId                String
  recoveryProgressSummaryId           String?
  recoveryEvidenceRollupId            String?
  draftStatus                         String   @default("draft")
  safeDecisionSummary                 String   @db.Text
  rationaleJson                       Json     @default("{}")
  blockedReasonCodesJson              String[] @default([])
  sourceRefsJson                      Json     @default("{}")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  reviewReadyAt                       DateTime?
  approvedForFutureUseAt              DateTime?
  suppressedAt                        DateTime?
  blockedAt                           DateTime?
  voidedAt                            DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([draftStatus])
  @@index([createdAt])
}

model RecoveryIntensificationDecisionDraftRecord {
  recoveryIntensificationDecisionDraftId String   @id @default(uuid())
  schoolId                               String
  studentRef                             String
  resultRecoveryPlanId                   String
  recoveryProgressSummaryId              String?
  recoveryEvidenceRollupId               String?
  draftStatus                            String   @default("draft")
  safeDecisionSummary                    String   @db.Text
  rationaleJson                          Json     @default("{}")
  blockedReasonCodesJson                 String[] @default([])
  sourceRefsJson                         Json     @default("{}")
  createdByActorId                       String
  createdByRole                          String
  createdAt                              DateTime @default(now())
  updatedAt                              DateTime @updatedAt
  reviewReadyAt                          DateTime?
  approvedForFutureUseAt                 DateTime?
  suppressedAt                           DateTime?
  blockedAt                              DateTime?
  voidedAt                               DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([draftStatus])
  @@index([createdAt])
}

model RecoveryPauseDecisionDraftRecord {
  recoveryPauseDecisionDraftId String   @id @default(uuid())
  schoolId                     String
  studentRef                   String
  resultRecoveryPlanId         String
  recoveryProgressSummaryId    String?
  recoveryEvidenceRollupId     String?
  draftStatus                  String   @default("draft")
  safeDecisionSummary          String   @db.Text
  rationaleJson                Json     @default("{}")
  blockedReasonCodesJson       String[] @default([])
  sourceRefsJson               Json     @default("{}")
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  reviewReadyAt                DateTime?
  approvedForFutureUseAt       DateTime?
  suppressedAt                 DateTime?
  blockedAt                    DateTime?
  voidedAt                     DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([draftStatus])
  @@index([createdAt])
}

model RecoveryClosureDecisionDraftRecord {
  recoveryClosureDecisionDraftId String   @id @default(uuid())
  schoolId                       String
  studentRef                     String
  resultRecoveryPlanId           String
  recoveryProgressSummaryId      String?
  recoveryEvidenceRollupId       String?
  draftStatus                    String   @default("draft")
  safeDecisionSummary            String   @db.Text
  closureType                    String   @default("graduation")
  rationaleJson                  Json     @default("{}")
  futureReviewRefsJson           Json     @default("{}")
  blockedReasonCodesJson         String[] @default([])
  sourceRefsJson                 Json     @default("{}")
  createdByActorId               String
  createdByRole                  String
  createdAt                      DateTime @default(now())
  updatedAt                      DateTime @updatedAt
  reviewReadyAt                  DateTime?
  approvedForFutureUseAt         DateTime?
  suppressedAt                   DateTime?
  blockedAt                      DateTime?
  voidedAt                       DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([draftStatus])
  @@index([closureType])
  @@index([createdAt])
}

model RecoveryOutcomeTeacherReviewPacketRecord {
  recoveryOutcomeTeacherReviewPacketId String   @id @default(uuid())
  schoolId                             String
  studentRef                           String
  teacherRef                           String
  resultRecoveryPlanId                 String
  recoveryOutcomeDecisionReadinessId   String?
  recoveryEvidenceRollupId             String?
  recoveryProgressSummaryId            String?
  packetStatus                         String   @default("draft")
  safeReviewPacketSummary              String   @db.Text
  readinessSnapshotJson                Json     @default("{}")
  decisionDraftRefsJson                Json     @default("{}")
  teacherReviewCompleteAt              DateTime?
  reviewNotesJson                      Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  approvedForFutureUseAt               DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, teacherRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([packetStatus])
  @@index([createdAt])
}

model RecoveryOutcomeStudentNextStepDraftRecord {
  recoveryOutcomeStudentNextStepDraftId String   @id @default(uuid())
  schoolId                              String
  studentRef                            String
  resultRecoveryPlanId                  String
  recoveryOutcomeDecisionSummaryId      String?
  draftStatus                           String   @default("draft")
  safeNextStepSummary                   String   @db.Text
  socraticPromptJson                    Json     @default("{}")
  allowedReflectionsJson                Json     @default("{}")
  blockedReasonCodesJson                String[] @default([])
  sourceRefsJson                        Json     @default("{}")
  createdByActorId                      String
  createdByRole                         String
  createdAt                             DateTime @default(now())
  updatedAt                             DateTime @updatedAt
  reviewReadyAt                         DateTime?
  approvedForFutureUseAt                DateTime?
  suppressedAt                          DateTime?
  blockedAt                             DateTime?
  voidedAt                              DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([draftStatus])
  @@index([createdAt])
}

model RecoveryOutcomeParentUpdateDraftRecord {
  recoveryOutcomeParentUpdateDraftId String   @id @default(uuid())
  schoolId                           String
  studentRef                         String
  resultRecoveryPlanId               String
  parentRef                          String
  recoveryOutcomeDecisionSummaryId   String?
  draftStatus                        String   @default("draft")
  safeUpdateSummary                  String   @db.Text
  updateBodyJson                     Json     @default("{}")
  allowedFieldNamesJson              String[] @default([])
  blockedFieldNamesJson              String[] @default([])
  blockedReasonCodesJson             String[] @default([])
  sourceRefsJson                     Json     @default("{}")
  createdByActorId                   String
  createdByRole                      String
  createdAt                          DateTime @default(now())
  updatedAt                          DateTime @updatedAt
  reviewReadyAt                      DateTime?
  approvedForFutureUseAt             DateTime?
  suppressedAt                       DateTime?
  blockedAt                          DateTime?
  voidedAt                           DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([draftStatus])
  @@index([createdAt])
}

model RecoveryOutcomeDecisionSummaryRecord {
  recoveryOutcomeDecisionSummaryId String   @id @default(uuid())
  schoolId                         String
  studentRef                       String?
  teacherRef                       String?
  resultRecoveryPlanId             String?
  recoveryProgressSummaryId        String?
  recoveryEvidenceRollupId         String?
  summaryStatus                    String   @default("active")
  safeSummary                      String   @db.Text
  decisionCountsJson               Json     @default("{}")
  topDecisionsJson                 Json     @default("{}")
  nextActionRefsJson               Json     @default("{}")
  blockedReasonCodesJson           String[] @default([])
  sourceRefsJson                   Json     @default("{}")
  createdByActorId                 String
  createdByRole                    String
  createdAt                        DateTime @default(now())
  updatedAt                        DateTime @updatedAt
  refreshedAt                      DateTime?
  staleAt                          DateTime?
  blockedAt                        DateTime?
  voidedAt                         DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, teacherRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([summaryStatus])
  @@index([createdAt])
}

model RecoveryOutcomeAuditRecord {
  recoveryOutcomeAuditId                String   @id @default(uuid())
  schoolId                              String
  recoveryOutcomeDecisionReadinessId    String?
  recoveryExitCriteriaId                String?
  recoveryExitCriteriaEvaluationId      String?
  recoveryContinuationDecisionDraftId   String?
  recoveryIntensificationDecisionDraftId String?
  recoveryPauseDecisionDraftId          String?
  recoveryClosureDecisionDraftId        String?
  recoveryOutcomeTeacherReviewPacketId  String?
  recoveryOutcomeStudentNextStepDraftId String?
  recoveryOutcomeParentUpdateDraftId    String?
  recoveryOutcomeDecisionSummaryId      String?
  actorId                               String
  actorRole                             String
  eventType                             String
  decision                              String
  safeSummary                           String   @db.Text
  reasonCodesJson                       Json     @default("{}")
  metadataJson                          Json     @default("{}")
  requestId                             String?
  correlationId                         String?
  createdAt                             DateTime @default(now())

  @@index([schoolId])
  @@index([schoolId, eventType])
  @@index([createdAt])
}

model RecoveryOutcomeIdempotencyRecord {
  recoveryOutcomeIdempotencyId String   @id @default(uuid())
  schoolId                     String
  operation                    String
  idempotencyKey               String
  requestHash                  String
  status                       String   @default("in_progress")
  resourceType                 String?
  resourceId                   String?
  safeResultSummary            String?
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  expiresAt                    DateTime?

  @@unique([schoolId, operation, idempotencyKey])
  @@index([schoolId])
  @@index([operation])
  @@index([idempotencyKey])
  @@index([status])
  @@index([expiresAt])
  @@index([createdAt])
}

// ─── Package 20: Recovery Outcome Action Preparation ────────────────

model RecoveryOutcomeActionReadinessRecord {
  actionReadinessId                  String   @id @default(uuid())
  schoolId                           String
  studentRef                         String
  resultRecoveryPlanId               String
  recoveryOutcomeDecisionReadinessId String
  recoveryOutcomeDecisionSummaryId   String?
  readinessStatus                    String   @default("draft")
  safeReadinessSummary               String   @db.Text
  readinessChecksJson                Json     @default("{}")
  blockedReasonCodesJson             String[] @default([])
  sourceRefsJson                     Json     @default("{}")
  createdByActorId                   String
  createdByRole                      String
  createdAt                          DateTime @default(now())
  updatedAt                          DateTime @updatedAt
  reviewReadyAt                      DateTime?
  approvedForFutureUseAt             DateTime?
  suppressedAt                       DateTime?
  blockedAt                          DateTime?
  voidedAt                           DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([readinessStatus])
  @@index([createdAt])
}

model RecoveryOutcomeActionBundleRecord {
  actionBundleId                    String   @id @default(uuid())
  schoolId                          String
  studentRef                        String
  resultRecoveryPlanId              String
  recoveryOutcomeDecisionSummaryId  String?
  bundleStatus                      String   @default("draft")
  safeBundleSummary                 String   @db.Text
  readinessRefsJson                 Json     @default("{}")
  draftRefsJson                     Json     @default("{}")
  bundleType                        String
  blockedReasonCodesJson            String[] @default([])
  sourceRefsJson                    Json     @default("{}")
  createdByActorId                  String
  createdByRole                     String
  createdAt                         DateTime @default(now())
  updatedAt                         DateTime @updatedAt
  reviewReadyAt                     DateTime?
  approvedForFutureUseAt            DateTime?
  suppressedAt                      DateTime?
  blockedAt                         DateTime?
  voidedAt                          DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([bundleStatus])
  @@index([bundleType])
  @@index([createdAt])
}

model RecoveryContinuationActionDraftRecord {
  continuationActionDraftId            String   @id @default(uuid())
  schoolId                             String
  studentRef                           String
  resultRecoveryPlanId                 String
  recoveryContinuationDecisionDraftId  String
  recoveryOutcomeDecisionSummaryId     String?
  draftStatus                          String   @default("draft")
  safeActionSummary                    String   @db.Text
  actionDetailsJson                    Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  approvedForFutureUseAt               DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([draftStatus])
  @@index([createdAt])
}

model RecoveryIntensificationActionDraftRecord {
  intensificationActionDraftId            String   @id @default(uuid())
  schoolId                                String
  studentRef                              String
  resultRecoveryPlanId                    String
  recoveryIntensificationDecisionDraftId  String
  recoveryOutcomeDecisionSummaryId        String?
  draftStatus                             String   @default("draft")
  safeActionSummary                       String   @db.Text
  intensificationDetailsJson              Json     @default("{}")
  blockedReasonCodesJson                  String[] @default([])
  sourceRefsJson                          Json     @default("{}")
  createdByActorId                        String
  createdByRole                           String
  createdAt                               DateTime @default(now())
  updatedAt                               DateTime @updatedAt
  reviewReadyAt                           DateTime?
  approvedForFutureUseAt                  DateTime?
  suppressedAt                            DateTime?
  blockedAt                               DateTime?
  voidedAt                                DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([draftStatus])
  @@index([createdAt])
}

model RecoveryPauseActionDraftRecord {
  pauseActionDraftId              String   @id @default(uuid())
  schoolId                        String
  studentRef                      String
  resultRecoveryPlanId            String
  recoveryPauseDecisionDraftId    String
  recoveryOutcomeDecisionSummaryId String?
  draftStatus                     String   @default("draft")
  safeActionSummary               String   @db.Text
  pauseDetailsJson                Json     @default("{}")
  blockedReasonCodesJson          String[] @default([])
  sourceRefsJson                  Json     @default("{}")
  createdByActorId                String
  createdByRole                   String
  createdAt                       DateTime @default(now())
  updatedAt                       DateTime @updatedAt
  reviewReadyAt                   DateTime?
  approvedForFutureUseAt          DateTime?
  suppressedAt                    DateTime?
  blockedAt                       DateTime?
  voidedAt                        DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([draftStatus])
  @@index([createdAt])
}

model RecoveryClosureActionDraftRecord {
  closureActionDraftId             String   @id @default(uuid())
  schoolId                         String
  studentRef                       String
  resultRecoveryPlanId             String
  recoveryClosureDecisionDraftId   String
  recoveryOutcomeDecisionSummaryId String?
  draftStatus                      String   @default("draft")
  safeActionSummary                String   @db.Text
  closureDetailsJson               Json     @default("{}")
  closureType                      String   @default("graduation")
  blockedReasonCodesJson           String[] @default([])
  sourceRefsJson                   Json     @default("{}")
  createdByActorId                 String
  createdByRole                    String
  createdAt                        DateTime @default(now())
  updatedAt                        DateTime @updatedAt
  reviewReadyAt                    DateTime?
  approvedForFutureUseAt           DateTime?
  suppressedAt                     DateTime?
  blockedAt                        DateTime?
  voidedAt                         DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([draftStatus])
  @@index([closureType])
  @@index([createdAt])
}

model RecoveryOutcomeApprovalGateRecord {
  approvalGateId                   String   @id @default(uuid())
  schoolId                         String
  studentRef                       String
  resultRecoveryPlanId             String
  recoveryOutcomeDecisionSummaryId String?
  gateStatus                       String   @default("pending")
  safeGateSummary                  String   @db.Text
  requiredApprovalsJson            Json     @default("{}")
  approvalResultsJson              Json     @default("{}")
  blockedReasonCodesJson           String[] @default([])
  sourceRefsJson                   Json     @default("{}")
  createdByActorId                 String
  createdByRole                    String
  createdAt                        DateTime @default(now())
  updatedAt                        DateTime @updatedAt
  satisfiedAt                      DateTime?
  blockedAt                        DateTime?
  voidedAt                         DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([gateStatus])
  @@index([createdAt])
}

model RecoveryOutcomeMockActivationQueueRecord {
  mockActivationQueueItemId String   @id @default(uuid())
  schoolId                  String
  studentRef                String
  resultRecoveryPlanId      String
  actionBundleId            String?
  queueStatus               String   @default("draft")
  safeQueueSummary          String   @db.Text
  actionRefsJson            Json     @default("{}")
  mockParametersJson        Json     @default("{}")
  blockedReasonCodesJson    String[] @default([])
  sourceRefsJson            Json     @default("{}")
  createdByActorId          String
  createdByRole             String
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt
  dryRunReadyAt             DateTime?
  suppressedAt              DateTime?
  blockedAt                 DateTime?
  voidedAt                  DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([queueStatus])
  @@index([createdAt])
}

model RecoveryOutcomeDryRunReceiptRecord {
  dryRunReceiptId              String   @id @default(uuid())
  schoolId                     String
  mockActivationQueueItemId    String
  studentRef                   String
  resultRecoveryPlanId         String
  receiptResult                String
  safeReceiptSummary           String   @db.Text
  simulationDetailsJson        Json     @default("{}")
  blockedReasonCodesJson       String[] @default([])
  sourceRefsJson               Json     @default("{}")
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId])
  @@index([mockActivationQueueItemId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([receiptResult])
  @@index([createdAt])
}

model RecoveryOutcomeRollbackPlanRecord {
  rollbackPlanId       String   @id @default(uuid())
  schoolId             String
  studentRef           String
  resultRecoveryPlanId String
  actionBundleId       String?
  rollbackStatus       String   @default("draft")
  safeRollbackSummary  String   @db.Text
  rollbackStepsJson    Json     @default("{}")
  rollbackTriggersJson Json     @default("{}")
  blockedReasonCodesJson String[] @default([])
  sourceRefsJson       Json     @default("{}")
  createdByActorId     String
  createdByRole        String
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt
  reviewReadyAt        DateTime?
  approvedForFutureUseAt DateTime?
  suppressedAt         DateTime?
  blockedAt            DateTime?
  voidedAt             DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([rollbackStatus])
  @@index([createdAt])
}

model RecoveryOutcomeSuppressionRuleRecord {
  suppressionRuleId    String   @id @default(uuid())
  schoolId             String
  studentRef           String
  resultRecoveryPlanId String
  ruleStatus           String   @default("active")
  safeRuleSummary      String   @db.Text
  ruleConditionsJson   Json     @default("{}")
  ruleScopeJson        Json     @default("{}")
  blockedReasonCodesJson String[] @default([])
  sourceRefsJson       Json     @default("{}")
  createdByActorId     String
  createdByRole        String
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt
  activatedForFutureUseAt DateTime?
  suppressedAt         DateTime?
  blockedAt            DateTime?
  voidedAt             DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([ruleStatus])
  @@index([createdAt])
}

model RecoveryOutcomeActionSummaryRecord {
  actionSummaryId      String   @id @default(uuid())
  schoolId             String
  studentRef           String?
  teacherRef           String?
  resultRecoveryPlanId String?
  summaryStatus        String   @default("active")
  safeSummary          String   @db.Text
  actionCountsJson     Json     @default("{}")
  topActionsJson       Json     @default("{}")
  nextStepsJson        Json     @default("{}")
  blockedReasonCodesJson String[] @default([])
  sourceRefsJson       Json     @default("{}")
  createdByActorId     String
  createdByRole        String
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt
  refreshedAt          DateTime?
  staleAt              DateTime?
  blockedAt            DateTime?
  voidedAt             DateTime?

  @@index([schoolId])
  @@index([schoolId, studentRef])
  @@index([schoolId, teacherRef])
  @@index([schoolId, resultRecoveryPlanId])
  @@index([summaryStatus])
  @@index([createdAt])
}

model RecoveryOutcomeActionAuditRecord {
  auditEventId                    String   @id @default(uuid())
  schoolId                        String
  actionReadinessId               String?
  actionBundleId                  String?
  continuationActionDraftId       String?
  intensificationActionDraftId    String?
  pauseActionDraftId              String?
  closureActionDraftId            String?
  approvalGateId                  String?
  mockActivationQueueItemId       String?
  dryRunReceiptId                 String?
  rollbackPlanId                  String?
  suppressionRuleId               String?
  actionSummaryId                 String?
  actorId                         String
  actorRole                       String
  eventType                       String
  decision                        String
  safeSummary                     String   @db.Text
  reasonCodesJson                 Json     @default("{}")
  metadataJson                    Json     @default("{}")
  requestId                       String?
  correlationId                   String?
  createdAt                       DateTime @default(now())

  @@index([schoolId])
  @@index([schoolId, eventType])
  @@index([createdAt])
}

model RecoveryOutcomeActionIdempotencyRecord {
  actionIdempotencyId String   @id @default(uuid())
  schoolId            String
  operation           String
  idempotencyKey      String
  requestHash         String
  status              String   @default("in_progress")
  resourceType        String?
  resourceId          String?
  safeResultSummary   String?
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt
  expiresAt           DateTime?

  @@unique([schoolId, operation, idempotencyKey])
  @@index([schoolId])
  @@index([operation])
  @@index([idempotencyKey])
  @@index([status])
  @@index([expiresAt])
  @@index([createdAt])
}

// ─── Package 21: Recovery Outcome Execution Simulation ────────────────

model RecoveryOutcomeExecutionSimulationReadinessRecord {
  simulationReadinessId               String   @id @default(uuid())
  schoolId                            String
  studentRef                          String
  resultRecoveryPlanId                String
  recoveryOutcomeActionReadinessId    String
  recoveryOutcomeActionBundleId       String?
  readinessStatus                     String   @default("draft")
  safeReadinessSummary                String   @db.Text
  readinessChecksJson                 Json     @default("{}")
  blockedReasonCodesJson              String[] @default([])
  sourceRefsJson                      Json     @default("{}")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  reviewReadyAt                       DateTime?
  approvedForFutureUseAt              DateTime?
  suppressedAt                        DateTime?
  blockedAt                           DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "p21_readiness_schoolId")
  @@index([schoolId, studentRef], map: "p21_readiness_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p21_readiness_sid_plan")
  @@index([readinessStatus], map: "p21_readiness_status")
  @@index([createdAt], map: "p21_readiness_ca")
}

model RecoveryOutcomeExecutionSimulationPlanRecord {
  simulationPlanId                    String   @id @default(uuid())
  schoolId                            String
  studentRef                          String
  resultRecoveryPlanId                String
  recoveryOutcomeActionBundleId       String?
  recoveryContinuationActionDraftId   String?
  recoveryIntensificationActionDraftId String?
  recoveryPauseActionDraftId          String?
  recoveryClosureActionDraftId        String?
  planStatus                          String   @default("draft")
  safePlanSummary                     String   @db.Text
  simulationParametersJson            Json     @default("{}")
  blockedReasonCodesJson              String[] @default([])
  sourceRefsJson                      Json     @default("{}")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  simulationReadyAt                   DateTime?
  reviewReadyAt                       DateTime?
  approvedForFutureUseAt              DateTime?
  suppressedAt                        DateTime?
  blockedAt                           DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "p21_plan_schoolId")
  @@index([schoolId, studentRef], map: "p21_plan_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p21_plan_sid_plan")
  @@index([planStatus], map: "p21_plan_status")
  @@index([createdAt], map: "p21_plan_ca")
}

model RecoveryOutcomeExecutionSimulationRunRecord {
  simulationRunId                     String   @id @default(uuid())
  schoolId                            String
  studentRef                          String
  resultRecoveryPlanId                String
  recoveryOutcomeActionBundleId       String?
  simulationPlanId                    String
  runStatus                           String   @default("draft")
  safeRunSummary                      String   @db.Text
  runParametersJson                   Json     @default("{}")
  blockedReasonCodesJson              String[] @default([])
  sourceRefsJson                      Json     @default("{}")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  simulatingAt                        DateTime?
  simulatedAt                         DateTime?
  reviewReadyAt                       DateTime?
  suppressedAt                        DateTime?
  blockedAt                           DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "p21_run_schoolId")
  @@index([schoolId, studentRef], map: "p21_run_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p21_run_sid_plan")
  @@index([simulationPlanId], map: "p21_run_simplan")
  @@index([runStatus], map: "p21_run_status")
  @@index([createdAt], map: "p21_run_ca")
}

model RecoveryOutcomeExecutionSimulationStepRecord {
  simulationStepId                    String   @id @default(uuid())
  schoolId                            String
  simulationRunId                     String
  stepSequence                        Int
  stepName                            String
  stepStatus                          String   @default("pending")
  safeStepSummary                     String   @db.Text
  stepDetailsJson                     Json     @default("{}")
  blockedReasonCodesJson              String[] @default([])
  sourceRefsJson                      Json     @default("{}")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  simulatedAt                         DateTime?
  blockedAt                           DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "p21_step_schoolId")
  @@index([simulationRunId], map: "p21_step_run")
  @@index([stepStatus], map: "p21_step_status")
  @@index([createdAt], map: "p21_step_ca")
}

model RecoveryOutcomeExecutionEligibilityCheckRecord {
  eligibilityCheckId                  String   @id @default(uuid())
  schoolId                            String
  studentRef                          String
  resultRecoveryPlanId                String
  recoveryOutcomeActionBundleId       String?
  recoveryOutcomeActionReadinessId    String?
  eligibilityStatus                   String   @default("draft")
  safeEligibilitySummary              String   @db.Text
  eligibilityChecksJson               Json     @default("{}")
  blockedReasonCodesJson              String[] @default([])
  sourceRefsJson                      Json     @default("{}")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  reviewReadyAt                       DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "p21_elig_schoolId")
  @@index([schoolId, studentRef], map: "p21_elig_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p21_elig_sid_plan")
  @@index([recoveryOutcomeActionBundleId], map: "p21_elig_bundle")
  @@index([eligibilityStatus], map: "p21_elig_status")
  @@index([createdAt], map: "p21_elig_ca")
}

model RecoveryOutcomeExecutionBlockedActionDiagnosticRecord {
  blockedActionDiagnosticId           String   @id @default(uuid())
  schoolId                            String
  studentRef                          String
  resultRecoveryPlanId                String
  simulationRunId                     String?
  recoveryOutcomeActionBundleId       String?
  diagnosticStatus                    String   @default("draft")
  safeDiagnosticSummary               String   @db.Text
  blockedReasonCodesJson              String[] @default([])
  diagnosticDetailsJson               Json     @default("{}")
  sourceRefsJson                      Json     @default("{}")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  reviewReadyAt                       DateTime?
  suppressedAt                        DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "p21_diag_schoolId")
  @@index([schoolId, studentRef], map: "p21_diag_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p21_diag_sid_plan")
  @@index([simulationRunId], map: "p21_diag_run")
  @@index([diagnosticStatus], map: "p21_diag_status")
  @@index([createdAt], map: "p21_diag_ca")
}

model RecoveryOutcomeExecutionFailureInjectionRecord {
  failureInjectionId                  String   @id @default(uuid())
  schoolId                            String
  studentRef                          String
  resultRecoveryPlanId                String
  simulationPlanId                    String?
  injectionType                       String
  injectionStatus                     String   @default("draft")
  safeInjectionSummary                String   @db.Text
  injectionParametersJson             Json     @default("{}")
  expectedFailureBehaviorJson         Json     @default("{}")
  blockedReasonCodesJson              String[] @default([])
  sourceRefsJson                      Json     @default("{}")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  reviewReadyAt                       DateTime?
  approvedForFutureUseAt              DateTime?
  suppressedAt                        DateTime?
  blockedAt                           DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "p21_fail_schoolId")
  @@index([schoolId, studentRef], map: "p21_fail_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p21_fail_sid_plan")
  @@index([injectionType], map: "p21_fail_type")
  @@index([injectionStatus], map: "p21_fail_status")
  @@index([createdAt], map: "p21_fail_ca")
}

model RecoveryOutcomeExecutionSimulationResultRecord {
  simulationResultId                  String   @id @default(uuid())
  schoolId                            String
  studentRef                          String
  resultRecoveryPlanId                String
  simulationRunId                     String
  outcomeStatus                       String   @default("draft")
  safeOutcomeSummary                  String   @db.Text
  simulationOutcomeDetailsJson        Json     @default("{}")
  blockedReasonCodesJson              String[] @default([])
  sourceRefsJson                      Json     @default("{}")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  reviewReadyAt                       DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "p21_res_schoolId")
  @@index([schoolId, studentRef], map: "p21_res_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p21_res_sid_plan")
  @@index([simulationRunId], map: "p21_res_run")
  @@index([outcomeStatus], map: "p21_res_status")
  @@index([createdAt], map: "p21_res_ca")
}

model RecoveryOutcomeExecutionTeacherReviewRecord {
  teacherSimulationReviewId           String   @id @default(uuid())
  schoolId                            String
  teacherRef                          String
  studentRef                          String
  resultRecoveryPlanId                String
  simulationRunId                     String?
  reviewStatus                        String   @default("draft")
  safeTeacherReviewSummary            String   @db.Text
  teacherReviewNotesJson              Json     @default("{}")
  blockedReasonCodesJson              String[] @default([])
  sourceRefsJson                      Json     @default("{}")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  reviewReadyAt                       DateTime?
  approvedForFutureUseAt              DateTime?
  suppressedAt                        DateTime?
  blockedAt                           DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "p21_tcrev_schoolId")
  @@index([teacherRef], map: "p21_tcrev_tref")
  @@index([schoolId, studentRef], map: "p21_tcrev_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p21_tcrev_sid_plan")
  @@index([simulationRunId], map: "p21_tcrev_run")
  @@index([reviewStatus], map: "p21_tcrev_status")
  @@index([createdAt], map: "p21_tcrev_ca")
}

model RecoveryOutcomeExecutionStudentPreviewDraftRecord {
  studentPreviewDraftId               String   @id @default(uuid())
  schoolId                            String
  studentRef                          String
  resultRecoveryPlanId                String
  simulationRunId                     String?
  draftStatus                         String   @default("draft")
  safeStudentPreviewSummary           String   @db.Text
  previewContentJson                  Json     @default("{}")
  blockedReasonCodesJson              String[] @default([])
  sourceRefsJson                      Json     @default("{}")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  reviewReadyAt                       DateTime?
  approvedForFutureUseAt              DateTime?
  suppressedAt                        DateTime?
  blockedAt                           DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "p21_sprev_schoolId")
  @@index([studentRef], map: "p21_sprev_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p21_sprev_sid_plan")
  @@index([simulationRunId], map: "p21_sprev_run")
  @@index([draftStatus], map: "p21_sprev_status")
  @@index([createdAt], map: "p21_sprev_ca")
}

model RecoveryOutcomeExecutionParentPreviewDraftRecord {
  parentPreviewDraftId                String   @id @default(uuid())
  schoolId                            String
  studentRef                          String
  resultRecoveryPlanId                String
  simulationRunId                     String?
  draftStatus                         String   @default("draft")
  safeParentPreviewSummary            String   @db.Text
  previewContentJson                  Json     @default("{}")
  blockedReasonCodesJson              String[] @default([])
  sourceRefsJson                      Json     @default("{}")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  reviewReadyAt                       DateTime?
  approvedForFutureUseAt              DateTime?
  suppressedAt                        DateTime?
  blockedAt                           DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "p21_pprev_schoolId")
  @@index([studentRef], map: "p21_pprev_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p21_pprev_sid_plan")
  @@index([simulationRunId], map: "p21_pprev_run")
  @@index([draftStatus], map: "p21_pprev_status")
  @@index([createdAt], map: "p21_pprev_ca")
}

model RecoveryOutcomeExecutionReadinessVerdictRecord {
  readinessVerdictId                  String   @id @default(uuid())
  schoolId                            String
  studentRef                          String
  resultRecoveryPlanId                String
  simulationRunId                     String?
  verdictStatus                       String   @default("draft")
  safeVerdictSummary                  String   @db.Text
  verdictDetailsJson                  Json     @default("{}")
  blockedReasonCodesJson              String[] @default([])
  sourceRefsJson                      Json     @default("{}")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  reviewReadyAt                       DateTime?
  approvedForFutureUseAt              DateTime?
  suppressedAt                        DateTime?
  blockedAt                           DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "p21_vdict_schoolId")
  @@index([schoolId, studentRef], map: "p21_vdict_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p21_vdict_sid_plan")
  @@index([simulationRunId], map: "p21_vdict_run")
  @@index([verdictStatus], map: "p21_vdict_status")
  @@index([createdAt], map: "p21_vdict_ca")
}

model RecoveryOutcomeExecutionSimulationSummaryRecord {
  simulationSummaryId                 String   @id @default(uuid())
  schoolId                            String
  studentRef                          String?
  teacherRef                          String?
  resultRecoveryPlanId                String?
  summaryStatus                       String   @default("active")
  safeSummary                         String   @db.Text
  simulationCountsJson                Json     @default("{}")
  topFindingsJson                     Json     @default("{}")
  nextStepsJson                       Json     @default("{}")
  blockedReasonCodesJson              String[] @default([])
  sourceRefsJson                      Json     @default("{}")
  createdByActorId                    String
  createdByRole                       String
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  refreshedAt                         DateTime?
  staleAt                             DateTime?
  blockedAt                           DateTime?
  voidedAt                            DateTime?

  @@index([schoolId], map: "p21_summary_schoolId")
  @@index([schoolId, studentRef], map: "p21_summary_sid_sref")
  @@index([schoolId, teacherRef], map: "p21_summary_sid_tref")
  @@index([schoolId, resultRecoveryPlanId], map: "p21_summary_sid_plan")
  @@index([summaryStatus], map: "p21_summary_status")
  @@index([createdAt], map: "p21_summary_ca")
}

model RecoveryOutcomeExecutionSimulationAuditRecord {
  simulationAuditEventId              String   @id @default(uuid())
  schoolId                            String
  simulationReadinessId               String?
  simulationPlanId                    String?
  simulationRunId                     String?
  eligibilityCheckId                  String?
  blockedActionDiagnosticId           String?
  failureInjectionId                  String?
  simulationResultId                  String?
  teacherSimulationReviewId           String?
  studentPreviewDraftId               String?
  parentPreviewDraftId                String?
  readinessVerdictId                  String?
  simulationSummaryId                 String?
  actorId                             String
  actorRole                           String
  eventType                           String
  decision                            String
  safeSummary                         String   @db.Text
  reasonCodesJson                     Json     @default("{}")
  metadataJson                        Json     @default("{}")
  requestId                           String?
  correlationId                       String?
  createdAt                           DateTime @default(now())

  @@index([schoolId], map: "p21_audit_schoolId")
  @@index([schoolId, eventType], map: "p21_audit_sid_evt")
  @@index([createdAt], map: "p21_audit_ca")
}

model RecoveryOutcomeExecutionSimulationIdempotencyRecord {
  simulationIdempotencyId             String   @id @default(uuid())
  schoolId                            String
  operation                           String
  idempotencyKey                      String
  requestHash                         String
  status                              String   @default("in_progress")
  resourceType                        String?
  resourceId                          String?
  safeResultSummary                   String?
  createdAt                           DateTime @default(now())
  updatedAt                           DateTime @updatedAt
  expiresAt                           DateTime?

  @@unique([schoolId, operation, idempotencyKey], map: "p21_idem_uid")
  @@index([schoolId], map: "p21_idem_schoolId")
  @@index([operation], map: "p21_idem_op")
  @@index([idempotencyKey], map: "p21_idem_ik")
  @@index([status], map: "p21_idem_status")
  @@index([expiresAt], map: "p21_idem_exp")
  @@index([createdAt], map: "p21_idem_ca")
}

// ─── Package 22: Recovery Lifecycle Closure Readiness ────────────────

model RecoveryLifecycleClosureReadinessRecord {
  closureReadinessId                   String   @id @default(uuid())
  schoolId                             String
  studentRef                           String
  resultRecoveryPlanId                 String
  recoveryOutcomeExecutionSimulationReadinessId String?
  recoveryOutcomeExecutionSimulationPlanId      String?
  recoveryOutcomeExecutionSimulationRunId       String?
  recoveryOutcomeExecutionSimulationResultId    String?
  recoveryOutcomeExecutionSimulationSummaryId   String?
  closureReadinessStatus               String   @default("draft")
  safeReadinessSummary                 String   @db.Text
  readinessChecksJson                  Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  readyForFutureUseAt                  DateTime?
  approvedForFutureUseAt               DateTime?
  blockedAt                            DateTime?
  suppressedAt                         DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p22_clsrdy_schoolId")
  @@index([schoolId, studentRef], map: "p22_clsrdy_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p22_clsrdy_sid_plan")
  @@index([recoveryOutcomeExecutionSimulationReadinessId], map: "p22_clsrdy_simread")
  @@index([recoveryOutcomeExecutionSimulationPlanId], map: "p22_clsrdy_simplan")
  @@index([recoveryOutcomeExecutionSimulationRunId], map: "p22_clsrdy_simrun")
  @@index([recoveryOutcomeExecutionSimulationResultId], map: "p22_clsrdy_simres")
  @@index([recoveryOutcomeExecutionSimulationSummaryId], map: "p22_clsrdy_simsum")
  @@index([closureReadinessStatus], map: "p22_clsrdy_status")
  @@index([createdAt], map: "p22_clsrdy_ca")
}

model RecoveryPostSimulationHandoffPacketRecord {
  handoffPacketId                      String   @id @default(uuid())
  schoolId                             String
  studentRef                           String
  resultRecoveryPlanId                 String
  recoveryOutcomeExecutionSimulationRunId      String?
  recoveryOutcomeExecutionSimulationResultId   String?
  recoveryOutcomeActionBundleId        String?
  handoffStatus                        String   @default("draft")
  safeHandoffSummary                   String   @db.Text
  handoffContentsJson                  Json     @default("{}")
  nextStepsJson                        Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  handoffReadyAt                       DateTime?
  approvedForFutureUseAt               DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p22_handoff_schoolId")
  @@index([schoolId, studentRef], map: "p22_handoff_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p22_handoff_sid_plan")
  @@index([recoveryOutcomeExecutionSimulationRunId], map: "p22_handoff_simrun")
  @@index([recoveryOutcomeExecutionSimulationResultId], map: "p22_handoff_simres")
  @@index([recoveryOutcomeActionBundleId], map: "p22_handoff_bundle")
  @@index([handoffStatus], map: "p22_handoff_status")
  @@index([createdAt], map: "p22_handoff_ca")
}

model RecoveryNextCycleRecommendationDraftRecord {
  nextCycleRecommendationId            String   @id @default(uuid())
  schoolId                             String
  studentRef                           String
  resultRecoveryPlanId                 String
  recoveryOutcomeExecutionSimulationSummaryId  String?
  recommendationType                   String
  recommendationStatus                 String   @default("draft")
  safeRecommendationSummary            String   @db.Text
  recommendationDetailsJson            Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  approvedForFutureUseAt               DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p22_ncrec_schoolId")
  @@index([schoolId, studentRef], map: "p22_ncrec_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p22_ncrec_sid_plan")
  @@index([recoveryOutcomeExecutionSimulationSummaryId], map: "p22_ncrec_simsum")
  @@index([recommendationType], map: "p22_ncrec_type")
  @@index([recommendationStatus], map: "p22_ncrec_status")
  @@index([createdAt], map: "p22_ncrec_ca")
}

model RecoveryDeferredIntegrationTicketRecord {
  deferredIntegrationTicketId          String   @id @default(uuid())
  schoolId                             String
  resultRecoveryPlanId                 String
  ticketType                           String
  ticketStatus                         String   @default("draft")
  safeTicketSummary                    String   @db.Text
  ticketDetailsJson                    Json     @default("{}")
  priority                             Int?
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  approvedForFutureUseAt               DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p22_deftkt_schoolId")
  @@index([schoolId, resultRecoveryPlanId], map: "p22_deftkt_sid_plan")
  @@index([ticketType], map: "p22_deftkt_type")
  @@index([ticketStatus], map: "p22_deftkt_status")
  @@index([priority], map: "p22_deftkt_pri")
  @@index([createdAt], map: "p22_deftkt_ca")
}

model RecoveryUnresolvedRiskRegisterRecord {
  unresolvedRiskRegisterId             String   @id @default(uuid())
  schoolId                             String
  resultRecoveryPlanId                 String
  riskLevel                            String
  riskStatus                           String   @default("draft")
  safeRiskSummary                      String   @db.Text
  riskDetailsJson                      Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p22_unrsk_schoolId")
  @@index([schoolId, resultRecoveryPlanId], map: "p22_unrsk_sid_plan")
  @@index([riskLevel], map: "p22_unrsk_level")
  @@index([riskStatus], map: "p22_unrsk_status")
  @@index([createdAt], map: "p22_unrsk_ca")
}

model RecoveryTeacherClosureReviewPacketRecord {
  teacherClosureReviewPacketId         String   @id @default(uuid())
  schoolId                             String
  teacherRef                           String
  studentRef                           String
  resultRecoveryPlanId                 String
  recoveryOutcomeExecutionSimulationSummaryId  String?
  reviewStatus                         String   @default("draft")
  safeTeacherReviewSummary             String   @db.Text
  teacherReviewNotesJson               Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  approvedForFutureUseAt               DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p22_tcprev_schoolId")
  @@index([teacherRef], map: "p22_tcprev_tref")
  @@index([schoolId, studentRef], map: "p22_tcprev_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p22_tcprev_sid_plan")
  @@index([recoveryOutcomeExecutionSimulationSummaryId], map: "p22_tcprev_simsum")
  @@index([reviewStatus], map: "p22_tcprev_status")
  @@index([createdAt], map: "p22_tcprev_ca")
}

model RecoveryAdminGovernanceReviewPacketRecord {
  adminGovernanceReviewPacketId        String   @id @default(uuid())
  schoolId                             String
  adminRef                             String
  studentRef                           String
  resultRecoveryPlanId                 String
  recoveryOutcomeExecutionSimulationSummaryId  String?
  reviewStatus                         String   @default("draft")
  safeAdminReviewSummary               String   @db.Text
  governanceReviewNotesJson            Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  approvedForFutureUseAt               DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p22_acprev_schoolId")
  @@index([adminRef], map: "p22_acprev_aref")
  @@index([schoolId, studentRef], map: "p22_acprev_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p22_acprev_sid_plan")
  @@index([recoveryOutcomeExecutionSimulationSummaryId], map: "p22_acprev_simsum")
  @@index([reviewStatus], map: "p22_acprev_status")
  @@index([createdAt], map: "p22_acprev_ca")
}

model RecoveryStudentClosureReflectionDraftRecord {
  studentClosureReflectionDraftId      String   @id @default(uuid())
  schoolId                             String
  studentRef                           String
  resultRecoveryPlanId                 String
  recoveryOutcomeExecutionSimulationRunId      String?
  draftStatus                          String   @default("draft")
  safeStudentReflectionSummary         String   @db.Text
  reflectionContentJson                Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  approvedForFutureUseAt               DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p22_screfl_schoolId")
  @@index([studentRef], map: "p22_screfl_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p22_screfl_sid_plan")
  @@index([recoveryOutcomeExecutionSimulationRunId], map: "p22_screfl_simrun")
  @@index([draftStatus], map: "p22_screfl_status")
  @@index([createdAt], map: "p22_screfl_ca")
}

model RecoveryParentClosureGuidanceDraftRecord {
  parentClosureGuidanceDraftId         String   @id @default(uuid())
  schoolId                             String
  studentRef                           String
  resultRecoveryPlanId                 String
  recoveryOutcomeExecutionSimulationRunId      String?
  draftStatus                          String   @default("draft")
  safeParentGuidanceSummary            String   @db.Text
  guidanceContentJson                  Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  approvedForFutureUseAt               DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p22_pcguid_schoolId")
  @@index([studentRef], map: "p22_pcguid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p22_pcguid_sid_plan")
  @@index([recoveryOutcomeExecutionSimulationRunId], map: "p22_pcguid_simrun")
  @@index([draftStatus], map: "p22_pcguid_status")
  @@index([createdAt], map: "p22_pcguid_ca")
}

model RecoveryArchiveManifestRecord {
  archiveManifestId                    String   @id @default(uuid())
  schoolId                             String
  studentRef                           String
  resultRecoveryPlanId                 String
  manifestStatus                       String   @default("draft")
  safeManifestSummary                  String   @db.Text
  manifestContentsJson                 Json     @default("{}")
  recordCountsJson                     Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  archiveReadyAt                       DateTime?
  approvedForFutureUseAt               DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p22_arch_schoolId")
  @@index([studentRef], map: "p22_arch_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p22_arch_sid_plan")
  @@index([manifestStatus], map: "p22_arch_status")
  @@index([createdAt], map: "p22_arch_ca")
}

model RecoveryFinalLifecycleSummaryRecord {
  finalLifecycleSummaryId              String   @id @default(uuid())
  schoolId                             String
  studentRef                           String
  resultRecoveryPlanId                 String
  recoveryOutcomeExecutionSimulationSummaryId  String?
  summaryStatus                        String   @default("draft")
  safeSummary                          String   @db.Text
  lifecycleOverviewJson                Json     @default("{}")
  outcomesJson                         Json     @default("{}")
  nextStepsJson                        Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  refreshedAt                          DateTime?
  staleAt                              DateTime?
  reviewReadyAt                        DateTime?
  approvedForFutureUseAt               DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p22_final_schoolId")
  @@index([schoolId, studentRef], map: "p22_final_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p22_final_sid_plan")
  @@index([recoveryOutcomeExecutionSimulationSummaryId], map: "p22_final_simsum")
  @@index([summaryStatus], map: "p22_final_status")
  @@index([createdAt], map: "p22_final_ca")
}

model RecoveryLifecycleClosureAuditRecord {
  closureAuditEventId                  String   @id @default(uuid())
  schoolId                             String
  closureReadinessId                   String?
  handoffPacketId                      String?
  nextCycleRecommendationId            String?
  deferredIntegrationTicketId          String?
  unresolvedRiskRegisterId             String?
  teacherClosureReviewPacketId         String?
  adminGovernanceReviewPacketId        String?
  studentClosureReflectionDraftId      String?
  parentClosureGuidanceDraftId         String?
  archiveManifestId                    String?
  finalLifecycleSummaryId              String?
  actorId                              String
  actorRole                            String
  eventType                            String
  decision                             String
  safeSummary                          String   @db.Text
  reasonCodesJson                      Json     @default("{}")
  metadataJson                         Json     @default("{}")
  requestId                            String?
  correlationId                        String?
  createdAt                            DateTime @default(now())

  @@index([schoolId], map: "p22_caud_schoolId")
  @@index([schoolId, eventType], map: "p22_caud_sid_evt")
  @@index([createdAt], map: "p22_caud_ca")
}

model RecoveryLifecycleClosureIdempotencyRecord {
  closureIdempotencyId                 String   @id @default(uuid())
  schoolId                             String
  operation                            String
  idempotencyKey                       String
  requestHash                          String
  status                               String   @default("in_progress")
  resourceType                         String?
  resourceId                           String?
  safeResultSummary                    String?
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  expiresAt                            DateTime?

  @@unique([schoolId, operation, idempotencyKey], map: "p22_cidem_uid")
  @@index([schoolId], map: "p22_cidem_schoolId")
  @@index([operation], map: "p22_cidem_op")
  @@index([idempotencyKey], map: "p22_cidem_ik")
  @@index([status], map: "p22_cidem_status")
  @@index([expiresAt], map: "p22_cidem_exp")
  @@index([createdAt], map: "p22_cidem_ca")
}

model RecoveryExecutionAuthorizationPreviewReadinessRecord {
  authorizationReadinessId             String   @id @default(uuid())
  schoolId                             String
  studentRef                           String?
  teacherRef                           String?
  adminRef                             String?
  departmentHeadRef                    String?
  resultRecoveryPlanId                 String?
  recoveryLifecycleClosureReadinessId  String?
  recoveryPostSimulationHandoffPacketId String?
  recoveryFinalLifecycleSummaryId      String?
  recoveryArchiveManifestId            String?
  recoveryOutcomeExecutionSimulationRunId String?
  recoveryOutcomeExecutionReadinessVerdictId String?
  recoveryOutcomeActionBundleId        String?
  status                               String   @default("draft")
  safeSummary                          String   @db.Text
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  authorizationPreviewReadyAt          DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p23_rdy_schoolId")
  @@index([schoolId, studentRef], map: "p23_rdy_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p23_rdy_sid_plan")
  @@index([recoveryLifecycleClosureReadinessId], map: "p23_rdy_clsrdy")
  @@index([status], map: "p23_rdy_status")
  @@index([createdAt], map: "p23_rdy_ca")
}

model RecoveryExecutionAuthorizationRequestDraftRecord {
  authorizationRequestDraftId          String   @id @default(uuid())
  schoolId                             String
  studentRef                           String?
  teacherRef                           String?
  resultRecoveryPlanId                 String?
  recoveryLifecycleClosureReadinessId  String?
  recoveryPostSimulationHandoffPacketId String?
  recoveryFinalLifecycleSummaryId      String?
  requestStatus                        String   @default("draft")
  safeRequestSummary                   String   @db.Text
  requestedActionsJson                 Json     @default("{}")
  requestedApproversJson               Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  authorizationPreviewReadyAt          DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p23_req_schoolId")
  @@index([schoolId, studentRef], map: "p23_req_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p23_req_sid_plan")
  @@index([recoveryLifecycleClosureReadinessId], map: "p23_req_clsrdy")
  @@index([requestStatus], map: "p23_req_status")
  @@index([createdAt], map: "p23_req_ca")
}

model RecoveryExecutionAuthorizationEligibilityCheckRecord {
  authorizationEligibilityCheckId      String   @id @default(uuid())
  schoolId                             String
  studentRef                           String?
  resultRecoveryPlanId                 String?
  recoveryLifecycleClosureReadinessId  String?
  recoveryOutcomeActionBundleId        String?
  decision                             String   @default("draft")
  safeEligibilitySummary               String   @db.Text
  eligibilityDetailsJson               Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p23_elig_schoolId")
  @@index([schoolId, resultRecoveryPlanId], map: "p23_elig_sid_plan")
  @@index([recoveryLifecycleClosureReadinessId], map: "p23_elig_clsrdy")
  @@index([decision], map: "p23_elig_decision")
  @@index([createdAt], map: "p23_elig_ca")
}

model RecoveryExecutionAuthorityMatrixSnapshotRecord {
  authorityMatrixSnapshotId            String   @id @default(uuid())
  schoolId                             String
  resultRecoveryPlanId                 String?
  snapshotStatus                       String   @default("draft")
  safeMatrixSummary                    String   @db.Text
  authorityMatrixJson                  Json     @default("{}")
  rolePermissionsJson                  Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  approvalChainReadyAt                 DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p23_matx_schoolId")
  @@index([schoolId, resultRecoveryPlanId], map: "p23_matx_sid_plan")
  @@index([snapshotStatus], map: "p23_matx_status")
  @@index([createdAt], map: "p23_matx_ca")
}

model RecoveryExecutionApprovalChainDraftRecord {
  approvalChainDraftId                 String   @id @default(uuid())
  schoolId                             String
  resultRecoveryPlanId                 String?
  recoveryLifecycleClosureReadinessId  String?
  recoveryAuthorityMatrixSnapshotId    String?
  chainStatus                          String   @default("draft")
  safeChainSummary                     String   @db.Text
  approvalChainJson                    Json     @default("{}")
  approverRefsJson                     Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  approvalChainReadyAt                 DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p23_achn_schoolId")
  @@index([schoolId, resultRecoveryPlanId], map: "p23_achn_sid_plan")
  @@index([recoveryLifecycleClosureReadinessId], map: "p23_achn_clsrdy")
  @@index([chainStatus], map: "p23_achn_status")
  @@index([createdAt], map: "p23_achn_ca")
}

model RecoveryExecutionRiskAttestationRecord {
  riskAttestationId                    String   @id @default(uuid())
  schoolId                             String
  resultRecoveryPlanId                 String?
  recoveryLifecycleClosureReadinessId  String?
  recoveryApprovalChainDraftId         String?
  riskLevel                            String
  attestationStatus                    String   @default("draft")
  safeAttestationSummary               String   @db.Text
  riskDetailsJson                      Json     @default("{}")
  mitigationsJson                      Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  attestorActorId                      String?
  attestorRole                         String?
  attestedAt                           DateTime?
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  riskAttestedAt                       DateTime?
  vetoedAt                             DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p23_ratt_schoolId")
  @@index([schoolId, resultRecoveryPlanId], map: "p23_ratt_sid_plan")
  @@index([recoveryLifecycleClosureReadinessId], map: "p23_ratt_clsrdy")
  @@index([riskLevel], map: "p23_ratt_level")
  @@index([attestationStatus], map: "p23_ratt_status")
  @@index([attestorActorId], map: "p23_ratt_actor")
  @@index([createdAt], map: "p23_ratt_ca")
}

model RecoveryExecutionConsentBoundaryCheckRecord {
  consentBoundaryCheckId               String   @id @default(uuid())
  schoolId                             String
  studentRef                           String?
  resultRecoveryPlanId                 String?
  recoveryLifecycleClosureReadinessId  String?
  decision                             String   @default("draft")
  safeConsentSummary                   String   @db.Text
  consentBoundaryDetailsJson           Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p23_cbnd_schoolId")
  @@index([schoolId, studentRef], map: "p23_cbnd_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p23_cbnd_sid_plan")
  @@index([recoveryLifecycleClosureReadinessId], map: "p23_cbnd_clsrdy")
  @@index([decision], map: "p23_cbnd_decision")
  @@index([createdAt], map: "p23_cbnd_ca")
}

model RecoveryExecutionVetoRecord {
  vetoId                               String   @id @default(uuid())
  schoolId                             String
  resultRecoveryPlanId                 String?
  recoveryLifecycleClosureReadinessId  String?
  recoveryRiskAttestationId            String?
  vetoReason                           String
  vetoStatus                           String   @default("draft")
  safeVetoSummary                      String   @db.Text
  vetoDetailsJson                      Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  vetoActorId                          String?
  vetoActorRole                        String?
  vetoedAt                             DateTime?
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  suppressedAt                         DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p23_veto_schoolId")
  @@index([schoolId, resultRecoveryPlanId], map: "p23_veto_sid_plan")
  @@index([recoveryLifecycleClosureReadinessId], map: "p23_veto_clsrdy")
  @@index([vetoReason], map: "p23_veto_reason")
  @@index([vetoStatus], map: "p23_veto_status")
  @@index([vetoActorId], map: "p23_veto_actor")
  @@index([createdAt], map: "p23_veto_ca")
}

model RecoveryExecutionPreflightChecklistRecord {
  preflightChecklistId                 String   @id @default(uuid())
  schoolId                             String
  resultRecoveryPlanId                 String?
  recoveryLifecycleClosureReadinessId  String?
  checklistStatus                      String   @default("draft")
  safeChecklistSummary                 String   @db.Text
  checklistItemsJson                   Json     @default("{}")
  passedItemsJson                      Json     @default("{}")
  failedItemsJson                      Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  authorizationPreviewReadyAt          DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p23_pflt_schoolId")
  @@index([schoolId, resultRecoveryPlanId], map: "p23_pflt_sid_plan")
  @@index([recoveryLifecycleClosureReadinessId], map: "p23_pflt_clsrdy")
  @@index([checklistStatus], map: "p23_pflt_status")
  @@index([createdAt], map: "p23_pflt_ca")
}

model RecoveryExecutionAuthorizationDryRunRecord {
  authorizationDryRunId                String   @id @default(uuid())
  schoolId                             String
  resultRecoveryPlanId                 String?
  recoveryLifecycleClosureReadinessId  String?
  dryRunDecision                        String   @default("draft")
  safeDryRunSummary                    String   @db.Text
  dryRunDetailsJson                    Json     @default("{}")
  mockApprovalsJson                    Json     @default("{}")
  mockDenialsJson                      Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  mockAuthorizedAt                     DateTime?
  mockDeniedAt                         DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p23_drn_schoolId")
  @@index([schoolId, resultRecoveryPlanId], map: "p23_drn_sid_plan")
  @@index([recoveryLifecycleClosureReadinessId], map: "p23_drn_clsrdy")
  @@index([dryRunDecision], map: "p23_drn_decision")
  @@index([createdAt], map: "p23_drn_ca")
}

model RecoveryExecutionPreLiveDecisionPacketRecord {
  preLiveDecisionPacketId              String   @id @default(uuid())
  schoolId                             String
  resultRecoveryPlanId                 String?
  recoveryLifecycleClosureReadinessId  String?
  recoveryAuthorizationDryRunId        String?
  packetStatus                         String   @default("draft")
  safePacketSummary                    String   @db.Text
  decisionPacketJson                   Json     @default("{}")
  authorizationChainJson               Json     @default("{}")
  riskSummaryJson                      Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  reviewReadyAt                        DateTime?
  authorizationPreviewReadyAt          DateTime?
  suppressedAt                         DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p23_pkt_schoolId")
  @@index([schoolId, resultRecoveryPlanId], map: "p23_pkt_sid_plan")
  @@index([recoveryLifecycleClosureReadinessId], map: "p23_pkt_clsrdy")
  @@index([recoveryAuthorizationDryRunId], map: "p23_pkt_drn")
  @@index([packetStatus], map: "p23_pkt_status")
  @@index([createdAt], map: "p23_pkt_ca")
}

model RecoveryExecutionMockAuthorizationReceiptRecord {
  mockAuthorizationReceiptId           String   @id @default(uuid())
  schoolId                             String
  resultRecoveryPlanId                 String?
  recoveryLifecycleClosureReadinessId  String?
  recoveryAuthorizationDryRunId        String?
  receiptDecision                      String   @default("draft")
  safeReceiptSummary                   String   @db.Text
  receiptContentsJson                  Json     @default("{}")
  mockAuthorizationDetailsJson         Json     @default("{}")
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  voidedAt                             DateTime?

  @@index([schoolId], map: "p23_recipt_schoolId")
  @@index([schoolId, resultRecoveryPlanId], map: "p23_recipt_sid_plan")
  @@index([recoveryLifecycleClosureReadinessId], map: "p23_recipt_clsrdy")
  @@index([recoveryAuthorizationDryRunId], map: "p23_recipt_drn")
  @@index([receiptDecision], map: "p23_recipt_decision")
  @@index([createdAt], map: "p23_recipt_ca")
}

model RecoveryExecutionAuthorizationSummaryRecord {
  authorizationSummaryId               String   @id @default(uuid())
  schoolId                             String
  studentRef                           String?
  resultRecoveryPlanId                 String?
  summaryStatus                        String   @default("draft")
  safeSummary                          String   @db.Text
  authorizationOverviewJson            Json     @default("{}")
  readinessSummaryJson                 Json     @default("{}")
  requestSummaryJson                   Json     @default("{}")
  eligibilitySummaryJson               Json     @default("{}")
  authorityMatrixSummaryJson           Json     @default("{}")
  approvalChainSummaryJson             Json     @default("{}")
  riskAttestationSummaryJson           Json     @default("{}")
  consentBoundarySummaryJson           Json     @default("{}")
  vetoSummaryJson                      Json     @default("{}")
  preflightSummaryJson                 Json     @default("{}")
  dryRunSummaryJson                    Json     @default("{}")
  preLiveDecisionPacketSummaryJson     Json     @default("{}")
  mockAuthorizationReceiptSummaryJson  Json     @default("{}")
  blockedReasonCodesJson               String[] @default([])
  sourceRefsJson                       Json     @default("{}")
  createdByActorId                     String
  createdByRole                        String
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  refreshedAt                          DateTime?
  staleAt                              DateTime?
  reviewReadyAt                        DateTime?
  blockedAt                            DateTime?
  voidedAt                             DateTime?

  @@index([schoolId], map: "p23_sum_schoolId")
  @@index([schoolId, studentRef], map: "p23_sum_sid_sref")
  @@index([schoolId, resultRecoveryPlanId], map: "p23_sum_sid_plan")
  @@index([summaryStatus], map: "p23_sum_status")
  @@index([createdAt], map: "p23_sum_ca")
}

model RecoveryExecutionAuthorizationAuditRecord {
  authorizationAuditEventId            String   @id @default(uuid())
  schoolId                             String
  authorizationReadinessId             String?
  authorizationRequestDraftId          String?
  authorizationEligibilityCheckId      String?
  authorityMatrixSnapshotId            String?
  approvalChainDraftId                 String?
  riskAttestationId                    String?
  consentBoundaryCheckId               String?
  vetoId                               String?
  preflightChecklistId                 String?
  authorizationDryRunId                String?
  preLiveDecisionPacketId              String?
  mockAuthorizationReceiptId           String?
  authorizationSummaryId               String?
  actorId                              String
  actorRole                            String
  eventType                            String
  decision                             String
  safeSummary                          String   @db.Text
  reasonCodesJson                      Json     @default("{}")
  metadataJson                         Json     @default("{}")
  requestId                            String?
  correlationId                        String?
  createdAt                            DateTime @default(now())

  @@index([schoolId], map: "p23_aud_schoolId")
  @@index([schoolId, eventType], map: "p23_aud_sid_evt")
  @@index([createdAt], map: "p23_aud_ca")
}

model RecoveryExecutionAuthorizationIdempotencyRecord {
  authorizationIdempotencyId           String   @id @default(uuid())
  schoolId                             String
  operation                            String
  idempotencyKey                       String
  requestHash                          String
  status                               String   @default("in_progress")
  resourceType                         String?
  resourceId                           String?
  safeResultSummary                    String?
  createdAt                            DateTime @default(now())
  updatedAt                            DateTime @updatedAt
  expiresAt                            DateTime?

  @@unique([schoolId, operation, idempotencyKey], map: "p23_idem_uid")
  @@index([schoolId], map: "p23_idem_schoolId")
  @@index([operation], map: "p23_idem_op")
  @@index([idempotencyKey], map: "p23_idem_ik")
  @@index([status], map: "p23_idem_status")
  @@index([expiresAt], map: "p23_idem_exp")
  @@index([createdAt], map: "p23_idem_ca")
}

model RecoveryExecutionReadinessBoardSnapshotRecord {
  boardSnapshotId            String   @id @default(uuid())
  schoolId                   String
  studentRef                 String?
  teacherRef                 String?
  adminRef                   String?
  departmentHeadRef          String?
  resultRecoveryPlanId       String?
  resultRecoveryFollowUpId   String?
  recoveryProgressObservationId              String?
  recoveryOutcomeDecisionSummaryId           String?
  recoveryOutcomeActionBundleId              String?
  recoveryOutcomeExecutionSimulationRunId    String?
  recoveryOutcomeExecutionSimulationSummaryId String?
  recoveryLifecycleClosureReadinessId        String?
  recoveryFinalLifecycleSummaryId            String?
  recoveryExecutionAuthorizationPreviewReadinessId  String?
  recoveryExecutionAuthorizationSummaryId    String?
  boardStatus                  String   @default("draft")
  boardPriority                String   @default("normal")
  riskLevel                    String   @default("low")
  blockerStatus                String   @default("none")
  snapshotSummary              String   @db.Text
  snapshotDetailsJson          Json     @default("{}")
  laneKeysJson                 Json     @default("[]")
  cardCountsJson               Json     @default("{}")
  riskSignalsJson              Json     @default("[]")
  blockedReasonCodesJson       String[] @default([])
  sourceRefsJson               Json     @default("{}")
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p24_bsnap_sid")
  @@index([schoolId, studentRef], map: "p24_bsnap_sid_stu")
  @@index([schoolId, teacherRef], map: "p24_bsnap_sid_tch")
  @@index([schoolId, adminRef], map: "p24_bsnap_sid_adm")
  @@index([resultRecoveryPlanId], map: "p24_bsnap_plan")
  @@index([resultRecoveryFollowUpId], map: "p24_bsnap_fup")
  @@index([boardStatus], map: "p24_bsnap_status")
  @@index([boardPriority], map: "p24_bsnap_pri")
  @@index([riskLevel], map: "p24_bsnap_risk")
  @@index([blockerStatus], map: "p24_bsnap_blk")
  @@index([recoveryExecutionAuthorizationPreviewReadinessId], map: "p24_bsnap_auth")
  @@index([createdAt], map: "p24_bsnap_ca")
}

model RecoveryExecutionReadinessBoardLaneRecord {
  boardLaneId                String   @id @default(uuid())
  boardSnapshotId            String
  schoolId                   String
  studentRef                 String?
  teacherRef                 String?
  adminRef                   String?
  resultRecoveryPlanId       String?
  laneKey                    String
  laneStatus                 String   @default("draft")
  lanePriority               String   @default("normal")
  laneSummary                String   @db.Text
  laneDetailsJson            Json     @default("{}")
  cardKeysJson               Json     @default("[]")
  cardCount                  Int      @default(0)
  blockedReasonCodesJson     String[] @default([])
  sourceRefsJson             Json     @default("{}")
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  voidedAt                   DateTime?

  @@index([boardSnapshotId], map: "p24_blane_snap")
  @@index([schoolId], map: "p24_blane_sid")
  @@index([schoolId, laneKey], map: "p24_blane_sid_key")
  @@index([laneKey], map: "p24_blane_key")
  @@index([laneStatus], map: "p24_blane_status")
  @@index([resultRecoveryPlanId], map: "p24_blane_plan")
  @@index([createdAt], map: "p24_blane_ca")
}

model RecoveryExecutionReadinessBoardCardRecord {
  boardCardId                String   @id @default(uuid())
  boardSnapshotId            String
  boardLaneId                String?
  schoolId                   String
  studentRef                 String?
  teacherRef                 String?
  adminRef                   String?
  resultRecoveryPlanId       String?
  laneKey                    String
  cardKey                    String
  cardStatus                 String   @default("draft")
  cardPriority               String   @default("normal")
  riskLevel                  String   @default("low")
  cardSummary                String   @db.Text
  cardDetailsJson            Json     @default("{}")
  blockedReasonCodesJson     String[] @default([])
  sourceRefsJson             Json     @default("{}")
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  voidedAt                   DateTime?

  @@index([boardSnapshotId], map: "p24_bcard_snap")
  @@index([boardLaneId], map: "p24_bcard_lane")
  @@index([schoolId], map: "p24_bcard_sid")
  @@index([schoolId, studentRef], map: "p24_bcard_sid_stu")
  @@index([resultRecoveryPlanId], map: "p24_bcard_plan")
  @@index([laneKey], map: "p24_bcard_key")
  @@index([cardStatus], map: "p24_bcard_status")
  @@index([cardPriority], map: "p24_bcard_pri")
  @@index([riskLevel], map: "p24_bcard_risk")
  @@index([createdAt], map: "p24_bcard_ca")
}

model RecoveryExecutionReadinessBoardFilterPresetRecord {
  boardFilterPresetId        String   @id @default(uuid())
  schoolId                   String
  actorId                    String?
  actorRole                  String?
  presetName                 String
  presetStatus               String   @default("draft")
  filterCriteriaJson         Json     @default("{}")
  laneFiltersJson            Json     @default("{}")
  statusFiltersJson          Json     @default("{}")
  riskFiltersJson            Json     @default("{}")
  priorityFiltersJson        Json     @default("{}")
  sourceRefsJson             Json     @default("{}")
  blockedReasonCodesJson     String[] @default([])
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt

  @@index([schoolId], map: "p24_bfilt_sid")
  @@index([schoolId, actorId], map: "p24_bfilt_sid_act")
  @@index([schoolId, actorRole], map: "p24_bfilt_sid_role")
  @@index([presetStatus], map: "p24_bfilt_status")
  @@index([createdAt], map: "p24_bfilt_ca")
}

model RecoveryExecutionReadinessBoardRiskSignalRecord {
  boardRiskSignalId          String   @id @default(uuid())
  boardSnapshotId            String?
  schoolId                   String
  studentRef                 String?
  teacherRef                 String?
  adminRef                   String?
  resultRecoveryPlanId       String?
  laneKey                    String?
  cardKey                    String?
  riskLevel                  String   @default("low")
  riskStatus                 String   @default("draft")
  riskCategory               String
  riskSummary                String   @db.Text
  riskDetailsJson            Json     @default("{}")
  mitigationsJson            Json     @default("{}")
  blockedReasonCodesJson     String[] @default([])
  sourceRefsJson             Json     @default("{}")
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  voidedAt                   DateTime?

  @@index([boardSnapshotId], map: "p24_brsk_snap")
  @@index([schoolId], map: "p24_brsk_sid")
  @@index([schoolId, studentRef], map: "p24_brsk_sid_stu")
  @@index([resultRecoveryPlanId], map: "p24_brsk_plan")
  @@index([riskLevel], map: "p24_brsk_level")
  @@index([riskStatus], map: "p24_brsk_status")
  @@index([riskCategory], map: "p24_brsk_cat")
  @@index([createdAt], map: "p24_brsk_ca")
}

model RecoveryExecutionReadinessBoardBlockerRecord {
  boardBlockerId             String   @id @default(uuid())
  boardSnapshotId            String?
  schoolId                   String
  studentRef                 String?
  teacherRef                 String?
  adminRef                   String?
  resultRecoveryPlanId       String?
  laneKey                    String?
  cardKey                    String?
  blockerStatus              String   @default("open")
  blockerPriority            String   @default("normal")
  blockerCategory            String
  blockerSummary             String   @db.Text
  blockerDetailsJson         Json     @default("{}")
  resolutionNotesJson        Json     @default("{}")
  blockedReasonCodesJson     String[] @default([])
  sourceRefsJson             Json     @default("{}")
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  resolvedAt                 DateTime?
  voidedAt                   DateTime?

  @@index([boardSnapshotId], map: "p24_bblk_snap")
  @@index([schoolId], map: "p24_bblk_sid")
  @@index([schoolId, studentRef], map: "p24_bblk_sid_stu")
  @@index([resultRecoveryPlanId], map: "p24_bblk_plan")
  @@index([blockerStatus], map: "p24_bblk_status")
  @@index([blockerPriority], map: "p24_bblk_pri")
  @@index([blockerCategory], map: "p24_bblk_cat")
  @@index([createdAt], map: "p24_bblk_ca")
}

model RecoveryExecutionReadinessBoardGovernanceNoteRecord {
  boardGovernanceNoteId      String   @id @default(uuid())
  boardSnapshotId            String?
  schoolId                   String
  studentRef                 String?
  teacherRef                 String?
  adminRef                   String?
  resultRecoveryPlanId       String?
  laneKey                    String?
  cardKey                    String?
  noteStatus                 String   @default("draft")
  noteCategory               String
  noteSummary                String   @db.Text
  noteDetailsJson            Json     @default("{}")
  blockedReasonCodesJson     String[] @default([])
  sourceRefsJson             Json     @default("{}")
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  voidedAt                   DateTime?

  @@index([boardSnapshotId], map: "p24_bgov_snap")
  @@index([schoolId], map: "p24_bgov_sid")
  @@index([schoolId, studentRef], map: "p24_bgov_sid_stu")
  @@index([resultRecoveryPlanId], map: "p24_bgov_plan")
  @@index([noteStatus], map: "p24_bgov_status")
  @@index([noteCategory], map: "p24_bgov_cat")
  @@index([createdByActorId], map: "p24_bgov_actor")
  @@index([createdAt], map: "p24_bgov_ca")
}

model RecoveryExecutionReadinessBoardRoleProjectionRecord {
  boardRoleProjectionId      String   @id @default(uuid())
  boardSnapshotId            String?
  schoolId                   String
  targetRole                 String
  actorId                    String?
  actorRef                   String?
  projectionStatus           String   @default("draft")
  projectionSummary          String   @db.Text
  projectionDetailsJson      Json     @default("{}")
  visibleLaneKeysJson        Json     @default("[]")
  visibleCardKeysJson        Json     @default("[]")
  blockedReasonCodesJson     String[] @default([])
  sourceRefsJson             Json     @default("{}")
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  voidedAt                   DateTime?

  @@index([boardSnapshotId], map: "p24_bproj_snap")
  @@index([schoolId], map: "p24_bproj_sid")
  @@index([targetRole], map: "p24_bproj_role")
  @@index([schoolId, targetRole], map: "p24_bproj_sid_role")
  @@index([actorRef], map: "p24_bproj_actor")
  @@index([projectionStatus], map: "p24_bproj_status")
  @@index([createdAt], map: "p24_bproj_ca")
}

model RecoveryExecutionReadinessBoardTeacherQueueRecord {
  boardTeacherQueueId        String   @id @default(uuid())
  boardSnapshotId            String?
  schoolId                   String
  teacherRef                 String
  queueStatus                String   @default("draft")
  queueSummary               String   @db.Text
  queueItemsJson             Json     @default("{}")
  laneBreakdownJson          Json     @default("{}")
  pendingCardsJson           Json     @default("[]")
  reviewReadyCardsJson       Json     @default("[]")
  riskFlaggedCardsJson       Json     @default("[]")
  blockerCardsJson           Json     @default("[]")
  blockedReasonCodesJson     String[] @default([])
  sourceRefsJson             Json     @default("{}")
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  voidedAt                   DateTime?

  @@index([boardSnapshotId], map: "p24_btchq_snap")
  @@index([schoolId], map: "p24_btchq_sid")
  @@index([schoolId, teacherRef], map: "p24_btchq_sid_tch")
  @@index([queueStatus], map: "p24_btchq_status")
  @@index([createdAt], map: "p24_btchq_ca")
}

model RecoveryExecutionReadinessBoardAdminQueueRecord {
  boardAdminQueueId          String   @id @default(uuid())
  boardSnapshotId            String?
  schoolId                   String
  adminRef                   String
  queueStatus                String   @default("draft")
  queueSummary               String   @db.Text
  queueItemsJson             Json     @default("{}")
  laneBreakdownJson          Json     @default("{}")
  pendingCardsJson           Json     @default("[]")
  reviewReadyCardsJson       Json     @default("[]")
  riskFlaggedCardsJson       Json     @default("[]")
  blockerCardsJson           Json     @default("[]")
  governanceNotesJson        Json     @default("{}")
  blockedReasonCodesJson     String[] @default([])
  sourceRefsJson             Json     @default("{}")
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  voidedAt                   DateTime?

  @@index([boardSnapshotId], map: "p24_badmq_snap")
  @@index([schoolId], map: "p24_badmq_sid")
  @@index([schoolId, adminRef], map: "p24_badmq_sid_adm")
  @@index([queueStatus], map: "p24_badmq_status")
  @@index([createdAt], map: "p24_badmq_ca")
}

model RecoveryExecutionReadinessBoardStudentSafeStatusDraftRecord {
  boardStudentSafeDraftId    String   @id @default(uuid())
  boardSnapshotId            String?
  schoolId                   String
  studentRef                 String
  resultRecoveryPlanId       String?
  draftStatus                String   @default("draft")
  safeStatusSummary          String   @db.Text
  statusDetailsJson          Json     @default("{}")
  visibleLaneSummaryJson     Json     @default("{}")
  blockedReasonCodesJson     String[] @default([])
  sourceRefsJson             Json     @default("{}")
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  voidedAt                   DateTime?

  @@index([boardSnapshotId], map: "p24_bssd_snap")
  @@index([schoolId], map: "p24_bssd_sid")
  @@index([schoolId, studentRef], map: "p24_bssd_sid_stu")
  @@index([resultRecoveryPlanId], map: "p24_bssd_plan")
  @@index([draftStatus], map: "p24_bssd_status")
  @@index([createdAt], map: "p24_bssd_ca")
}

model RecoveryExecutionReadinessBoardParentSafeStatusDraftRecord {
  boardParentSafeDraftId     String   @id @default(uuid())
  boardSnapshotId            String?
  schoolId                   String
  studentRef                 String
  parentRef                  String?
  resultRecoveryPlanId       String?
  draftStatus                String   @default("draft")
  safeStatusSummary          String   @db.Text
  statusDetailsJson          Json     @default("{}")
  visibleLaneSummaryJson     Json     @default("{}")
  blockedReasonCodesJson     String[] @default([])
  sourceRefsJson             Json     @default("{}")
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  voidedAt                   DateTime?

  @@index([boardSnapshotId], map: "p24_bpsd_snap")
  @@index([schoolId], map: "p24_bpsd_sid")
  @@index([schoolId, studentRef], map: "p24_bpsd_sid_stu")
  @@index([resultRecoveryPlanId], map: "p24_bpsd_plan")
  @@index([draftStatus], map: "p24_bpsd_status")
  @@index([createdAt], map: "p24_bpsd_ca")
}

model RecoveryExecutionReadinessBoardRefreshJobRecord {
  boardRefreshJobId          String   @id @default(uuid())
  boardSnapshotId            String?
  schoolId                   String
  resultRecoveryPlanId       String?
  jobStatus                  String   @default("pending")
  jobType                    String
  jobSummary                 String   @db.Text
  refreshResultsJson         Json     @default("{}")
  snapshotsRefreshed         Int      @default(0)
  lanesRefreshed             Int      @default(0)
  cardsRefreshed             Int      @default(0)
  blockersRefreshed          Int      @default(0)
  errorDetailsJson           Json     @default("{}")
  blockedReasonCodesJson     String[] @default([])
  sourceRefsJson             Json     @default("{}")
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  completedAt                DateTime?
  voidedAt                   DateTime?

  @@index([boardSnapshotId], map: "p24_brfr_snap")
  @@index([schoolId], map: "p24_brfr_sid")
  @@index([resultRecoveryPlanId], map: "p24_brfr_plan")
  @@index([jobStatus], map: "p24_brfr_status")
  @@index([jobType], map: "p24_brfr_type")
  @@index([createdAt], map: "p24_brfr_ca")
}

model RecoveryExecutionReadinessBoardSummaryRecord {
  boardSummaryId             String   @id @default(uuid())
  boardSnapshotId            String?
  schoolId                   String
  studentRef                 String?
  teacherRef                 String?
  adminRef                   String?
  resultRecoveryPlanId       String?
  summaryStatus              String   @default("draft")
  safeSummary                String   @db.Text
  overviewJson               Json     @default("{}")
  laneStatusSummaryJson      Json     @default("{}")
  cardCountSummaryJson       Json     @default("{}")
  riskSummaryJson            Json     @default("{}")
  blockerSummaryJson         Json     @default("{}")
  governanceNotesSummaryJson Json     @default("{}")
  queueSummaryJson           Json     @default("{}")
  blockedReasonCodesJson     String[] @default([])
  sourceRefsJson             Json     @default("{}")
  createdByActorId           String
  createdByRole              String
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  voidedAt                   DateTime?

  @@index([boardSnapshotId], map: "p24_bsum_snap")
  @@index([schoolId], map: "p24_bsum_sid")
  @@index([schoolId, studentRef], map: "p24_bsum_sid_stu")
  @@index([schoolId, teacherRef], map: "p24_bsum_sid_tch")
  @@index([resultRecoveryPlanId], map: "p24_bsum_plan")
  @@index([summaryStatus], map: "p24_bsum_status")
  @@index([createdAt], map: "p24_bsum_ca")
}

model RecoveryExecutionReadinessBoardAuditRecord {
  boardAuditEventId          String   @id @default(uuid())
  schoolId                   String
  boardSnapshotId            String?
  boardLaneId                String?
  boardCardId                String?
  boardFilterPresetId        String?
  boardRiskSignalId          String?
  boardBlockerId             String?
  boardGovernanceNoteId      String?
  boardRoleProjectionId      String?
  boardTeacherQueueId        String?
  boardAdminQueueId          String?
  boardStudentSafeDraftId    String?
  boardParentSafeDraftId     String?
  boardRefreshJobId          String?
  boardSummaryId             String?
  action                     String
  actorId                    String
  actorRole                  String
  safeAuditSummary           String   @db.Text
  auditDetailsJson           Json     @default("{}")
  sourceRefsJson             Json     @default("{}")
  createdAt                  DateTime @default(now())

  @@index([schoolId], map: "p24_baud_sid")
  @@index([boardSnapshotId], map: "p24_baud_snap")
  @@index([boardLaneId], map: "p24_baud_lane")
  @@index([boardCardId], map: "p24_baud_card")
  @@index([action], map: "p24_baud_action")
  @@index([actorId], map: "p24_baud_actor")
  @@index([createdAt], map: "p24_baud_ca")
}

model RecoveryExecutionReadinessBoardIdempotencyRecord {
  boardIdempotencyId         String   @id @default(uuid())
  schoolId                   String
  operation                  String
  idempotencyKey             String
  requestHash                String
  status                     String   @default("in_progress")
  resourceType               String?
  resourceId                 String?
  safeResultSummary          String?
  createdAt                  DateTime @default(now())
  updatedAt                  DateTime @updatedAt
  expiresAt                  DateTime?

  @@unique([schoolId, operation, idempotencyKey], map: "p24_idem_uid")
  @@index([schoolId], map: "p24_idem_schoolId")
  @@index([operation], map: "p24_idem_op")
  @@index([idempotencyKey], map: "p24_idem_ik")
  @@index([status], map: "p24_idem_status")
  @@index([expiresAt], map: "p24_idem_exp")
  @@index([createdAt], map: "p24_idem_ca")
}

model RecoveryCaseTriageReadinessRecord {
  triageReadinessId             String   @id @default(uuid())
  schoolId                      String
  studentRef                    String
  resultRecoveryPlanId          String
  boardSnapshotId               String
  boardCardId                   String
  triageStatus                  String   @default("draft")
  safeReadinessSummary          String   @db.Text
  readinessChecksJson           Json     @default("{}")
  blockedReasonCodesJson        String[] @default([])
  sourceRefsJson                Json     @default("{}")
  createdByActorId              String
  createdByRole                 String
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt
  voidedAt                      DateTime?

  @@index([schoolId], map: "p25_trdy_sid")
  @@index([schoolId, studentRef], map: "p25_trdy_sid_stu")
  @@index([schoolId, resultRecoveryPlanId], map: "p25_trdy_sid_plan")
  @@index([boardSnapshotId], map: "p25_trdy_snap")
  @@index([boardCardId], map: "p25_trdy_card")
  @@index([triageStatus], map: "p25_trdy_status")
  @@index([createdAt], map: "p25_trdy_ca")
}

model RecoveryCasePriorityAssessmentRecord {
  priorityAssessmentId          String   @id @default(uuid())
  schoolId                      String
  studentRef                    String
  resultRecoveryPlanId          String
  boardSnapshotId               String
  boardCardId                   String
  assessmentVersion             String   @default("v1")
  assessmentStatus              String   @default("draft")
  priorityScore                 Int?
  priorityBand                  String?
  riskRank                      String   @default("low")
  oldestUnresolvedEvidenceAt    DateTime?
  scoringPolicyVersion          String   @default("RECOVERY_CASE_TRIAGE_PRIORITY_V1")
  safePrioritySummary           String   @db.Text
  blockedReasonCodesJson        String[] @default([])
  sourceRefsJson                Json     @default("{}")
  createdByActorId              String
  createdByRole                 String
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt
  voidedAt                      DateTime?

  @@index([schoolId], map: "p25_pas_sid")
  @@index([schoolId, studentRef], map: "p25_pas_sid_stu")
  @@index([schoolId, resultRecoveryPlanId], map: "p25_pas_sid_plan")
  @@index([boardSnapshotId], map: "p25_pas_snap")
  @@index([boardCardId], map: "p25_pas_card")
  @@index([assessmentStatus], map: "p25_pas_status")
  @@index([priorityBand], map: "p25_pas_band")
  @@index([riskRank], map: "p25_pas_risk")
  @@index([createdAt], map: "p25_pas_ca")
}

model RecoveryCasePriorityFactorRecord {
  priorityFactorId             String   @id @default(uuid())
  schoolId                     String
  priorityAssessmentId         String
  factorCode                   String
  factorWeight                 Int      @default(0)
  factorValueJson              Json     @default("{}")
  appliedPoints                Int      @default(0)
  safeFactorExplanation        String   @db.Text
  sourceRef                    String?
  createdAt                    DateTime @default(now())

  @@index([schoolId], map: "p25_pfac_sid")
  @@index([priorityAssessmentId], map: "p25_pfac_pas")
  @@index([factorCode], map: "p25_pfac_code")
  @@index([createdAt], map: "p25_pfac_ca")
}

model RecoveryCaseFairnessCheckRecord {
  fairnessCheckId              String   @id @default(uuid())
  schoolId                     String
  priorityAssessmentId         String?
  queueSnapshotId              String?
  fairnessStatus               String   @default("draft")
  policyVersion                String   @default("RECOVERY_CASE_TRIAGE_PRIORITY_V1")
  checkedFactorCodesJson       Json     @default("[]")
  prohibitedFactorMatchesJson  Json     @default("[]")
  safeFairnessSummary          String   @db.Text
  blockedReasonCodesJson       String[] @default([])
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())

  @@index([schoolId], map: "p25_fair_sid")
  @@index([priorityAssessmentId], map: "p25_fair_pas")
  @@index([queueSnapshotId], map: "p25_fair_qsnap")
  @@index([fairnessStatus], map: "p25_fair_status")
  @@index([createdAt], map: "p25_fair_ca")
}

model RecoveryCaseCapacitySnapshotRecord {
  capacitySnapshotId           String   @id @default(uuid())
  schoolId                     String
  audienceRole                 String
  reviewerRef                  String?
  teamRef                      String?
  capacityLimit                Int      @default(0)
  currentLoad                  Int      @default(0)
  availableSlots              Int      @default(0)
  capacityStatus               String   @default("draft")
  windowStart                  DateTime?
  windowEnd                    DateTime?
  safeCapacitySummary          String   @db.Text
  sourceRefsJson               Json     @default("{}")
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt

  @@index([schoolId], map: "p25_cap_sid")
  @@index([schoolId, audienceRole], map: "p25_cap_sid_role")
  @@index([reviewerRef], map: "p25_cap_reviewer")
  @@index([teamRef], map: "p25_cap_team")
  @@index([capacityStatus], map: "p25_cap_status")
  @@index([createdAt], map: "p25_cap_ca")
}

model RecoveryCaseTriageQueueSnapshotRecord {
  queueSnapshotId              String   @id @default(uuid())
  schoolId                     String
  audienceRole                 String
  queueStatus                  String   @default("draft")
  orderingPolicyVersion        String   @default("RECOVERY_CASE_TRIAGE_PRIORITY_V1")
  capacitySnapshotId           String?
  candidateCount               Int      @default(0)
  rankedCount                  Int      @default(0)
  blockedCount                 Int      @default(0)
  duplicateSuppressedCount     Int      @default(0)
  capacityExceeded             Boolean  @default(false)
  generatedAt                  DateTime?
  safeQueueSummary             String   @db.Text
  sourceRefsJson               Json     @default("{}")
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p25_qsnap_sid")
  @@index([schoolId, audienceRole], map: "p25_qsnap_sid_role")
  @@index([queueStatus], map: "p25_qsnap_status")
  @@index([capacitySnapshotId], map: "p25_qsnap_cap")
  @@index([createdAt], map: "p25_qsnap_ca")
}

model RecoveryCaseTriageQueueItemRecord {
  queueItemId                  String   @id @default(uuid())
  schoolId                     String
  queueSnapshotId              String
  priorityAssessmentId         String?
  boardCardId                  String
  resultRecoveryPlanId         String
  studentRef                   String
  rank                         Int      @default(0)
  priorityScore                Int      @default(0)
  priorityBand                 String   @default("deferred")
  triageDecision               String   @default("queued")
  queueItemStatus              String   @default("queued")
  safeItemSummary              String   @db.Text
  blockedReasonCodesJson       String[] @default([])
  sourceRefsJson               Json     @default("{}")
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p25_qitem_sid")
  @@index([queueSnapshotId], map: "p25_qitem_qsnap")
  @@index([priorityAssessmentId], map: "p25_qitem_pas")
  @@index([boardCardId], map: "p25_qitem_card")
  @@index([studentRef], map: "p25_qitem_stu")
  @@index([resultRecoveryPlanId], map: "p25_qitem_plan")
  @@index([rank], map: "p25_qitem_rank")
  @@index([priorityBand], map: "p25_qitem_band")
  @@index([triageDecision], map: "p25_qitem_dec")
  @@index([queueItemStatus], map: "p25_qitem_status")
  @@index([createdAt], map: "p25_qitem_ca")
}

model RecoveryCaseWorkloadAllocationDraftRecord {
  allocationDraftId            String   @id @default(uuid())
  schoolId                     String
  queueItemId                  String
  proposedReviewerRef          String
  proposedReviewerRole         String
  allocationStatus             String   @default("draft")
  capacitySnapshotId           String?
  safeAllocationSummary        String   @db.Text
  allocationReasonsJson        Json     @default("{}")
  blockedReasonCodesJson       String[] @default([])
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p25_ald_sid")
  @@index([queueItemId], map: "p25_ald_qitem")
  @@index([proposedReviewerRef], map: "p25_ald_reviewer")
  @@index([proposedReviewerRole], map: "p25_ald_role")
  @@index([allocationStatus], map: "p25_ald_status")
  @@index([capacitySnapshotId], map: "p25_ald_cap")
  @@index([createdAt], map: "p25_ald_ca")
}

model RecoveryCaseEscalationDraftRecord {
  escalationDraftId            String   @id @default(uuid())
  schoolId                     String
  queueItemId                  String
  escalationLevel              String
  proposedTargetRole           String
  escalationStatus             String   @default("draft")
  safeEscalationSummary        String   @db.Text
  reasonCodesJson              Json     @default("{}")
  blockedReasonCodesJson       String[] @default([])
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p25_escd_sid")
  @@index([queueItemId], map: "p25_escd_qitem")
  @@index([escalationLevel], map: "p25_escd_level")
  @@index([proposedTargetRole], map: "p25_escd_trole")
  @@index([escalationStatus], map: "p25_escd_status")
  @@index([createdAt], map: "p25_escd_ca")
}

model RecoveryCaseReviewWindowDraftRecord {
  reviewWindowDraftId          String   @id @default(uuid())
  schoolId                     String
  queueItemId                  String
  proposedReviewerRef          String?
  windowStart                  DateTime?
  windowEnd                    DateTime?
  windowStatus                 String   @default("draft")
  safeWindowSummary            String   @db.Text
  blockedReasonCodesJson       String[] @default([])
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p25_rwnd_sid")
  @@index([queueItemId], map: "p25_rwnd_qitem")
  @@index([proposedReviewerRef], map: "p25_rwnd_reviewer")
  @@index([windowStatus], map: "p25_rwnd_status")
  @@index([createdAt], map: "p25_rwnd_ca")
}

model RecoveryCaseQueueExplanationRecord {
  queueExplanationId           String   @id @default(uuid())
  schoolId                     String
  queueItemId                  String
  priorityAssessmentId         String
  safeExplanation              String   @db.Text
  factorBreakdownJson          Json     @default("{}")
  tieBreakExplanationJson      Json     @default("{}")
  policyVersion                String   @default("RECOVERY_CASE_TRIAGE_PRIORITY_V1")
  createdAt                    DateTime @default(now())

  @@index([schoolId], map: "p25_qexp_sid")
  @@index([queueItemId], map: "p25_qexp_qitem")
  @@index([priorityAssessmentId], map: "p25_qexp_pas")
  @@index([createdAt], map: "p25_qexp_ca")
}

model RecoveryCaseDuplicateSuppressionRecord {
  duplicateSuppressionId       String   @id @default(uuid())
  schoolId                     String
  canonicalBoardCardId         String
  duplicateBoardCardId         String
  resultRecoveryPlanId         String
  suppressionStatus            String   @default("draft")
  safeSuppressionSummary       String   @db.Text
  matchingKeysJson             Json     @default("{}")
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  voidedAt                     DateTime?

  @@index([schoolId], map: "p25_dsup_sid")
  @@index([canonicalBoardCardId], map: "p25_dsup_canon")
  @@index([duplicateBoardCardId], map: "p25_dsup_dup")
  @@index([resultRecoveryPlanId], map: "p25_dsup_plan")
  @@index([suppressionStatus], map: "p25_dsup_status")
  @@index([createdAt], map: "p25_dsup_ca")
}

model RecoveryCaseTriageSummaryRecord {
  triageSummaryId              String   @id @default(uuid())
  schoolId                     String
  studentRef                   String?
  resultRecoveryPlanId         String?
  queueSnapshotId              String?
  summaryStatus                String   @default("draft")
  safeSummary                  String   @db.Text
  priorityBandCountsJson       Json     @default("{}")
  riskCountsJson               Json     @default("{}")
  blockerCountsJson            Json     @default("{}")
  capacitySummaryJson          Json     @default("{}")
  sourceRefsJson               Json     @default("{}")
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p25_tsum_sid")
  @@index([schoolId, studentRef], map: "p25_tsum_sid_stu")
  @@index([schoolId, resultRecoveryPlanId], map: "p25_tsum_sid_plan")
  @@index([queueSnapshotId], map: "p25_tsum_qsnap")
  @@index([summaryStatus], map: "p25_tsum_status")
  @@index([createdAt], map: "p25_tsum_ca")
}

model RecoveryCaseTriageAuditRecord {
  triageAuditEventId           String   @id @default(uuid())
  schoolId                     String
  entityType                   String
  entityId                     String
  action                       String
  actorId                      String
  actorRole                    String
  correlationId                String?
  safeMetadataJson             Json     @default("{}")
  createdAt                    DateTime @default(now())

  @@index([schoolId], map: "p25_taud_sid")
  @@index([schoolId, entityType], map: "p25_taud_sid_et")
  @@index([entityId], map: "p25_taud_eid")
  @@index([action], map: "p25_taud_action")
  @@index([actorId], map: "p25_taud_actor")
  @@index([createdAt], map: "p25_taud_ca")
}

model RecoveryCaseTriageIdempotencyRecord {
  triageIdempotencyId          String   @id @default(uuid())
  schoolId                     String
  idempotencyKey               String
  operation                    String
  requestHash                  String
  responseRef                  String?
  status                       String   @default("in_progress")
  createdAt                    DateTime @default(now())
  completedAt                  DateTime?

  @@unique([schoolId, idempotencyKey, operation], map: "p25_tidem_uid")
  @@index([schoolId], map: "p25_tidem_sid")
  @@index([idempotencyKey], map: "p25_tidem_ik")
  @@index([operation], map: "p25_tidem_op")
  @@index([status], map: "p25_tidem_status")
  @@index([createdAt], map: "p25_tidem_ca")
}

// ══════════════════════════════════════════════════════════════════════
// Package 26: Recovery Case Adjudication and Review Governance Models
// ══════════════════════════════════════════════════════════════════════

model RecoveryCaseAdjudicationReadinessRecord {
  adjudicationReadinessId       String   @id @default(uuid())
  schoolId                      String
  studentRef                    String
  resultRecoveryPlanId          String
  queueItemId                   String
  priorityAssessmentId          String?
  fairnessCheckId               String?
  triageReadinessId             String?
  readinessStatus               String   @default("draft")
  safeReadinessSummary          String   @db.Text
  blockedReasonCodesJson        String[] @default([])
  sourceRefsJson                Json     @default("{}")
  createdByActorId              String
  createdByRole                 String
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt
  voidedAt                      DateTime?

  @@index([schoolId], map: "p26_ard_sid")
  @@index([schoolId, studentRef], map: "p26_ard_sid_stu")
  @@index([schoolId, resultRecoveryPlanId], map: "p26_ard_sid_plan")
  @@index([queueItemId], map: "p26_ard_qitem")
  @@index([priorityAssessmentId], map: "p26_ard_pas")
  @@index([readinessStatus], map: "p26_ard_status")
  @@index([createdAt], map: "p26_ard_ca")
}

model RecoveryCaseReviewSessionRecord {
  reviewSessionId              String   @id @default(uuid())
  schoolId                     String
  queueItemId                  String
  adjudicationReadinessId      String?
  reviewerActorId              String
  reviewerRole                 String
  sessionStatus                String   @default("draft")
  safeSessionSummary           String   @db.Text
  blockedReasonCodesJson       String[] @default([])
  sourceRefsJson               Json     @default("{}")
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p26_rsess_sid")
  @@index([schoolId, queueItemId], map: "p26_rsess_sid_qitem")
  @@index([reviewerActorId], map: "p26_rsess_reviewer")
  @@index([sessionStatus], map: "p26_rsess_status")
  @@index([createdAt], map: "p26_rsess_ca")
}

model RecoveryCaseReviewEvidenceBundleRecord {
  evidenceBundleId             String   @id @default(uuid())
  schoolId                     String
  queueItemId                  String
  priorityAssessmentId         String?
  boardSnapshotId              String?
  boardCardId                  String?
  sourceRefsJson               Json     @default("{}")
  safeEvidenceItemsJson        Json     @default("[]")
  sourceUpdatedAtJson          Json     @default("{}")
  evidenceDigest               String   @default("")
  digestAlgorithm              String   @default("SHA-256")
  bundleStatus                 String   @default("draft")
  safeBundleSummary            String   @db.Text
  blockedReasonCodesJson       String[] @default([])
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p26_eb_sid")
  @@index([schoolId, queueItemId], map: "p26_eb_sid_qitem")
  @@index([bundleStatus], map: "p26_eb_status")
  @@index([createdAt], map: "p26_eb_ca")
}

model RecoveryCaseReviewChecklistRecord {
  reviewChecklistId            String   @id @default(uuid())
  schoolId                     String
  queueItemId                  String
  evidenceBundleId             String?
  conflictDeclarationId        String?
  checklistOutcome             String   @default("pending")
  checklistResultsJson         Json     @default("{}")
  safeChecklistSummary         String   @db.Text
  blockedReasonCodesJson       String[] @default([])
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p26_cl_sid")
  @@index([schoolId, queueItemId], map: "p26_cl_sid_qitem")
  @@index([checklistOutcome], map: "p26_cl_outcome")
  @@index([createdAt], map: "p26_cl_ca")
}

model RecoveryCaseConflictOfInterestDeclarationRecord {
  conflictDeclarationId        String   @id @default(uuid())
  schoolId                     String
  queueItemId                  String
  reviewerActorId              String
  reviewerRole                 String
  conflictType                 String   @default("none_declared")
  conflictStatus               String   @default("draft")
  safeDeclarationSummary       String   @db.Text
  blockedReasonCodesJson       String[] @default([])
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p26_coi_sid")
  @@index([schoolId, queueItemId], map: "p26_coi_sid_qitem")
  @@index([reviewerActorId], map: "p26_coi_reviewer")
  @@index([conflictStatus], map: "p26_coi_status")
  @@index([createdAt], map: "p26_coi_ca")
}

model RecoveryCaseReviewerDecisionDraftRecord {
  reviewerDecisionId           String   @id @default(uuid())
  schoolId                     String
  queueItemId                  String
  reviewSessionId              String?
  reviewerActorId              String
  reviewerRole                 String
  reviewerPosition             String   @default("primary")
  decisionCode                 String   @default("no_change")
  currentPriorityScore         Int?
  currentPriorityBand          String?
  recommendedPriorityBand     String?
  safeDecisionSummary          String   @db.Text
  reasonCodesJson              Json     @default("{}")
  evidenceBundleId             String?
  checklistId                  String?
  conflictDeclarationId        String?
  sourceRefsJson               Json     @default("{}")
  decisionStatus               String   @default("draft")
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p26_rd_sid")
  @@index([schoolId, queueItemId], map: "p26_rd_sid_qitem")
  @@index([reviewSessionId], map: "p26_rd_session")
  @@index([reviewerActorId], map: "p26_rd_reviewer")
  @@index([decisionCode], map: "p26_rd_code")
  @@index([decisionStatus], map: "p26_rd_status")
  @@index([createdAt], map: "p26_rd_ca")
}

model RecoveryCasePriorityOverrideRequestRecord {
  priorityOverrideRequestId    String   @id @default(uuid())
  schoolId                     String
  queueItemId                  String
  priorityAssessmentId         String
  currentPriorityScore         Int?
  currentPriorityBand          String?
  requestedPriorityBand        String
  safeOverrideRationale        String   @db.Text
  reasonCodesJson              Json     @default("{}")
  supportingDecisionIdsJson    Json     @default("[]")
  supportingEvidenceBundleIdsJson Json  @default("[]")
  overrideStatus               String   @default("draft")
  blockedReasonCodesJson       String[] @default([])
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p26_po_sid")
  @@index([schoolId, queueItemId], map: "p26_po_sid_qitem")
  @@index([priorityAssessmentId], map: "p26_po_pas")
  @@index([overrideStatus], map: "p26_po_status")
  @@index([createdAt], map: "p26_po_ca")
}

model RecoveryCaseSecondReviewRequestRecord {
  secondReviewRequestId        String   @id @default(uuid())
  schoolId                     String
  queueItemId                  String
  primaryDecisionId            String
  requestedReviewerRole        String
  requestReasonCodesJson       Json     @default("{}")
  safeRequestSummary           String   @db.Text
  requestStatus                String   @default("draft")
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p26_sr_sid")
  @@index([schoolId, queueItemId], map: "p26_sr_sid_qitem")
  @@index([primaryDecisionId], map: "p26_sr_pdec")
  @@index([requestStatus], map: "p26_sr_status")
  @@index([createdAt], map: "p26_sr_ca")
}

model RecoveryCaseReviewerConsensusRecord {
  consensusId                  String   @id @default(uuid())
  schoolId                     String
  queueItemId                  String
  primaryDecisionId            String?
  secondaryDecisionId          String?
  resultRecoveryPlanId         String?
  consensusStatus              String   @default("pending")
  safeConsensusSummary         String   @db.Text
  blockedReasonCodesJson       String[] @default([])
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p26_cs_sid")
  @@index([schoolId, queueItemId], map: "p26_cs_sid_qitem")
  @@index([consensusStatus], map: "p26_cs_status")
  @@index([createdAt], map: "p26_cs_ca")
}

model RecoveryCaseDisagreementResolutionDraftRecord {
  disagreementResolutionDraftId String   @id @default(uuid())
  schoolId                      String
  queueItemId                   String
  consensusId                   String?
  primaryDecisionId             String?
  secondaryDecisionId           String?
  safeDisagreementSummary       String   @db.Text
  reasonCodeComparisonJson      Json     @default("{}")
  evidenceGapsJson              Json     @default("[]")
  proposedGovernanceRole        String?
  proposedResolutionOptionsJson Json     @default("[]")
  draftStatus                   String   @default("draft")
  blockedReasonCodesJson        String[] @default([])
  createdByActorId              String
  createdByRole                 String
  createdAt                     DateTime @default(now())
  updatedAt                     DateTime @updatedAt
  voidedAt                      DateTime?

  @@index([schoolId], map: "p26_dr_sid")
  @@index([schoolId, queueItemId], map: "p26_dr_sid_qitem")
  @@index([consensusId], map: "p26_dr_cs")
  @@index([draftStatus], map: "p26_dr_status")
  @@index([createdAt], map: "p26_dr_ca")
}

model RecoveryCaseQueueDispositionRecord {
  queueDispositionId           String   @id @default(uuid())
  schoolId                     String
  queueItemId                  String
  consensusId                  String?
  disagreementResolutionDraftId String?
  priorityOverrideRequestId    String?
  dispositionCode              String   @default("retain_in_queue")
  dispositionStatus            String   @default("draft")
  safeDispositionSummary       String   @db.Text
  reasonCodesJson              Json     @default("{}")
  sourceRefsJson               Json     @default("{}")
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p26_qd_sid")
  @@index([schoolId, queueItemId], map: "p26_qd_sid_qitem")
  @@index([dispositionCode], map: "p26_qd_code")
  @@index([dispositionStatus], map: "p26_qd_status")
  @@index([createdAt], map: "p26_qd_ca")
}

model RecoveryCaseQualitySampleRecord {
  qualitySampleId              String   @id @default(uuid())
  schoolId                     String
  queueItemId                  String
  priorityBand                 String
  selected                     Boolean  @default(false)
  bucket                       Int      @default(0)
  sampleBasisPoints            Int      @default(0)
  policyVersion                String   @default("RECOVERY_CASE_ADJUDICATION_QUALITY_V1")
  sampleStatus                 String   @default("draft")
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  voidedAt                     DateTime?

  @@index([schoolId], map: "p26_qs_sid")
  @@index([schoolId, queueItemId], map: "p26_qs_sid_qitem")
  @@index([selected], map: "p26_qs_sel")
  @@index([priorityBand], map: "p26_qs_band")
  @@index([policyVersion], map: "p26_qs_pv")
  @@index([createdAt], map: "p26_qs_ca")
}

model RecoveryCaseAdjudicationSummaryRecord {
  adjudicationSummaryId        String   @id @default(uuid())
  schoolId                     String
  studentRef                   String?
  resultRecoveryPlanId         String?
  queueItemId                  String?
  summaryStatus                String   @default("draft")
  safeSummary                  String   @db.Text
  adjudicationCountsJson       Json     @default("{}")
  consensusCountsJson          Json     @default("{}")
  disagreementCountsJson       Json     @default("{}")
  dispositionCountsJson        Json     @default("{}")
  sourceRefsJson               Json     @default("{}")
  createdByActorId             String
  createdByRole                String
  createdAt                    DateTime @default(now())
  updatedAt                    DateTime @updatedAt
  voidedAt                     DateTime?

  @@index([schoolId], map: "p26_as_sid")
  @@index([schoolId, studentRef], map: "p26_as_sid_stu")
  @@index([schoolId, resultRecoveryPlanId], map: "p26_as_sid_plan")
  @@index([queueItemId], map: "p26_as_qitem")
  @@index([summaryStatus], map: "p26_as_status")
  @@index([createdAt], map: "p26_as_ca")
}

model RecoveryCaseAdjudicationAuditRecord {
  adjudicationAuditEventId     String   @id @default(uuid())
  schoolId                     String
  entityType                   String
  entityId                     String
  action                       String
  actorId                      String
  actorRole                    String
  correlationId                String?
  safeMetadataJson             Json     @default("{}")
  createdAt                    DateTime @default(now())

  @@index([schoolId], map: "p26_aaud_sid")
  @@index([schoolId, entityType], map: "p26_aaud_sid_et")
  @@index([entityId], map: "p26_aaud_eid")
  @@index([action], map: "p26_aaud_action")
  @@index([actorId], map: "p26_aaud_actor")
  @@index([createdAt], map: "p26_aaud_ca")
}

model RecoveryCaseAdjudicationIdempotencyRecord {
  adjudicationIdempotencyId    String   @id @default(uuid())
  schoolId                     String
  idempotencyKey               String
  operation                    String
  requestHash                  String
  responseRef                  String?
  status                       String   @default("in_progress")
  createdAt                    DateTime @default(now())
  completedAt                  DateTime?

  @@unique([schoolId, idempotencyKey, operation], map: "p26_aidem_uid")
  @@index([schoolId], map: "p26_aidem_sid")
  @@index([idempotencyKey], map: "p26_aidem_ik")
  @@index([operation], map: "p26_aidem_op")
  @@index([status], map: "p26_aidem_status")
  @@index([createdAt], map: "p26_aidem_ca")
}
