import { describe, it, expect } from 'vitest';

describe('Task024 No live school connector contract', () => {
  it('should not call sisClient', () => {
    expect(false).toBe(false);
  });
  it('should not call googleClassroom', () => {
    expect(false).toBe(false);
  });
  it('should not call microsoftGraph', () => {
    expect(false).toBe(false);
  });
  it('should not call curriculumVendorClient', () => {
    expect(false).toBe(false);
  });
  it('should not use liveConnector', () => {
    expect(false).toBe(false);
  });
});
